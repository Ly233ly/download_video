# 项目上下文

## 产品目标

留底下载器（品牌名“留底”）是一款免费开源的 Windows 本机媒体下载与归档工具。它把浏览器、微信视频号和 IDM 中的媒体变成用户可识别、可选择、可验证的本机文件，并按需通过 Eagle 官方 API 导入当前资源库。宣传口号为“想留的，留个底。”

## 领域术语

- **媒体候选组**：同一内容的标题、预览、质量、音轨和技术地址集合。
- **下载计划**：用户确认后的持久任务意图，包含来源、输出方式、所选质量和文件安全选项。
- **仅下载**：文件在本机完成并保留，不自动进入 Eagle。
- **导入 Eagle**：下载验证完成后通过 Eagle 官方 API 创建导入任务。
- **导入后删除**：用户明确选择后，在 Eagle 成功与文件归属复验通过时删除程序创建的最终副本。
- **可靠来源**：由浏览器或明确事件提供、能够证明属于当前媒体的网页地址。
- **技术资源**：播放分片、预加载请求或无法归属具体内容的底层地址，不作为默认可下载内容。
- **捕获租约**：视频号捕获期间由程序拥有的代理、端口、证书和恢复状态。
- **当前视频绑定**：只由卡片 ID、稳定媒体标识、权威详情主对象、当前视频封面或完整标题等强证据建立的短期 `objectId` 归属。

## 当前边界

- 浏览器负责发现、展示、选择和提交，不负责最终下载与文件管理。
- 桌面端负责持久状态、下载、合并、校验、Eagle 和安全删除。
- IDM hook 只快速入队和唤醒，不做网络、哈希或长时间等待。
- 微信视频号是用户显式启用的独立桌面来源，不属于浏览器扩展或 IDM。
- Eagle 只通过官方本地 Web API 访问。
- Eagle 是可选归档能力，不是下载引擎依赖；浏览器和视频号在 Eagle 不可用时必须自动走本机保留路线。IDM 入口只负责导入已经存在的原文件，因此可以等待 Eagle，但不得影响其他下载入口。
- DRM 只识别和提示，不绕过。

## 不可破坏的规则

- 无来源 IDM 文件仍然导入；有可靠来源时才写入来源，不猜测网址。
- 网站关闭只接受扩展明确事件，不能从文件反推网站。
- 用户原下载文件、IDM 文件和目录外文件不得移动、删除或修改。
- Eagle 能力未知或明确不可用时，不得承诺导入或删除本机副本；明确请求导入的旧客户端必须由桌面 API 安全降级为仅下载。
- 补导对同一计划必须是原子且幂等的；并发点击只能绑定一条导入任务。保留期删除必须同时匹配计划路径、导入任务路径和程序“已完成”目录。
- 可选服务探测、临时数据库/文件系统异常和 UI 异步旧响应不得杀死后台循环、覆盖新状态或阻断本机下载。
- 自绘 UI 必须在首次映射时完成最终圆角绘制，销毁时取消待执行回调；圆角容器的矩形内容层不得覆盖抗锯齿圆弧。
- 只有程序创建且有明确计划归属的文件可以按规则清理。
- Cookie、Authorization、签名地址、配对秘密和视频号解密信息不得进入普通持久状态或发行包。
- 本机 API 与捕获代理只监听回环地址。
- 系统代理、证书、注册表和安装目录只能在验证归属后修改或恢复。
- 自动更新只信任固定 GitHub 仓库的正式 Release、唯一 Windows 资产和 GitHub SHA-256；重定向、大小和 ZIP 结构必须受控，失败不得启动安装器。

## 当前人工边界

- 浏览器安全策略要求用户亲自加载或重载开发扩展。
- Windows 安全策略要求用户亲自确认视频号捕获证书。
- 用户实际 VPN 和微信环境始终属于人工复验边界；2026-08-21 已完成当前视频号热修复的真实闭环，后续修改捕获、代理或当前绑定时必须重新复验。
- 刷新、轮询和页面切换必须以最新代次提交状态；旧响应、超时快照和已关闭视图不得覆盖当前页面。
- 外部工具能力探测必须并发共享且有界；历史任务列表、摘要和状态传播不得依赖无索引全表排序或无界候选扫描。
