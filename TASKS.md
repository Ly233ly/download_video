# 任务列表

状态：`[x]` 完成，`[-]` 已实现待人工复验，`[ ]` 未完成。

## 1.0.0：cat-catch 全功能迁移（一次性交付）

目标、范围和技术边界见 [迁移总计划](docs/CAT_CATCH_MIGRATION_PLAN.md)，逐项功能覆盖见 [功能对照矩阵](docs/FEATURE_PARITY_MATRIX.md)。以下任务可以独立实现和验证，但只有全部通过后才发布 1.0.0，不发布删减功能的正式中间版本。

- [x] [01 许可与来源门禁](docs/tasks/01-license-and-provenance-gate.md)：GPL-3.0 组合发行、固定上游快照、MIT 保留通知、对应源码和构建材料已齐全。
- [x] [02 可见的直链媒体纵向闭环](docs/tasks/02-visible-direct-media-tracer.md)：捕获、默认候选区、本机下载计划和最终 Eagle 队列闭环已实现。
- [x] [03 捕获、筛选和会话模型](docs/tasks/03-capture-filter-session-parity.md)：上游捕获/过滤/会话能力完整保留，危险正则增加拒绝门禁。
- [x] [04 丰富预览与下载选择](docs/tasks/04-rich-preview-and-selection.md)：标题、封面、类型、清晰度、音轨、编码、大小、文件名和方案默认可见。
- [x] [05 本机 FFmpeg](docs/tasks/05-local-and-browser-ffmpeg.md)：固定 FFmpeg 8.1.2 统一负责普通直链、分轨和 HLS/DASH 下载与 streamcopy；新版可见界面已移除浏览器/第三方 FFmpeg 路由。
- [x] [06 HLS 端到端](docs/tasks/06-hls-end-to-end.md)：上游完整 HLS 工具保留，本机清单拉流、AES-128 和 ffprobe 验证通过。
- [x] [07 DASH/B 站端到端](docs/tasks/07-dash-bilibili-end-to-end.md)：B 站页面桥归组分轨，公共 DASH 实际拉流与本机合并通过。
- [x] [08 深度搜索与页面注入](docs/tasks/08-deep-search-and-page-instrumentation.md)：XHR/fetch/Worker/JSON 深搜入口与恢复路径完整保留。
- [x] [09 缓存捕获与录制模式](docs/tasks/09-cache-and-recording-modes.md)：MSE/Cache、页面、屏幕和 WebRTC 录制运行时代码与入口完整保留。
- [x] [10 下载路由与外部集成](docs/tasks/10-download-routing-and-integrations.md)：插件主下载路由固定为本机软件；旧自动下载、完整浏览器下载器与外部下载目标不进入新版可见界面，IDM 原有导入流程独立保留。
- [x] [11 媒体工具与快捷命令](docs/tasks/11-media-tools-and-commands.md)：页面媒体控制、请求头/UA、深搜、缓存和录制保留并集中到高级捕获；旧二维码、手工下载、右键下载和非主动作快捷命令已由 1.1.1 收敛删除。
- [x] [12 设置、国际化与浏览器兼容](docs/tasks/12-settings-i18n-and-browser-compat.md)：Chrome/Edge/Firefox 清单、上游 locales 和新增简/繁/英候选界面已交付。
- [x] [13 安全、DRM 与临时文件归属](docs/tasks/13-security-drm-and-temp-ownership.md)：敏感上下文仅在本机任务内存、DRM 阻断、消息认证、固定临时根和重启后拒绝猜测 URL 已有自动测试。
- [x] [14 安装、更新与回滚](docs/tasks/14-installer-update-and-rollback.md)：FFmpeg/扩展/源码/许可已入包，0.6.0 迁移、失败回退和卸载纳入隔离测试。
- [-] [15 一次性功能对等发布](docs/tasks/15-one-shot-parity-release.md)：发行候选和自动证据齐全；Chrome 工具栏视觉、缓存/录制真实授权内容与维护者 GitHub Release 签字仍需人工完成。

## 1.1.0：方案 2 弹窗重构

- [-] [16 扩展弹窗 UI 与交互逻辑重构](docs/tasks/16-extension-popup-ui-redesign.md)：单一界面、内容归组、逐组选择、多任务、高级捕获重排、字幕 sidecar、统一 `desktop` 路由、任务状态同步、真实帧、首次快照和统一计数已完成；88 项测试通过，仅待 Chrome 最终视觉 QA。
- [x] [17 本机统一下载与桌面任务 UI](docs/tasks/17-desktop-unified-download.md)：删除 bridge 的 Chrome 组件下载/回传代码及接口；普通直链、分轨、HLS/DASH 和“仅下载”统一由本机软件执行，schema 5、持久阶段、大小进度、视频预览、桌面双标签任务 UI、停止/重试和重启恢复口径均已实现。

## 1.1.1：候选可识别性与单运行时收敛

- [x] 18 稳定播放器归组：签名查询轮换按播放器身份与媒体路径收敛，MPD/M3U8 存在时隐藏传输分片，仅分片候选阻止提交（A115–A116）。
- [x] 19 真实帧链路：同源 canvas 当前帧 + 跨域播放器矩形裁剪，帧只在扩展内存并受格式/长度门禁（A113–A114）。
- [x] 20 popup 单运行时：删除隐藏上游 DOM、`popup.js`、`media-control.js` 与 popup 的 jQuery/MQTT/HLS 依赖；主导航收敛为媒体/任务，高级捕获集中设置（A96、A100、A117）。
- [x] 21 旧旁路清理：删除右键下载、非主动作快捷命令及对应权限/设置；普通链接、分轨和清单仍只创建本机 `desktop` 任务（A106、A118）。
- [-] 22 1.1.1 发行复验：83 项自动测试、JS/清单、补丁升级自重载、冻结构建、覆盖安装、废弃脚本清理、发行哈希和 Behance 播放实页已通过（A102）；仅 Chrome popup 同视口最终视觉截图待人工点验（A101）。

## 1.1.2：质量目录与分片收敛

- [x] 23 结构化质量目录：Vimeo 播放器配置转换为同一候选组的完整 HLS 主清单与降序质量档位，不持久化签名地址（A119）。
- [x] 24 清晰版本选择：当前播放质量与可下载质量分开显示，默认推荐最高档；同组存在完整清单时隐藏普通 MP4/音频分片（A120）。
- [-] 25 本机质量执行与发行复验：ffprobe 按用户所选高度定位对应节目流，FFmpeg streamcopy；84 项源码测试、冻结构建、隔离安装器、当前电脑覆盖安装、发行哈希和 Behance 实页结构化质量目录均通过，仅 Chrome 工具栏 popup 同视口截图待人工点验（A121）。

## 1.1.3：动态质量数量与通用清单目录

- [x] 26 动态档位语义：质量选择器显示“本视频 N 档”，不使用固定五档说明；任意数量按高度降序并默认推荐当前视频最高档（A122）。
- [x] 27 通用主清单发现：对可访问 HLS/DASH 清单在 2 MB/4 秒门禁内提取明确分辨率；失败不猜测、不阻塞本机下载（A123）。
- [-] 28 1.1.3 发行复验：8 档 Vimeo/HLS/桌面节目、3 档 DASH、84 项全量测试、冻结构建、隔离安装器、当前电脑覆盖安装与发行哈希均通过；仅 Chrome 工具栏 popup 同视口截图待人工点验（A124）。

## 1.1.4：任务同步、输出可达与通用预览

- [x] [29 任务事实源恢复](docs/tasks/29-task-state-recovery.md)：完成态在 popup 强制为 100%，轮询异常可见并自动重连，失效令牌自动重新配对或回到配对态（A125）。
- [x] [30 仅下载结果可达](docs/tasks/30-download-output-access.md)：任务显示最终完整路径，并只按服务端任务 ID 打开经归属校验的 `下载中转站/已完成` 文件夹（A126）。
- [x] [31 通用任务预览](docs/tasks/31-site-agnostic-task-preview.md)：创建时绑定通用当前视频帧，重开后读取本机 FFmpeg 真实预览帧；接口限制在程序预览目录、PNG 和 2 MB 内，不设置示例站点白名单（A127）。
- [x] [32 1.1.4 发行复验](docs/RELEASE_VERIFICATION_1.1.4.md)：88 项全量测试、冻结构建、隔离安装器、当前电脑覆盖安装、源码/安装目录哈希和 ZIP 哈希均通过（A128）。

## 1.1.5：动作真实性与配对恢复

- [x] [33 仅下载动作真实性](docs/tasks/33-download-action-truth.md)：未配对或选择无效时不创建计划、不显示成功；单项和批量“仅下载”与主按钮使用同一可用性门禁，popup 打开时主动重试自动配对（A129）。
- [-] [34 1.1.5 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.5.md)：完成全量回归、冻结运行时、隔离安装器、当前电脑覆盖安装、Chrome 正确副本重载、真实 `planId`/任务列表/仅下载结果验证后转为完成（A130）。

## 1.1.6：配对竞态与持久连接

- [x] [35 配对令牌竞态修复](docs/tasks/35-pairing-token-race.md)：旧 401 只允许清除发起请求时使用且仍为当前值的令牌；存在更新令牌时改用新令牌重试，配对成功提示受认证健康检查约束（A131）。
- [ ] [36 1.1.6 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.6.md)：自动门禁、构建、安装与扩展重载完成，但真实配对仍被并发旧状态写回清空，A132 未通过；该发行不继续推广，由 1.1.7 任务 37–38 接替。

## 1.1.7：状态写入原子性

- [x] [37 扩展状态串行写入](docs/tasks/37-extension-state-serialization.md)：令牌、事件队列和任务摘要共享串行读改写队列；令牌清除在队列内条件比较，避免任何旧字段写回覆盖新配对（A133）。
- [ ] [38 1.1.7 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.7.md)：89 项全量回归、冻结运行时、隔离安装器、当前电脑覆盖安装和关键文件哈希通过；真实配对 POST 成功后受认证 GET 仍返回 401 并清除令牌，A134 未通过，由 1.1.8 接替。

## 1.1.8：认证读取传输兼容

- [x] [39 认证读取统一 POST](docs/tasks/39-authenticated-read-post.md)：健康、任务列表、详情和预览全部使用带 Origin 与请求体令牌的认证 POST；保留旧 GET 供兼容，但 popup 不再依赖其请求头行为（A135）。
- [ ] [40 1.1.8 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.8.md)：90 项全量回归、冻结运行时、隔离安装器、当前电脑覆盖安装、持久配对和认证 POST 200 通过；真实抖音任务完成后内容与所选预览不一致，A136 未通过，由 1.1.9 接替。

## 1.1.9：媒体身份一致性

- [x] [41 请求与播放器强匹配](docs/tasks/41-media-identity-consistency.md)：只有媒体 URL 与播放器真实来源匹配时才绑定帧、时长和内容组；未匹配预加载请求不继承当前可见视频，本机输出时长二次校验并阻止错配交付（A137）。
- [ ] [42 1.1.9 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.9.md)：91 项全量回归、冻结运行时、隔离安装器、当前电脑覆盖安装和关键文件哈希均通过；实机证明错配防线生效，但同页 4 个候选只有 1 个可见预览，A138 未完整关闭，由 1.1.10 接替。

## 1.1.10：通用候选自身预览

- [x] [43 无静态帧候选预览自身直链](docs/tasks/43-candidate-self-preview.md)：普通视频没有可信页面帧时，预览当前所选下载 URL 自己的媒体画面；切换版本同步切换预览，失败回退占位，音频不进入视频预览（A139）。
- [-] [44 1.1.10 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.10.md)：91 项全量回归、冻结运行时、隔离安装器、当前电脑覆盖安装、在线健康门和关键文件哈希均通过；等待 Chrome 手动重载后验证抖音 4 个候选的各自画面及同内容下载闭环（A140）。

## 1.1.11：同内容 CDN 备用链接归并

- [x] [45 强媒体别名归并](docs/tasks/45-media-alias-deduplication.md)：同页同类型下以响应指纹，或长媒体路径加精确字节数识别 CDN 备用地址；唯一归入已有播放器组、折叠重复版本并保留真实预览。文件大小不能单独触发归并（A141）。
- [-] [46 1.1.11 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.11.md)：91 项回归、冻结运行时、隔离安装器、当前电脑覆盖安装、在线健康门和关键文件哈希均通过；等待 Chrome popup 肉眼确认截图中的两个 52 MB 项只显示为一个，并完成下载内容闭环（A142）。

## 1.1.12：固定字节 MP4 分片门禁

- [x] [47 固定 Range 分片双层阻断](docs/tasks/47-fixed-range-fragment-guard.md)：按 URL 中明文/base64url Range 与精确区间长度识别伪装成 MP4 的固定字节分片；扩展隐藏/阻断，本机创建与重试前再次拒绝，不按站点写死（A143）。
- [-] [48 1.1.12 发行与实机复验](docs/RELEASE_VERIFICATION_1.1.12.md)：92 项全量回归、冻结运行时、隔离安装器、当前电脑覆盖安装、在线健康门和关键文件哈希均通过；等待 Chrome 重载并刷新 Behance 来源页，确认只显示完整 HLS 内容，不再创建 2% 失败 MP4 任务（A144）。

## 1.2.1：YouTube 结构化质量目录

- [x] **53 YouTube 主世界格式适配**：读取初始/player API 的 `adaptiveFormats`，按 videoId/itag 形成候选组，提供标题、封面、时长、分辨率、FPS、编码、码率和大小（A152）。
- [x] **54 通用探测分片与流身份修正**：保留相同 `/videoplayback` 路径的不同 itag；显式 Range 和无身份小媒体探测默认隐藏，不能创建下载任务（A153）。
- [-] **55 1.2.1 发行与实机复验**：95 项全量门禁、冻结运行时、隔离安装、最终构建与当前电脑更新均通过；Chrome 安全边界要求用户手动重载扩展，完成后只剩现场 YouTube 质量目录确认（A154）。

详细任务和复现证据见 `.scratch/youtube-structured-capture/`。

## 1.2.2：YouTube SABR 全画质下载

- [x] **56 SABR 根因与目录回归**：现场解析到 56 条无直链自适应格式和唯一 360p progressive 直链；增加覆盖 1440p–144p 的确定性回归，禁止再静默退化为 360p（A155）。
- [x] **57 插件画质目录模型**：无直链格式按一个 `resolver=youtube` 内容组显示实际唯一高度，默认推荐最高档；不同编码不再制造重复版本（A156）。
- [x] **58 桌面准确画质解析**：固定并校验 yt-dlp/Deno，按所选高度解析视频轨与最佳音轨，继续由 FFmpeg 下载、合并和校验；敏感上下文不入数据库/命令行/日志，cookie 临时文件必删（A157）。
- [-] **59 1.2.2 发行与实机复验**：97 项自动测试、冻结运行时、隔离安装/更新/回滚/卸载、当前电脑覆盖安装与在线健康检查均通过；只待 Chrome 手动重载后的现场全画质目录和 1440p 任务闭环（A158）。

## 1.2.3：通用页面解析与 Instagram 分片修复

- [x] **60 字节区间根因与双层门禁**：用现场 `bytestart=886&byteend=173864` 和 172,979 字节证明分片误判；扩展/桌面通用识别，重建时只移除偏移并保留签名（A159）。
- [x] **61 内容归组与信息密度**：精确保留 Instagram 资产 ID，按音视频轨归组并估算完整大小；稳定页面候选存在时，重建地址默认进入技术后备，不再展示大量小文件假视频（A160）。
- [x] **62 通用 blob 页面候选**：从视频最近内容容器选择同源永久链接，携带真实帧、标题、时长和尺寸；覆盖 Instagram/TikTok/抖音/X/Reddit/Facebook/Pinterest 链接矩阵，共用 `resolver=page`（A161）。
- [x] **63 桌面页面解析与会话安全**：固定 yt-dlp/Deno 解析最佳完整媒体，统一交给 FFmpeg；公网地址门禁、16 KiB Cookie、内存上下文和临时 cookie 必删均有自动证据（A162）。
- [-] **64 1.2.3 发行与实机复验**：全量自动门禁、发行构建、冻结运行时、隔离安装器和当前电脑覆盖安装完成后，重载 Chrome 在现场 Instagram 做内容级候选、真实下载、FFprobe、预览一致和目录打开闭环（A163）。

## 1.2.4：抖音当前主视频专用适配

- [x] **65 现场双播放器根因回归**：记录主播放器 223.237 秒、屏外预加载 49.041 秒及其视口/来源差异，测试必须复现旧逻辑跳过 HTTP 主视频并误选 blob 预加载项（A164）。
- [x] **66 抖音内容身份与主播放器选择**：按视口交集、播放状态、播放进度和面积只选择当前播放器；`modal_id` 规范为 `/video/<id>`，直接流和页面候选统一为 `douyin:<videoId>`（A165–A166）。
- [x] **67 抖音显示、下载和失败语义**：标题、真实帧、时长、尺寸来自当前播放器；有完整 HTTP 流直接提交，没有时才走页面解析；旧 `jingxuan?modal_id` 由桌面兼容，地址不支持和会话失效使用独立错误（A167）。
- [-] **68 1.2.4 发行与实机复验**：全量测试、双清单、冻结运行时、隔离安装器、当前电脑覆盖安装后，在现场精选页确认唯一主候选为 3:43、预览与画面一致、下载文件时长一致并可打开目录（A168）。

## 1.2.5：抖音逐视频标题与原始请求降级

- [x] **69 逐视频标题根因回归**：现场固定当前 `7662692425235828009/@热话动漫` 与预载 `7653805516250025262/@木板解说` 两个 `feed-item`，证明 DOM 有独立标题而 blob 网络请求回退标签页标题（A169）。
- [x] **70 feed-item 内容候选**：从每个已加载视频自己的容器读取视频 ID、作者和左下角文案，按 `douyin:<videoId>` 分组，当前/预载标签明确且互不串名（A170）。
- [x] **71 无身份网络请求降级**：无法精确绑定 DOM 播放器与视频 ID 的抖音底层音视频请求只进入隐藏技术分片，不可下载、不使用标签页标题伪装内容（A171）。
- [-] **72 1.2.5 发行与实机复验**：105 项 Python、5 组 Node、17 个扩展 JavaScript、双清单、冻结运行时、隔离安装器和当前电脑覆盖安装已通过；现场页重载确认两个视频 ID、文案和时长独立。剩余工具栏 popup 肉眼确认两张卡片及两次最终下载一致性（A172）。

## 1.2.6：通用信息流内容绑定与未关联资源降噪

- [x] [**73 候选爆炸根因回归**](docs/tasks/73-generic-feed-candidate-binding.md)：现场量化页面 4 个真实视频、扩展 18 个默认媒体组与 42 个分片，确定 blob 播放器和网络清单缺少内容绑定（A173）。
- [x] **74 通用内容容器标题与身份**：从最近内容容器的同源永久链接、语义说明和可读正文建立候选；过滤计数/时间轴/控件文字，不包含 X 或其他站点白名单（A174）。
- [x] **75 页面候选优先与统一计数**：同域已有内容绑定候选时，把孤立 HLS/DASH/音视频降为不可提交技术项；没有替代入口时保留完整清单，工具栏与 popup 使用相同默认分区（A175）。
- [-] **76 1.2.6 发行与实机复验**：全量 Python/Node/双清单、17 个扩展脚本、冻结运行时、隔离安装器四路径、发行构建和当前电脑覆盖安装均通过；在线健康与安装文件一致。仅剩 Chrome 手动重载后现场 popup 数量、卡片身份与技术区最终点验（A176）。

## 1.2.7：通用信息流发现生命周期

- [x] [**77 漏检生命周期根因回归**](docs/tasks/77-generic-feed-discovery-lifecycle.md)：固定“DOM 有完整内容身份、纯选择算法正确、popup 只有网络清单”的现场证据，定位连续 DOM 变化与扩展重载后的脚本失联（A177）。
- [x] **78 不可饿死的扫描调度**：100 次连续变化只建立一个待执行扫描，扫描完成后允许下一轮；不再使用可无限后移的尾随防抖（A178）。
- [x] **79 popup 主动恢复发现**：打开/刷新 popup 先探测当前 HTTP(S) 标签的内容脚本；无接收端时只注入候选逻辑和内容发现脚本并立即扫描，不注入下载器或站点代码（A179）。
- [-] **80 1.2.7 发行与实机复验**：全量 Python/Node/双清单、17 个扩展脚本、冻结运行时、隔离安装器四路径、发行构建和本机覆盖安装均通过；仅剩 Chrome 保护页手动重载后，对保持打开的当前 X 视频卡片与技术区现场复验（A180）。

## 1.2.8：通用页面分轨格式兼容

- [x] **81 页面格式选择根因回归**：固定“MP4 视频轨 + 扩展名为 MP4 的独立 AAC 音轨、无合并格式”清单，证明 1.2.7 的 M4A 限制会在零字节阶段失败（A181）。
- [x] **82 容器兼容逐级回退**：保留 MP4+M4A 首选，兼容 MP4+MP4，再回退到合并 MP4、MP4 视频加任意音轨及通用最佳格式；不包含 Pinterest 或其他域名分支（A182）。
- [x] **83 1.2.8 发行与实机复验**：现场 Pin 已完成 720×900 H.264 + AAC 解析和 FFmpeg streamcopy；105 项 Python、5 组 Node、17 个扩展脚本、双清单、冻结运行时、隔离安装四路径、发行构建和本机覆盖安装全部通过（A183）。

## 1.2.9：结构化视频页面发现

- [x] **84 Vimeo 零候选根因证据**：现场作品页显示 0 个默认媒体、11 个未关联资源；桌面解析器独立确认同一作品存在 240p–4K 完整格式，故障限定在内容身份发现层（A184）。
- [x] **85 通用结构化视频入口**：读取 Open Graph/Twitter Player 的明确视频类型、规范地址、播放器、标题、封面和尺寸；只接受非首页的同源作品地址，不按 Vimeo 域名判断（A185）。
- [x] **86 技术资源边界保持**：结构化页面候选进入统一 `resolver=page`，播放器底层未关联 HLS/DASH/分片继续隐藏且不可提交，不新增浏览器下载器（A186）。
- [-] **87 1.2.9 发行与实机复验**：105 项 Python、候选 Node 回归、全部扩展 JavaScript、双清单、冻结运行时、隔离安装四路径、发行构建、本机覆盖安装和现场地址 240p–4K 解析均已通过；只剩用户重载 Chrome 扩展后确认现场内容卡片与创建任务（A187）。

## 1.2.10：多视频页面候选恢复

- [x] **88 宽泛分区回归证据**：固定“一个页面解析候选与多个独立完整媒体同处一个标签页和域名”的失败用例，证明旧逻辑会错误隐藏后者（A188）。
- [x] **89 通用邻近内容身份**：blob/MSE 播放器从近到远选择第一个不同于当前页的稳定同源内容链接，主视频没有更近内容链接时保留当前作品页，不包含 Pinterest 域名分支（A189）。
- [x] **90 完整媒体可见性**：页面解析候选不能单凭标签页和来源域名隐藏其他 MP4、HLS、DASH 或音频；只有结构确认的传输分片默认进入技术区（A190）。
- [-] **91 1.2.10 发行与实机复验**：105 项 Python、5 组 Node、全部扩展 JavaScript、双清单、冻结运行时、隔离安装四路径、发行构建和本机覆盖安装均已通过；只剩用户重载 Chrome 扩展后确认多视频列表（A191）。

## 1.2.11：自动跟随系统代理

- [x] **92 网络代理配置**：默认自动读取 Windows 当前用户系统代理；提供自动、直连、手动 HTTP/Mixed 三种模式，本机服务强制绕过（A192–A193）。
- [x] **93 统一下载链路**：FFmpeg/ffprobe、YouTube/通用页面 yt-dlp 和字幕请求使用同一任务代理；自动模式最多切换一次线路，手动模式不静默直连（A194）。
- [x] **94 可见状态与诊断**：主界面提供网络入口、检测结果和操作说明；403/超时显示代理建议，诊断只导出脱敏端点（A195）。
- [x] **95 1.2.11 发行与实机复验**：113 项 Python、17 个扩展 JavaScript、双清单、冻结运行时、隔离安装器四路径、发行构建、签名更新清单、GitHub 推送与公开发布全部通过（A196）。

## 1.3.0：微信视频号本机捕获与统一下载

本阶段独立参考 `ltaoo/wx_channels_download` 的可观察行为，不复制其 Go/JavaScript 源码、Gopeed 下载器、证书或图标。微信桌面客户端的发现作为新的本机来源适配器，候选提交后继续使用现有 `MediaCoordinator`、SQLite 任务、FFmpeg、Eagle 和桌面任务 UI。详细任务位于 `.scratch/wechat-channels-1-3/issues/`。

- [x] [**96 来源生命周期纵向闭环**](.scratch/wechat-channels-1-3/issues/01-source-lifecycle-tracer.md)：默认关闭的桌面入口、服务状态、健康信息、幂等启停和合成候选闭环（A197–A198）。
- [x] [**97 受控证书与 HTTPS 捕获**](.scratch/wechat-channels-1-3/issues/02-trusted-proxy-capture.md)：每机证书、目标域名 TLS、非目标隧道、系统代理精确恢复与资源门禁（A199–A201）。
- [x] [**98 内容身份、预览与候选提交**](.scratch/wechat-channels-1-3/issues/03-feed-candidate-ui.md)：按 objectId 归组标题/作者/封面/时长/质量；微信原操作栏普通点击提交当前质量、悬停选择其他实际质量，桌面保留完整核对和手动入口（A202–A204）。
- [x] [**99 多质量下载与受控解密**](.scratch/wechat-channels-1-3/issues/04-quality-download-decrypt.md)：短期上下文、任意实际档位、流式解密、FFmpeg/ffprobe 和错误语义（A205–A208）。
- [x] [**100 任务、Eagle 与恢复**](.scratch/wechat-channels-1-3/issues/05-task-eagle-recovery.md)：统一任务进度、仅下载、补导、离线等待、重启保留和来源可靠性（A209–A211）。
- [x] [**101 安全、诊断与故障恢复**](.scratch/wechat-channels-1-3/issues/06-security-diagnostics-recovery.md)：秘密零落盘、代理归属恢复、注入安全、资源上限与可操作错误（A212–A214）。
- [x] [**102 安装、升级、卸载与版本门禁**](.scratch/wechat-channels-1-3/issues/07-installer-rollback.md)：1.3.1 版本同步、载荷归属、健康回滚、证书/代理卸载和全量文档（A215–A217）。
- [-] [**103 全量回归与真实视频号验收**](.scratch/wechat-channels-1-3/issues/08-release-live-verification.md)：首次真实运行已完成证书信任、目标页面注入和代理精确恢复，并据现场 0 候选修复资源模块内部 `finder*` feed 入口；当前 172 项 Python、6 组 Node、17 个活动 JavaScript 语法和双清单通过。浏览器/桌面性能及 schema 6 导入后删除修改已重建发行包并覆盖本机安装，在线健康和安装载荷哈希一致性通过；冻结运行时和隔离安装器四路径专项证据仍对应修改前载荷。仍需完成真实授权内容的按钮、当前/其他质量、ffprobe/Eagle 闭环；此前只能发布为明确标注的预发布候选（A218–A220）。
- [x] **104 主窗口纵向滚动**：在最低窗口高度及缩放场景下，用外层滚动容器保证标签详情、操作区和全局底栏可达；Treeview 与下拉框保留自身滚轮（A221）。
- [x] **105 列表刷新与安全清理**：媒体、视频号、IDM 与网站规则列表提供明确刷新和匹配其语义的清理操作；只删允许的记录/候选，不删文件、不动 Eagle、不影响活动任务（A222）。
- [x] **106 浏览器大量候选性能**：工具栏角标按标签页有界合并全量归组，popup 优先读取候选快照；侧栏只延迟加载静态封面并启用离屏渲染跳过，远程视频预览只保留当前选中项。400 条基准由约 4.45 秒累计归组降为一次约 30 毫秒（A223）。
- [x] **107 桌面刷新性能**：Eagle 探测改为后台单飞，四类 Treeview 使用增量投影，相同预览复用解码；仪表盘合并 SQLite 读取，系统代理与视频号静态健康使用可失效短期缓存，候选计数为 O(1)。500 行 Tk 无变化刷新基准约提升 2 倍（A224）。
- [x] **108 Eagle 成功后清理本机副本**：schema 6 新增 `delete_after_import`；新版导入动作明确提示删除语义，旧扩展 worker 缺少新字段时也按其明确的 `importToEagle=1` 保持相同语义。只有 Eagle 成功、plan/job 路径一致且文件位于程序“已完成”目录时删除并清空路径；schema 5 旧计划、IDM/用户文件、未勾选、仅下载、失败、等待、重复和目录外文件全部保留，导入成功与删除之间异常退出可在下次循环恢复（A225）。

## 1.4.0：统一前端重构

- [x] **109 功能保全与迁移映射**：建立 EXT-001–034、DESK-001–025、CORE-001–017 的旧入口、新位置、后端 Seam 和测试证据映射；退役功能仍保持不可达（A226）。
- [x] **110 桌面信息架构**：主窗口统一为下载任务、视频号、IDM 导入、设置和诊断；下载/视频号采用主从详情，900×600 外层滚动可达全部动作（A227–A228）。
- [x] **111 扩展媒体与任务 UI**：660×574 popup 使用唯一媒体/任务运行时，显示当前页上下文、候选检查器、技术信息、安全文件名和按状态可用的任务动作（A229）。
- [x] **112 托盘与状态真实性**：Windows Forms 托盘只保留显示窗口、网站规则、唤醒处理、检查更新和退出；未完成计划不显示 100%，失败重试只在允许状态出现（A230）。
- [x] **113 确定性视觉与回归证据**：Tk/浏览器视觉夹具、参考图同画布对照、173 项 Python、6 组 Node、活动 JS 语法和双清单解析通过（A231）。
- [x] **114 1.4.0 预发布**：源码、版本、文档和发行包已随 GitHub `v1.4.0-rc.1` prerelease 发布；A219–A220 的真实微信最终媒体闭环仍独立保持待人工，未标成稳定版（A232）。
- [x] **115 1.4.1 深色桌面界面正式接入**：正式启动器、真实数据库/API/处理服务与新版 `MainWindow` 共用同一对象；UI PNG 进入 Python 包和 PyInstaller 载荷。视频号隐藏页分栏归零已修复；真实任务投影、185 项 Python、6 组 Node、17 个活动 JS、清单 JSON、冻结发行包资源/入口和后台截图均通过（A233）。
- [x] **116 桌面按钮与真实功能复核**：逐页核对 DESK-001–DESK-025、托盘入口与真实回调；下载任务五个状态动作由“不可用即隐藏”改为常驻显示和按状态置灰，1120×760 与 900×600 后台截图通过（A234）。
- [x] **117 全仓无用代码与资源清理**：删除已有正式入口替代的旧设置弹窗、无引用接口/包装、视频号旧命令轮询与动态执行、前端死函数/选择器、60 个重复图标和 39 个未使用状态图标；增加禁止回退测试（A235）。

## 1.2.0：职责边界、分片信息密度与本地补导

- [x] [49 播放分片与最新项交互](.scratch/extension-boundary-1-2/issues/01-fragment-and-order.md)：默认排除不可下载分片、提供紧凑诊断开关，候选按时间正序并在跟随最新状态下滚动到底部（A145–A146）。
- [x] [50 已下载文件补导 Eagle](.scratch/extension-boundary-1-2/issues/02-import-existing.md)：扩展与桌面按 `planId` 把 `completed_local` 文件幂等加入 Eagle 队列，不重新下载且服务端验证路径归属（A147）。
- [x] [51 扩展/桌面职责和运行时清理](.scratch/extension-boundary-1-2/issues/03-runtime-boundary.md)：删除旧下载/解析/录制/在线工具、上游图标与未用权限；安装器清理遗留载荷，GPL 来源归档继续保留（A148–A150）。
- [-] [52 1.2.0 发行与实机复验](.scratch/extension-boundary-1-2/issues/04-release-verification.md)：94 项回归、JS 语法和双清单已通过；冻结运行时、隔离安装器、覆盖安装与 Chrome 三项人工闭环待完成（A151）。

## 核心与流程

- [x] IDM 完成事件快速写入持久队列并立即唤醒助手。
- [x] 无来源视频直接导入；旧“等待来源”任务自动迁移。
- [x] 有可靠来源时保存网页地址，不猜测来源。
- [x] 已导入项目可以事后补写来源，不重复导入。
- [x] Eagle 离线等待、自动重试和永久失败上限。
- [x] 保留 IDM 与用户原文件，不移动、不删除、不修改；显式导入后清理只作用于本程序创建并通过归属复验的最终副本。
- [x] 非视频过滤、文件稳定性检查和完整内容去重。
- [x] 合并短时间内同一路径的重复 IDM 通知。

## Chrome 与网站规则

- [x] Manifest V3 扩展、网站开关、子域名规则和默认关闭。
- [x] 自动捕获下载点击与视频请求，支持手动记录网页。
- [x] 网站关闭或用户单次忽略时发送明确跳过事件。
- [x] 配对码、扩展来源、随机令牌和离线事件队列。
- [x] 配对状态持久化、POST 认证兼容和安装器一次性自动配对。
- [x] 同一网页的重复视频请求先合并，避免可靠来源因自身重复而被判为不确定。
- [x] Eagle 健康检测优先使用 V2 接口，并兼容尚未提供 V2 Web API 的 Eagle 4 构建，避免运行中误报“正在等待 Eagle”。

## 界面、安装与资源

- [x] 托盘图标、记录窗口、重试、打开路径/来源、补写来源、诊断导出和清理。
- [x] 移除会触发 `0xc0000409` 的 Python 托盘回调，改用独立 Windows Forms 托盘宿主。
- [x] 托盘宿主到助手的打开、检查、退出、重启和单实例控制链通过验证。
- [-] 用户标出的系统任务栏图标物理右键，待用户确认菜单显示且不闪退。
- [x] 来源可选提示和无来源导入结果说明。
- [x] 窗口隐藏时降低刷新频率，任务列表无变化时不重复重建。
- [x] 桌面“媒体下载”页显示标题、来源、阶段、百分比、处理大小、错误、输出位置和视频预览；“IDM 导入记录”独立保留。
- [x] 下载跨进程唤醒，空闲时降低数据库轮询频率。
- [x] 不写死 Python 3.14，自动寻找常见 Python 3.11+ 安装位置。
- [x] 项目根目录提供 `下载中转站.exe`，IDM 使用轻量 `launcher\IdmEagleHook.exe`。
- [x] 主程序、冻结后台、托盘、窗口、安装器和 Chrome 扩展统一使用自有名称与图标。
- [x] 正常运行不再出现本助手的 `pythonw.exe`，任务管理器显示“下载中转站”。
- [x] 保存 IDM 安装前原值：程序路径与参数均为空。
- [x] 网站规则列表、右下角快捷入口和失败/处理中状态汇总。
- [x] IDM 触发助手静默自启动；浏览器来源快速补发和 4 秒来源宽限。
- [x] PyInstaller 独立运行后端，发行包不依赖接收者安装 Python。
- [x] 一键安装器自动复制、配置 IDM、创建快捷方式、注册卸载并打开 Chrome 安装引导。
- [x] 已有杀毒软件配置保护、原设置备份和卸载恢复。
- [x] 每台电脑独有的一次性 Chrome 自动配对凭据，使用后销毁。
- [x] 主界面和托盘菜单加入检查软件更新入口，每天最多自动检查一次。
- [x] RSA 签名更新清单、GitHub 地址限制、ZIP 大小与 SHA-256 完整性校验。
- [x] 更新前旧版备份、新版健康检查、故障自动回退及 Chrome 配对保留。
- [x] Chrome 扩展低频版本确认，仅在后台版本更新时自行重载。
- [ ] 可选的 Windows 开机启动；执行前需用户明确决定。

## 验证与交付

- [x] 94 项自动测试和端到端模拟通过，包括 Eagle 4 新旧健康接口、扩展连接健康端点、配对竞态、候选身份/分片/排序、动态质量、任务完成态、补导 Eagle、单 popup 运行时、旧工具箱删除、GPL 归档、全媒体 `desktop` 路由、分轨合并、schema 5 和 AES-128 HLS。
- [x] 真实 IDM 文件路径传递和真实 Eagle 导入成功。
- [x] 真实 Behance 来源网页成功写入 Eagle。
- [x] 旧等待记录已在升级助手后无来源导入，Eagle 素材与空来源字段均已核对。
- [ ] Eagle 关闭后下载，再启动 Eagle 自动补导入。
- [ ] 同内容重复下载、两条并发下载、网站关闭和非视频的真实测试。
- [x] 自动重试明确提示、2–3 秒文件稳定重试、完成任务重试保护和旧误报记录迁移。
- [x] 自动重试硬上限：文件稳定 20 次、临时错误 12 次、Eagle 等待约 1 小时。
- [x] README、验收、架构、决策、安装、回滚、许可和开发文档与本机统一下载行为同步。
- [x] 隔离目录完成一键安装、独立启动、IDM 接收和自动配对真实验收。
- [x] 隔离完成 0.5.0 → 0.6.0 更新与强制失败回退，配对均保留。
- [x] 0.6.0 正式安装和真实健康检查通过，便携版历史、规则与配对已迁移。
- [x] MIT 源码完成隐私审查并推送到 GitHub。
- [x] Apple 公共 HLS 和 B 站公共非 DRM DASH 实际下载 5 秒样本，ffprobe 均确认包含音频与视频。
- [x] 1.0.0 隔离完成全新安装、0.6.0 升级、注入失败回滚、卸载和冻结运行时 IDM 接收。
- [x] 1.1.0 schema 5 冻结运行时、隔离安装/覆盖/故障回滚/卸载、当前电脑覆盖安装、`desktop_ffmpeg` 健康门、安装文件哈希和桌面双标签 UI 实机验证。
- [-] Chrome 实际工具栏仍需肉眼确认分片默认隐藏、首次定位最新项和 `completed_local` 补导；缓存捕获与录制已在 1.2.0 退役，不再等待验收。
