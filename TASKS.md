# 当前任务

本文件只保存当前版本仍然有效的完成项和待办。历史任务由 Git 历史保存。

## 已完成

- [x] 将品牌统一为“留底”、产品全称统一为“留底下载器”，同步留底桌面端、留底浏览器扩展、留底安装器、标题栏、发布文件、代码和当前文档。
- [x] 修复视频号原始视频错误携带转码上下文、只能得到低清版本的问题。
- [x] 用户已在真实微信会话确认原始画质修复有效。
- [x] 视频号捕获继承 Windows HTTP/Mixed 代理和 PAC/WPAD 上游；解析失败时在改写系统代理前停止。
- [x] 修复部分 VPN 使用独立 `ProxyOverride=*` 时，捕获代理错误断开全部 VPN 上游并导致视频号黑屏的问题。
- [x] 为证书工具和安装器等待加入超时与子进程终止，避免升级永久挂起。
- [x] 修复扩展长列表点击底部候选时自动滚回顶部的问题。
- [x] 修复抖音页面解析把超长页面 Cookie 传给 FFmpeg、导致下载失败的问题。
- [x] 修复抖音 SPA 切换新视频时误清除之前已捕获候选的问题。
- [x] 修复新网页候选先出现又消失的导航清理时序竞争；高频清理提前到 `onBeforeNavigate`，冷启动导航在首个新候选入库前同步冲洗。
- [x] 移除桌面六位配对码界面，为扩展增加首次自动连接、同源令牌恢复和手动“自动连接本机软件”入口。
- [x] 发行包改为单文件内嵌安装载荷，不在用户目录暴露 Python、C#、PowerShell 和构建配置。
- [x] 完成 Python、Node、活动 JavaScript、双清单、冻结运行时和隔离安装器验证。
- [x] 完成当前电脑覆盖安装，保留数据库、任务、设置、网站规则和浏览器连接认证状态。
- [x] 将当前源码、媒体工具和 Windows 发布包全量上传到 `Ly233ly/download_video`。
- [x] 精简 README 和项目历史文档。
- [x] 在桌面“文件管理”中增加缓存占用统计、手动清理和按天自动清理，并保护活动任务、“已完成”目录、IDM 原文件和用户文件。
- [x] 移除桌面下载任务列表的页码和翻页按钮，改为同一列表连续滚动查看全部已读取任务。
- [x] 将桌面所有共用自绘控件从 Tk 非抗锯齿多边形改为超采样圆角图块与直边组合，消除斜切角、分段圆弧和像素锯齿，并完成视觉与压力场景验证。
- [x] 修复首次启动状态点背景使用模块加载期旧主题色的问题，将三个连接状态点改为抗锯齿正圆，并让徽标和全部缓存圆角图块在一次主题切换后同步重绘。
- [x] 完成无 Eagle 使用体验适配：统一本机 API 能力探测与安全降级，覆盖扩展单个/批量下载、任务补导、桌面下载任务、桌面/页内视频号下载、IDM 重试与来源更新，并明确文件保留边界。
- [x] 使用三个子 agent 全面审计后端、浏览器扩展与桌面/视频号，修复并发补导、删除归属、后台线程恢复、音频与重复内容状态、扩展断线/异步竞态、视频号降级和 IDM 文件丢失等 26 类逻辑问题。
- [x] 使用三个子 agent 完成第二轮全功能、前端 UI、刷新顺序和性能压力审计，修复扩展跨页面旧响应、超时预算、桌面设置覆盖与键盘焦点、后端探测惊群、查询索引和停止竞态，并完成大数据量化回归。
- [x] 修复全部自绘圆角控件的首次绘制、销毁回调和禁用焦点，并防止圆角列表卡片的矩形内容层遮住或裁切外层圆弧。
- [x] 修正 Tk Canvas 排他右/下边界引起的共用圆角直边少绘一像素问题，消除按钮底边和右边白色断口；补齐深色主题浏览器连接状态行样式。
- [x] 锁定主题持久化语义：首次无记录时使用浅色，用户切换后保存最后选择，后续启动与覆盖升级不重置主题。
- [x] 使用三个子 agent 完成 1.6.0 独立审计，修复扩展滚动/并发/重复提交/MV3 持久化、后端缓存锁与关闭死锁，以及旧请求缺少明确删除授权时误删本机副本的问题。
- [x] 构建 1.6.0 单文件安装器，通过 347 项 Python、6 组 Node、冻结运行时、隔离新装、覆盖、回滚与卸载验证，并在当前电脑完成保留用户数据的覆盖安装。
- [x] 清理应用缓存和项目构建/测试缓存，保留发行产物、CodeGraph 索引、数据库和用户文件。
- [x] 将 1.6.0 源码和完整发行文件全量提交到 `Ly233ly/download_video`，并发布带 Windows ZIP、源码 ZIP 和校验文件的 GitHub Release `v1.6.0`。

## 待人工复验

- [ ] 在用户实际 VPN 开启状态下启动视频号捕获，确认页面可访问、候选可创建、任务可下载，并在停止捕获后恢复原线路。
- [ ] 在 Chrome 实际工具栏 popup 中打开大量候选，滚动到底部并点击最后一项，确认选中正确且滚动位置不回跳。
- [ ] 在一台全新安装电脑验证扩展无需配对码自动连接；停止桌面软件后确认插件提示未找到软件，重新启动后点击自动连接可恢复。
- [ ] 在未安装 Eagle 的全新电脑验证浏览器单个/批量下载、视频号页内下载、桌面视频号仅下载、任务打开目录和稍后安装 Eagle 后补导。

## 发布阻塞

- [ ] 找回项目现有 RSA 更新签名私钥。
- [ ] 使用原私钥生成并验证 `update.json`，然后完成稳定更新发布。

不得临时生成新密钥替代原密钥，否则已安装客户端会拒绝更新。
