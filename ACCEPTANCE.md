# 验收标准

## 1.0.0 验收结果

状态：`通过` 表示已有自动、固定样本、隔离安装或此前真实 Eagle 结果；`待人工` 表示浏览器安全边界使当前自动化无法替用户点验。

| 范围 | 状态 | 主要证据 |
| --- | --- | --- |
| A01–A45 原 IDM/Eagle 能力 | 通过 | 88 项回归中的数据库、处理器、Eagle、hook、端到端、更新与安全测试；此前真实 IDM/Eagle/Behance 记录 |
| A46–A60 可见候选、计划与合并 | 通过（视觉待 A93 复验） | `test_extension.py`、`test_media.py`、固定 FFmpeg 8.1.2、真实 streamcopy/ffprobe |
| A61–A67 HLS、DASH、B站 | 通过（工具栏点击待 A93 复验） | 上游运行时对照、AES-128 集成测试、Apple HLS 与 B站公共 DASH 实际拉流证据 |
| A68–A79 深搜、缓存、录制、路由、工具、兼容 | 代码与自动门通过 | 96 文件上游对照、关键路由检查、Chrome/Firefox 清单与三语新增界面；真实缓存/录制归 A93 |
| A80–A90 安全、安装、许可 | 通过 | 内存请求上下文、注入/DRM/owned-temp/Origin 测试，隔离安装回滚脚本，GPL 对应源码与二进制清单 |
| A91 | 通过 | 当前架构、验收项和自动测试已覆盖保留功能 |
| A92 | 通过 | 68/68、全部 JS/manifest、C# 构建、隔离安装/升级/回滚/卸载、冻结运行时 |
| A93 | 待人工补齐 | 直链/HLS/DASH/B站与停止已有证据；Chrome 工具栏缓存捕获和一种授权录制需用户肉眼点验 |
| A94 | 通过 | README、STATUS、TASKS、本文、安全、开发、架构、决策、安装回滚、许可与版本已同步 |
| A95 | 待项目所有者签字 | 不自动代替维护者上传或发布 GitHub Release |

1.0.0 的详细结果与可重跑命令已从工作区精简，可通过 Git 历史恢复；最终验收事实继续保留在本文件。

## A. 核心自动化

- **A01**：IDM 完成受支持的视频下载后，系统自动创建导入任务。
- **A02**：Eagle 正在运行时，视频自动出现在当前资源库的“全部”中。
- **A03**：没有来源网页时仍然导入，Eagle 来源字段保持为空。
- **A04**：捕获到可靠来源时，Eagle 来源字段等于视频所在网页地址，不使用视频直链或猜测网址。
- **A05**：Eagle 未运行时任务保持“等待 Eagle”，启动后无需重新下载即可补导入。
- **A06**：导入完成后，下载目录中的原视频仍存在且内容未改变。

## B. 网站规则与来源

- **A07**：用户可在 Chrome 中开启或关闭当前网站自动导入。
- **A08**：开启网站后，扩展识别到的下载动作会保存网页来源。
- **A09**：关闭网站后，扩展识别到的下载动作会生成跳过指令；无法被浏览器识别的 IDM 动作仍按“无来源直接导入”处理。
- **A10**：可配置多个网站并正确处理常见子域名。
- **A11**：来源不确定时不得猜测；导入记录明确显示“未记录来源”。
- **A12**：已导入且有 Eagle 项目编号的记录可以事后补写来源，不重复导入文件。

## C. 文件与重复保护

- **A13**：非视频文件不会导入，并留下“非视频忽略”原因。
- **A14**：至少支持 MP4、MOV、MKV、WebM、AVI、M4V、MPEG、MPG、TS、M2TS 和 WMV。
- **A15**：相同内容的视频再次下载时跳过；不同内容即使同名也不能误判。
- **A16**：中文名称、空格、括号和较长 Windows 路径可以正确处理。
- **A17**：文件不存在、为空或仍在增长时不得提前导入。
- **A18**：同一路径在短时间内被 IDM 重复通知时只保留一个活动任务。

## D. 记录、恢复与占用

- **A19**：右下角存在托盘图标；左键可打开记录，右键显示 Windows 原生菜单，记录窗口可区分成功、等待、失败和跳过。
- **A20**：失败任务可以手动重试，错误原因对普通用户可理解。
- **A21**：程序或电脑意外关闭后，未完成队列不会丢失。
- **A22**：新下载通过轻量信号立即唤醒处理；空闲时不高频轮询，窗口隐藏后降低界面刷新频率。
- **A23**：默认记录保留 90 天、最多 10,000 条，并可手动清理。
- **A24**：助手和 IDM 接收器不会弹出终端窗口。

## E. 安全与回滚

- **A25**：默认流程的数据只保存在本机；媒体只从原网站下载到本机，除 Eagle 本机 API 和 GitHub Release 更新端点外不后台调用第三方处理服务；更新检查不得上传下载记录、来源或规则。
- **A26**：不保存 Cookie、账号密码、浏览器请求头或网页正文。
- **A27**：本机接口只监听 `127.0.0.1`，Chrome 扩展使用配对码、扩展来源和随机令牌认证。
- **A28**：安装前记录 IDM 原设置；停用后可以恢复。
- **A29**：卸载不会删除下载视频或 Eagle 项目。
- **A30**：网站规则、完整来源和本地路径不会写入对外诊断文件。

## F. 分享与一键安装

- **A31**：发行 ZIP 自带运行环境，未安装 Python 的 Windows 10/11 电脑也可运行。
- **A32**：一键安装器把完整程序复制到用户本地应用目录，创建桌面与开始菜单入口并注册卸载项。
- **A33**：IDM 对应设置为空或旧助手路径时自动配置为接收器与 `[File]`；发现其他安全软件配置时不覆盖。
- **A34**：安装前 IDM 原值只备份一次；卸载时仅在当前值仍指向本助手时恢复，避免覆盖用户后续修改。
- **A35**：安装器生成的自动配对凭据每台电脑不同、七天过期、仅允许 Chrome 扩展 Origin，并在成功配对后删除。
- **A36**：安装器自动打开 Chrome 扩展页面和正确文件夹；由于浏览器安全限制，最终“加载已解压”由用户亲自确认。
- **A37**：文件尚未稳定时界面明确说明会自动重试，单次等待不超过约 3 秒且最多 20 次；用户无需手动操作，也不会无限循环。
- **A38**：已经成功导入的同一任务不得因手动重试竞态变为“重复跳过”；历史误报记录在升级后自动恢复为“导入成功”。
- **A39**：普通临时错误最多自动重试 12 次；Eagle 长期未启动最多低频等待约 1 小时，达到上限后停止并允许用户手动重新开始。
- **A40**：同一网页产生多条重复视频请求时只作为一个来源候选；不得因候选自身重复而漏写可靠来源。
- **A41**：正常运行时任务管理器不得把本助手显示为 Python；主程序、后台、托盘和扩展应显示“下载中转站”及自有图标。
- **A42**：主界面和托盘菜单都提供检查更新入口；自动检查每天最多一次，未经用户确认不得安装。
- **A43**：更新清单必须通过内置 RSA 公钥验签；下载包的 SHA-256、实际大小或 GitHub 仓库地址任一不符时停止安装。
- **A44**：更新前保留旧程序目录，新版本必须通过本机版本健康检查；失败时恢复并重新启动旧版本。
- **A45**：更新不得删除或重置下载记录、网站规则、Chrome 配对和 Eagle 内容；0.6.0 后扩展可在后台版本变更后自行重载。

## G. 可见媒体候选与下载计划（1.0.0）

- **A46**：扩展默认展示当前标签页捕获到的媒体候选组，不需要先进入隐藏页面或打开调试开关。
- **A47**：每个候选组在下载前至少显示可识别标题、来源页面、封面或明确的预览失败原因、媒体类型、清晰度、音轨和已知大小。
- **A48**：用户选择普通直链媒体后，本机软件负责下载并持续记录进度、成功或失败原因；成功文件按选择进入现有 Eagle 队列，并保留可靠来源。
- **A49**：0.6.0 的 IDM 完成事件、无来源导入、网站开关、旧任务状态和扩展事件结构继续工作，旧客户端不会因新字段崩溃。
- **A50**：捕获同时支持 URL 扩展名、响应 MIME、Content-Disposition 和已知媒体请求特征；重复请求只形成一个可解释候选。
- **A51**：用户可以按媒体类型、扩展名和大小过滤；默认规则能排除常见图片、字体、脚本和小型噪声请求。
- **A52**：自定义正则无论有效、无效或灾难性输入都不能卡死扩展；错误规则会被禁用并显示原因。
- **A53**：站点规则、全局规则、临时忽略和手动强制捕获具有确定优先级，并能在界面解释最终结果。
- **A54**：候选预览支持站点封面、媒体首帧或安全的图标占位；不会仅以不可读长 URL 作为名称。
- **A55**：跨域、格式、编码、网络或 DRM 导致预览不可用时，界面显示具体类别，不把“无法预览”误报为“无法下载”。
- **A56**：候选可以按捕获时间、大小、类型、清晰度和名称筛选或排序，选择状态在列表刷新时保持稳定。
- **A57**：下载前能修改合法输出名，并看到将下载哪些媒体流、是否合并、是否导入 Eagle和最终容器；非法 Windows 文件名会被安全规范化。
- **A58**：原始 URL、请求类型和调试信息默认折叠但可展开复制；键盘操作、焦点和状态提示可用。

## H. 合并、HLS、DASH 与深度捕获（1.0.0）

- **A59**：本机 FFmpeg 可在不重新编码的场景使用 streamcopy 合并独立音视频，输出音画同步并通过 ffprobe 完整性检查。
- **A60**：可见主界面不加载浏览器 FFmpeg/WASM 或第三方在线处理页；普通直链、分轨和清单统一使用随软件安装的本机 FFmpeg。
- **A61**：HLS 主清单能展示全部变体、音轨、字幕和分辨率，并生成用户可检查的下载计划。
- **A62**：HLS 媒体清单正确处理普通分片、`EXT-X-BYTERANGE`、初始化段、相对地址、有限并发、重试和中止。
- **A63**：直播/持续增长清单能明确显示录制状态、已捕获时长和停止操作；停止后可以产出可播放文件。
- **A64**：AES-128 HLS 在获得页面本身可访问的密钥时能本地解密；SAMPLE-AES、Widevine 等 DRM 明确阻断且不尝试绕过。
- **A65**：HLS 合并产物经探测可播放后才进入现有 Eagle 导入流；失败时保留可诊断状态且不导入损坏文件。
- **A66**：DASH MPD 能解析 Period、AdaptationSet、Representation、BaseURL、SegmentTemplate/Timeline 和初始化段，并展示视频、音频及字幕选择。
- **A67**：在真实 B 站非 DRM 视频上，独立视频流和音频流属于同一候选组；用户一次选择即可由本机软件下载两路媒体、streamcopy、验证并把一个最终文件导入 Eagle。
- **A68**：深度搜索可以从 XHR、fetch、Worker、页面 JSON 和文本中发现常规请求监听遗漏的 HLS/DASH/媒体地址，并标出发现来源。
- **A69**：页面注入有明确启停、按标签页隔离且可撤销；禁用后恢复原 API 行为，不泄漏跨标签页数据，也不造成持续性能退化。

## I. 缓存、录制、路由与工具（1.0.0）

- **A70**：MSE/Cache 捕获能识别页面实际播放的缓存媒体，显示缓存范围和缺口，并在数据足够时导出可验证产物。
- **A71**：`MediaRecorder` 媒体录制和屏幕录制需要明确用户操作，显示录制来源、时长、大小和停止状态，并产出可播放文件。
- **A72**：WebRTC 流录制只处理用户选定且浏览器允许的流；权限拒绝、流结束和轨道变化均有明确结果。
- **A73**：所有可见主下载动作的最终路由固定为本机软件；失败进入同一任务错误/重试状态，不得静默回退 Chrome、第三方页面或造成重复下载。
- **A74**：IDM 路由由本机桌面助手执行，参数经过固定模板和路径校验；浏览器扩展不直接依赖或冒充 IDM 官方接口。
- **A75**：新版可见主界面不提供 Aria2、m3u8dl、自定义调用、send2local、MQTT 或完整浏览器下载器入口；上游兼容代码不得截获本机任务消息，凭据不会出现在日志或诊断导出中。
- **A76**：媒体播放控制、倍速、画中画、全屏和二维码功能只作用于用户当前选择的标签页或候选，并可恢复页面原状态。
- **A77**：M3U8/MPD/JSON/文本查看器、请求头与 User-Agent 覆盖、自定义命令都提供作用域、重置入口和错误提示。
- **A78**：Chrome、Edge 和目标 Firefox 版本的清单、权限、后台生命周期和核心下载路径均有自动或人工兼容证据。
- **A79**：所有新增设置都有默认值、版本迁移、导入导出和中英文文案；从 0.6.0 升级不会丢网站规则、配对或任务。

## J. 隐私、安全与 DRM（1.0.0）

- **A80**：Cookie、Authorization、Referer 等敏感下载上下文只在任务内存或受限临时记录中按需使用，默认不写入长期数据库和日志。
- **A81**：诊断导出会去除令牌、Cookie、完整本地路径和带查询参数的网址；用户可以预览将导出的内容。
- **A82**：外部目标、命令调用和第三方在线合并不出现在新版可见下载流程，也不会后台自行运行；本机 FFmpeg 参数按固定数组构造，不能形成命令注入。
- **A83**：扩展消息、回环媒体计划、停止/重试和本机命令均验证来源、配对令牌和允许状态转换；已删除临时组件完成/失败回传接口。
- **A84**：检测到 DRM 时清楚提示不支持；程序只删除明确位于任务专属临时目录的本程序中间文件，绝不删除用户原下载文件。

## K. 安装、升级、许可与回滚（1.0.0）

- **A85**：1.0.0 安装包包含目标浏览器扩展、受支持平台的 FFmpeg/ffprobe 及其许可证；安装后可离线完成普通音视频合并。
- **A86**：真实 0.6.0 数据目录升级后，队列、指纹、网站规则、配对、更新状态和旧 IDM 工作流保持可用。
- **A87**：健康检查覆盖后端、数据库迁移、扩展协议和 FFmpeg；任一关键项失败时恢复旧程序并保留数据。
- **A88**：卸载只删除安装器拥有的程序和临时目录，恢复属于本助手的 IDM 设置，并保留用户下载、Eagle 项目和可选数据备份。
- **A89**：若发行物复用或修改 cat-catch GPL-3.0 代码，则提供 GPL-3.0 许可证、版权说明、对应源码、构建/安装信息和获得源码方式；对应源码可以与用户二进制 ZIP 分开，但必须在同一下载位置同步提供。
- **A90**：FFmpeg、浏览器兼容代码及所有新增依赖都有版本、来源、许可证和发行包归属清单，自动检查不存在未登记二进制。

## L. 一次性 1.0.0 发布门禁

- **A91**：当前保留功能必须能在架构、验收项或自动测试中找到实现与验证证据，且没有未处理、无法解释的缺失。
- **A92**：0.6.0 全量回归、新增单元/集成/端到端测试、扩展语法与清单校验、C# 构建和安装包烟雾测试全部通过。
- **A93**：至少保留普通直链、HLS、DASH/B 站、直播停止、缓存捕获和一种录制模式的真实站点脱敏证据。
- **A94**：README、STATUS、TASKS、ACCEPTANCE、SECURITY、DEVELOPMENT、架构、决策、安装回滚、许可和版本号与最终行为一致。
- **A95**：维护者在完整功能、安全、许可、真实站点证据和回滚报告上一次性签字后才标记 1.0.0 并生成正式发行包。

每项验收至少保留自动测试、截图、任务记录或 Eagle 实际结果之一；没有验证的项目不得标记为完成。

## M. 1.1.x 方案 2 弹窗重构门禁

- **A96**：`popup.html` 只有一个应用根、一套运行时和“媒体 / 任务”导航；不得加载隐藏上游 popup DOM、旧 popup 事件层或第二套状态源。
- **A97**：同一明确内容 ID 的视频与音轨只在组内配对；不同 `groupKey`、清单身份或低置信度普通资源不得自动混组。
- **A98**：标题、封面、清晰度、音轨、文件名和预计动作在提交前可见；DRM、清单/直链混选与无效选择在提交前阻断。
- **A99**：多个方案从本机 `plans` 恢复并分别显示本机下载进度、阶段详情、错误和停止；一个调度器批量轮询，重开 popup 不丢任务。
- **A100**：深度搜索、缓存、三类录制、移动 UA、疑似密钥、当前页媒体控制、设置与独立窗口均有唯一可达入口；旧预览/二维码/手工解析器、自动下载快捷键/右键项、完整浏览器下载器和外部下载目标退出新版产品流程。
- **A101**：Chrome 实际工具栏 popup 与选定方案 2 在同一 660×574 视口对照，P0/P1/P2 视觉和交互问题全部修复。
- **A102**：84 项自动测试、全部扩展 JavaScript 语法、双清单、冻结运行时、安装升级/回滚、覆盖安装健康检查、文档与发行哈希通过。
- **A103**：B 站等校验 Referer/User-Agent 的分轨 CDN 由统一的认证本机 `desktop` 任务拉取，不再交给 Chrome 下载器；视频与音频使用仅存内存的白名单请求头，FFmpeg streamcopy 后生成一个有效媒体并进入 Eagle。
- **A104**：任意普通网页和嵌入播放器不依赖专用站点适配即可按真实当前帧、跨域播放器矩形裁剪、媒体 poster、邻近内容图和页面元数据逐级显示可识别内容图；站点 favicon 或整页截图不得充当视频封面。
- **A105**：popup 首次打开等待 session 媒体缓存恢复完成，并在缓存变化时自动刷新；工具栏数字与展开列表使用同一候选快照和内容归组算法。
- **A106**：普通直链、分离音视频、HLS/DASH、“下载并导入”和“仅下载”均创建 `route=desktop` 的本机任务；popup、预览页、直链页和清单解析页的媒体下载都提交同一计划，专用 bridge 不包含 `chrome.downloads`，后台不保留自动下载或自动外发旁路。
- **A107**：“仅下载”完成后状态为 `completed_local/100`，保留最终文件和预览但不创建 Eagle job；“下载并导入”在媒体校验后才创建 job。
- **A108**：数据库 `user_version=6` 保存本机进度、大小、阶段、预览、导入选择和导入后删除选择；升级时删除旧 `component_files` 交接表，把无法恢复完整 URL 的旧活动任务明确标为需重建，并把全部旧计划的删除选择安全默认为关闭。
- **A109**：桌面软件的“媒体下载”页持久显示标题、来源、阶段、百分比、处理大小、错误、输出位置和本机视频预览；另一个标签保留 IDM 导入记录。
- **A110**：任务按 `queued → downloading → validating → ready_to_import → imported` 或 `completed_local` 推进；Eagle 未连接和导入失败从关联 job 解释，不在下载完成时提前显示 100%。
- **A111**：完整签名 URL 和 Cookie/Authorization 等白名单请求上下文只存在于本机进程内存；同次运行可重试，软件重启后要求回来源页重建，绝不从脱敏 URL 猜测。
- **A112**：`blob:` 地址不能被桌面进程访问时在创建任务前明确阻断并指导选择真实直链/清单，禁止静默退回浏览器下载。
- **A113**：候选卡片优先显示真实视频帧；canvas 跨域失败时只能裁剪当前活动标签页中对应播放器矩形，不得把整页截图、favicon 或通用下载图标描述成视频帧。
- **A114**：瞬时帧只存在于 service worker/popup 内存，格式限 JPEG/PNG/WebP 且有长度门禁；`storage.session`、任务 API、SQLite、诊断和 Eagle 元数据不得包含帧 data URL。
- **A115**：同一播放器、相同媒体路径但签名查询变化的请求只形成一个版本；不同播放器、不同明确内容 ID 或不同 frame 不得因此混组。
- **A116**：同组已有 MPD/M3U8/完整媒体时普通 `.m4s`/`.ts` 分片不显示；只有分片时保留一个不可提交项并说明需要继续发现清单。
- **A117**：popup 不加载 `popup.js`、`media-control.js`、jQuery、MQTT 或 HLS 运行时；深搜、缓存、录制、移动 UA、密钥和媒体控制由新版 UI 直接调用后台/内容脚本。
- **A118**：Chrome/Firefox 清单不再声明旧右键下载权限或非主动作快捷命令，后台不存在其远程媒体下载旁路。

- **A119**：支持结构化播放器目录时，完整 HLS/DASH 清单与明确质量档位进入同一候选组；签名清单地址仍只存在于扩展/本机任务内存。
- **A120**：当前播放分辨率不得冒充每条网络请求的文件质量；UI 只列明确的可下载质量并按高度降序，最高档标记推荐，同组清单存在时普通 MP4/音频分片不出现在版本选择器。
- **A121**：用户选择的清晰度进入本机任务上下文；ffprobe 从清单节目中选择对应高度的视频及关联音频，失败时安全回退，不把多个清晰度同时写入输出。
- **A122**：质量目录不得预设五档或其他固定集合；UI 显示当前视频实际档位数，保留全部明确高度、去重降序并说明不同视频会随源站变化。
- **A123**：除站点结构化目录外，可访问的 HLS/DASH 主清单应在有界大小与超时内提取明确分辨率；读取失败、媒体清单或缺少高度时不猜测档位且不阻断桌面下载。
- **A124**：至少用 8 档播放器目录、8 档 HLS、3 档 DASH 和 8 档本机节目映射证明质量数量为数据驱动，并纳入全量扩展/桌面回归。
- **A125**：popup 重新打开或轮询期间必须从桌面持久方案恢复状态；`completed_local`/`imported` 必须为 100%。同步失败不得继续把旧 0% 描述为实时状态，失效认证应自动恢复或明确要求重新配对。
- **A126**：“仅下载”完成后必须显示最终路径并提供“打开所在文件夹”；本机接口只接受任务 ID，最终文件必须存在且位于程序拥有的 `下载中转站/已完成`，拒绝任意客户端路径和目录逃逸。
- **A127**：任务预览不得按 Behance、Tmall 或其他域名白名单实现。创建时复用通用视频帧/播放器裁剪，重开 popup 后读取本机从最终视频抽取的 PNG；预览接口必须认证、限制在程序预览目录且有格式和大小门禁。
- **A128**：1.1.4 必须通过全量 Python/JavaScript/清单回归、冻结运行时和隔离安装器验证；当前电脑覆盖安装后健康版本、扩展版本与关键修复文件必须和源码一致。
- **A129**：未配对、助手离线、选择无效或 DRM 阻断时，“下载并导入 Eagle”和“仅下载”均不可提交；校验失败不得显示成功提示，只有本机返回真实计划后才切换任务页并显示“下载已开始”。popup 连接检查必须主动重试一次自动配对。
- **A130**：1.1.5 必须通过全量 Python/JavaScript/清单、冻结运行时和隔离安装器；当前电脑覆盖安装后，应在 Chrome 实际加载副本中完成配对，并以真实媒体证明点击“仅下载”后桌面创建同一 `planId`、popup 任务数增加、最终状态为 `completed_local/100` 且可打开目录。
- **A131**：配对前发出的请求在配对后返回 401 时不得清除新令牌；只有请求所用令牌仍等于当前令牌时才允许清除。配对 UI 必须在令牌持久化且受认证 `/api/media/health` 成功后才显示完成。
- **A132**：1.1.6 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；Chrome 重载后手动配对一次，关闭并重开设置仍保持已连接，真实“仅下载”创建持久任务并完成为 `completed_local/100`，且可打开最终目录。
- **A133**：扩展所有持久状态更新必须在同一串行读改写队列中执行；并发更新令牌、来源事件和最近任务字段不得互相覆盖。401 清除必须在队列内部再次确认当前令牌仍等于被拒绝令牌。
- **A134**：1.1.7 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；Chrome 重载后手动配对一次，设置关闭重开仍保持已连接，并完成真实“仅下载”任务、100% 终态与目录打开。
- **A135**：扩展所有受认证读取——健康、任务列表、任务详情和任务预览——必须使用 JSON POST，并在保留浏览器扩展 Origin 校验的同时把随机令牌放入请求体；不得因跨源 GET 缺少 Origin 或 Authorization 头而误判令牌失效。
- **A136**：1.1.8 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；安装后的认证 POST 健康接口必须返回 200。Chrome 重载后手动配对一次，设置关闭重开仍保持已连接，并完成真实“仅下载”任务、100% 终态与目录打开。
- **A137**：网络媒体候选只有在去签名查询后的 URL 主机与路径均匹配某个播放器真实 `currentSrc/src/source` 时，才可继承该播放器的帧、poster、时长、尺寸和稳定内容组；未匹配的后台预加载或隐藏 MSE/blob 请求不得借用当前可见播放器身份。非清单直链下载后必须用 FFprobe 比较声明时长与最终输出时长，明显不一致时以可见错误阻止移动到完成目录及 Eagle 导入。
- **A138**：1.1.9 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；Chrome 重载并刷新抖音来源页后，选中卡片的播放器画面、显示时长、提交 URL 与最终文件的实际时长/抽帧必须属于同一视频，任务达到正确终态且可打开目录。
- **A139**：普通视频候选没有可信页面帧或结构化封面时，popup 必须预览当前所选 `http/https` 下载 URL 自身解码出的画面；切换组内版本必须同时切换预览源。不得借用其他播放器、整页截图或示例站点白名单，音频/blob/非法 URL 不得作为远程视频预览，加载失败明确回退占位。
- **A140**：1.1.10 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；Chrome 重载并刷新来源页后，抖音同页多个普通视频候选应各自显示与其下载 URL 对应的画面，选择任一候选完成下载后，最终文件抽帧/时长与选择一致，任务达到 100% 终态且可打开目录。
- **A141**：同一标签页和来源网站、同媒体类型的多个 URL，只有在响应 MD5/Digest/强 ETag 相同，或长媒体路径相同且总字节数完全一致时，才可作为同内容 CDN 备用地址归并；只保留信息最丰富的下载版本并共享真实标题、预览、时长和质量。仅大小相同、短通用路径、不同明确内容 ID 或多个显式组存在歧义时必须保持分离。
- **A142**：1.1.11 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；Chrome 重载并刷新截图中的抖音来源页后，同一 52 MB 视频的有图主链接与无图备用链接只显示一个内容，内容计数不随备用 CDN 请求增加，选择下载后最终文件抽帧/时长与该唯一预览一致，任务达到 100% 终态且可打开目录。
- **A143**：扩展必须识别 URL 查询、路径明文或 base64url 路径段中声明的固定 `range/bytes=start-end`，并用已知字节数等于区间长度作为强确认；非显式分轨的固定 Range MP4 在同组有 HLS/DASH 时隐藏，仅有分片时阻止创建计划。桌面软件必须在计划创建和重试执行前复验并以 `fixed_range_fragment` 拒绝，不能把站点域名、`.mp4` 后缀或 FFmpeg 失败当作识别条件。
- **A144**：1.1.12 必须通过全量 Python/JavaScript/双清单、冻结运行时、隔离安装器和当前电脑覆盖安装；Chrome 重载并刷新现场 Behance 页面后，版本选择器只保留完整 HLS/DASH 或完整直链，固定 Range MP4 不可提交，重复操作不得新增 `desktop_download_failed/2%` 任务，完整清单下载达到正确 100% 终态并可打开目录。

当前 A96–A100、A102–A133、A135、A137、A139、A141 与 A143 已有实现和自动检查；A134 的实机失败证明配对 POST 成功后认证 GET 仍可能丢失请求头，A136 的实机失败证明任务完成不等于候选身份正确，A138 的实机截图又证明安全留空仍需候选自身预览，后续现场又依次暴露 CDN 备用地址和固定 Range MP4 分片问题。A144 的发行与真实 Behance 闭环按 1.1.12 验证报告收尾。A101/Chrome popup 同视口视觉截图仍按独立验证报告收尾。

## N. 1.2.0 职责收口、分片信息密度与补导门禁

本节取代 A100、A117 和 A93 中“必须继续提供缓存/录制/移动 UA/密钥/媒体控制入口”的历史要求；这些功能在 1.2.0 被明确退役，不再是缺失项。

- **A145**：`segmentOnly` 媒体候选组默认不进入列表；筛选器显示已隐藏数量并提供显式开关。打开后只显示紧凑诊断行和说明，不显示大预览、不提供任何下载按钮。
- **A146**：媒体候选组按捕获时间升序，最新项位于底部。首次打开自动选择并滚动到底部；用户主动选中旧项后，新候选到达不得抢走选择。
- **A147**：`completed_local` 任务在扩展和桌面均提供“导入现有文件到 Eagle”；调用只传 `planId`，服务端验证最终文件属于程序 `已完成` 根，幂等创建 Eagle job 并转为 `ready_to_import/90`，不得重新下载。
- **A148**：扩展只保留发现、媒体候选组、预览、筛选、配对、来源规则、提交与任务控制；桌面独占下载、HLS/DASH 执行、合并、FFprobe、文件与 Eagle。活动扩展不得含旧下载/解析/预览页、录制器、移动 UA、密钥面板、在线 FFmpeg、外部路由或 `chrome.downloads`/DNR 权限。
- **A149**：活动扩展不引用上游 `img/` 或猫图标，清单和 UI 只使用下载中转站自有图标。当前发行许可统一为 GPL-3.0；固定上游源码、版权、修改说明和历史 MIT 通知继续保留并与活动载荷明确区分。
- **A150**：覆盖安装在校验过的程序扩展目录内清理 1.1.x 遗留工具文件和目录，不删除扩展外文件、用户下载、数据目录或 Eagle 内容；失败回滚仍恢复覆盖前程序目录。
- **A151**：1.2.0 至少通过 94 项 Python/JS/双清单回归、全部扩展 JavaScript 语法、清单 JSON、冻结运行时和隔离安装器门禁；Chrome 人工复验确认分片默认隐藏、最新项定位和本地完成任务补导。

A145–A150 已有实现和自动证据。A151 的 94 项测试与语法/清单已通过，冻结运行时、隔离安装器和 Chrome 人工复验在 1.2.0 验证报告记录。

## O. 1.2.1 YouTube 结构化质量目录

- **A152**：YouTube MSE 页面不依赖 `video.currentSrc` 或零散 `webRequest` 猜完整媒体；扩展应从当前播放器公开的格式目录提取所有带直接 URL 的视频/音频格式，按 videoId 组成一个候选组，并在提交前展示真实标题、缩略图、时长、itag、分辨率、FPS、编码、码率和预计大小。只有加密签名字串而没有直接 URL 的格式不得伪造成可下载项，不实现签名或 DRM 绕过。
- **A153**：同一 CDN `/videoplayback` 路径的不同 itag 必须保留为独立质量/音轨，轮换但 itag 相同的请求才折叠。URL 明确携带 `range/bytes/start/end` 的请求即使响应头给出完整文件总大小也属于传输分片；无内容组、无时长且小于 128 KB 的音视频探测默认隐藏且不可提交。
- **A154**：1.2.1 必须通过全量 Python/JavaScript/双清单、全部扩展 JavaScript 语法、冻结运行时和隔离安装器；覆盖当前电脑后重载扩展并刷新现场 YouTube 页面，媒体列表只显示当前视频内容组，质量选择器出现播放器实际提供的多个视频档位和音轨，不再出现四条 7 KB 音频探测。选择方案必须继续创建 `route=desktop` 本机任务。

A152–A153 已有定向自动证据；A154 的历史发行和实机证据可通过 Git 历史恢复。

## P. 1.2.2 YouTube SABR 全画质

- **A155**：当 YouTube `adaptiveFormats` 提供多个视频高度但没有任何可复用 `url/signatureCipher`，且存在 `serverAbrStreamingUrl` 时，不得回退成唯一 360p progressive 候选；必须保留播放器实际公开的全部唯一高度，并继续把固定 Range/小型探测隐藏在诊断分区。
- **A156**：同一 videoId 的 SABR 目录只显示一个内容组；画质选择器按高度从高到低显示，不把 AV1/VP9/H.264 等同高度编码重复为用户无法判断的版本。标题、最大缩略图、时长和当前播放器信息仍属于该内容组，默认选择最高可用高度。
- **A157**：SABR 任务必须为 `route=desktop`。桌面按用户选择的准确高度解析视频轨和最佳音轨，再交给 FFmpeg 下载、streamcopy 合并、FFprobe 校验及 Eagle 流程。Cookie/Authorization 不写数据库、命令行、日志或诊断；若解析器需要 Netscape cookie 文件，只能创建在 `临时/<planId>`，并在成功、失败、取消时立即删除。
- **A158**：1.2.2 发行物必须固定并校验 yt-dlp 2026.06.09、Deno 2.8.1、FFmpeg/ffprobe；二进制包提供第三方许可和哈希，yt-dlp 对应源码进入同位置发布的独立源码 ZIP；全量测试、JS/清单、冻结运行时、隔离安装、当前电脑覆盖和现场 YouTube 1440p–144p 目录/至少一个所选画质下载闭环全部通过。

## Q. 1.2.3 通用内容页解析与字节分片重建

- **A159**：扩展与桌面必须把成对 `bytestart/byteend` 当作固定字节区间；原始分片不得进入 FFmpeg。重建只可删除这两个偏移参数并保留其他签名参数；无效、缺一项、逆序或超出安全整数的区间不得猜测。现场 `886–173864` 与 172,979 字节的等式必须进入确定性回归。
- **A160**：Instagram CDN 元数据只用于按精确资产 ID/流角色归组并补充时长、码率和完整大小估算；大整数资产 ID 不得因 JavaScript 浮点精度变化。同一轨的轮换分片只形成一个后备候选；稳定内容页候选存在时，重建后备默认退出媒体主列表，但可通过技术分片开关审计。
- **A161**：顶层 blob 视频只有在当前页或最近内容容器能提供同源稳定内容永久链接时才形成 `resolver=page`。候选必须显示该视频自身的当前帧、标题、时长和尺寸；不得从主页猜链接。链接矩阵至少覆盖 Instagram、TikTok、抖音、X、Reddit、Facebook Watch 和 Pinterest，且不为每站复制下载状态机。
- **A162**：通用页面解析必须由本机固定 yt-dlp/Deno 执行，并将结果交给统一 FFmpeg/FFprobe 流程。只允许公网 HTTP(S) 页面；Cookie/Authorization 只存当前任务内存，Cookie 上限 16 KiB，临时 Netscape 文件位于 `临时/<planId>` 并在成功、失败、取消时删除；数据库、命令行、错误和诊断不得包含秘密。
- **A163**：1.2.3 必须通过全量 Python/JavaScript/双清单、全部扩展 JavaScript 语法、冻结运行时、隔离安装/更新/回滚/卸载和当前电脑覆盖安装。Chrome 重载后现场 Instagram 默认只能看到内容级候选，创建“仅下载”任务不再出现 2%/Invalid data，最终文件 FFprobe 有效、预览与所选内容一致并可打开目录。

## R. 1.2.4 抖音当前主视频专用适配

- **A164**：确定性回归必须包含现场精选页的两个播放器：视口内主视频 `223.237` 秒、1920×1080、HTTP `currentSrc`，屏外预加载项 `49.041` 秒、720×1282、blob；测试必须证明旧“跳过 HTTP、只解析 blob”路径会选错 0:49。
- **A165**：抖音只允许视口相交且已就绪的播放器参与当前内容选择，并按正在播放、已有进度、可见面积和播放器面积确定唯一主项；屏外预加载、下一条缓存和空播放器不得继承当前标题、画面、时长或下载身份。
- **A166**：明确的 `/video/<id>` 或数字 `modal_id` 必须规范为 `https://www.douyin.com/video/<id>`；直接媒体与页面解析候选统一使用 `douyin:<videoId>`。不含明确内容 ID 的旧 `/jingxuan` 页面解析候选必须从可下载列表移除，不能猜测或重试。
- **A167**：当前播放器存在 HTTP 媒体源时直接提交该源；只有当前主播放器为 blob 时才提交规范化详情页给本机解析。标题、真实帧、时长、尺寸与当前播放器绑定；不支持的页面和失效会话分别返回 `douyin_page_unsupported`、`douyin_session_expired`，错误中不得泄露媒体地址或 Cookie。
- **A168**：1.2.4 必须通过全量 Python/JavaScript/双清单、全部扩展 JavaScript 语法、冻结运行时、隔离安装/更新/回滚/卸载和当前电脑覆盖安装。Chrome 重载并刷新现场精选页后，唯一主候选显示 3:43 及当前画面，下载输出经 FFprobe 验证时长/画面一致，并可打开目录或按选择导入 Eagle。

## S. 1.2.5 抖音逐视频标题与候选身份

- **A169**：回归必须包含至少两个同时存在的抖音 `feed-item`，并证明各自 `video_<id>`、作者和左下角文案不同；不得用标签页标题代替内容标题。
- **A170**：每个已加载且有明确视频 ID 的抖音播放器形成独立 `douyin:<videoId>` 候选；标题只读取自己的 `feed-video-nickname` 与 `video-desc`，当前项与预载项不得互相继承标题、预览、时长或下载地址。
- **A171**：blob/MSE 的底层音视频请求只有在 URL 与某个播放器来源精确匹配时才可继承内容身份；无法绑定视频 ID 的请求必须标记为技术分片、默认隐藏且不可提交，不能回退标签页标题伪装为完整视频。
- **A172**：1.2.5 必须通过全量 Python/JavaScript/双清单、全部扩展 JavaScript 语法、冻结运行时、隔离安装/更新/回滚/卸载和当前电脑覆盖安装。现场抖音页重载后，每张可下载卡片的作者/文案、视频 ID、预览、时长和最终下载必须一致。

## T. 1.2.6 通用信息流内容绑定与降噪

- **A173**：回归必须固定“页面 DOM 只有少量真实视频卡片，但网络层产生大量 HLS/DASH/音视频请求”的场景，并证明旧实现会把无法归属的请求各自显示成同名内容组。现场 X 首页证据为 4 个 `<video>`、18 个默认媒体组和 42 个隐藏播放分片；X 只作为复现样本，不得进入站点判断。
- **A174**：每个 blob/MSE 播放器若能从最近内容容器找到同源稳定永久链接，必须形成独立 `resolver=page` 候选；标题从该容器的语义标题/说明/可读正文中选择最有信息量的一条，过滤计数、时间轴、相对时间和控件文案。封面、时长和链接必须来自同一容器，不得回退标签页标题覆盖已识别正文。
- **A175**：同一标签页和来源域已有内容绑定页面候选时，无 `groupKey`/内容身份的孤立 HLS、DASH、视频和音频请求默认进入“未关联播放资源”诊断分区，紧凑显示且不可提交；没有内容绑定替代项的唯一完整清单必须继续可见可用。工具栏角标与 popup 必须调用同一默认分区口径。
- **A176**：1.2.6 必须通过全量 Python/JavaScript/双清单、全部扩展 JavaScript 语法、冻结运行时、隔离安装/更新/回滚/卸载和当前电脑覆盖安装。Chrome 重载并刷新现场信息流后，默认列表数量应接近真实视频卡片数，每张卡片标题/预览/时长可区分，技术开关能审计被隐藏请求且不能误下载。

## U. 1.2.7 通用信息流发现生命周期

- **A177**：回归必须固定“网页 DOM 中当前播放器已有稳定内容永久链接、独立正文、poster 和时长，但 popup 只有网络清单”的漏检场景；示例站点仅作现场证据，不得进入实现条件。
- **A178**：全页 MutationObserver 连续产生至少 100 次变化时，页面候选扫描必须在首个调度窗口内执行，不能因尾随防抖反复清除定时器而无限延期；一次扫描完成后必须能调度下一轮。
- **A179**：打开或刷新 popup 必须主动探测当前 HTTP(S) 标签的发现脚本。接收端缺失时只允许通过 `chrome.scripting` 注入 `eagle-bridge-candidate-logic.js` 与 `content-script.js`，随后立即触发页面候选扫描；已响应时不得重复注入，受限协议页面不得注入。
- **A180**：1.2.7 必须通过全量 Python/Node、全部扩展 JavaScript 语法、双清单、冻结运行时、隔离安装器四路径和当前电脑覆盖安装。扩展重载但不刷新已打开信息流页面后，打开 popup 必须出现当前视频自己的标题、预览和时长；未关联网络请求仍在技术区且不可下载。

## V. 1.2.8 通用页面分轨格式兼容

- **A181**：确定性回归必须覆盖通用页面只提供 MP4 视频轨和扩展名为 MP4 的独立音频轨、且没有合并格式的情况；格式选择不得继续只接受 M4A 音轨。
- **A182**：格式选择必须优先 MP4 视频+M4A 音频，其次兼容 MP4 视频+MP4 音频，再按合并 MP4、MP4 视频+任意音频和通用最佳格式逐级回退。实现不得包含 Pinterest 或其他站点域名分支，解析结果仍交给统一 FFmpeg streamcopy 与 FFprobe 验证。
- **A183**：1.2.8 必须通过全量 Python/Node、全部扩展 JavaScript 语法、双清单、冻结运行时、隔离安装器和当前电脑覆盖安装。现场 Pinterest Pin `72690981480504869` 必须选择 720×900 H.264 视频与 AAC 音频，合并输出同时包含有效视频和音频流。

## W. 1.2.9 结构化视频页面发现

- **A184**：必须固定“网页视频正常播放、扩展捕获多条网络请求但默认媒体为 0”的现场证据，并独立证明桌面页面解析器能列出该作品的完整格式，避免把下载器问题误判成发现问题。
- **A185**：页面明确提供视频类型、播放器地址和非首页的同源规范作品地址时，扩展必须形成一个 `resolver=page` 内容候选。标题、封面、时长和尺寸优先取页面公开元数据；普通网页、主页、跨源规范地址和没有视频声明的页面不得生成候选。
- **A186**：结构化页面入口必须按元数据能力工作，不包含 Vimeo 或其他站点域名分支。无法绑定内容身份的底层 HLS/DASH/分片继续只在技术区显示且不可提交，下载仍由统一桌面 yt-dlp/FFmpeg/FFprobe 状态机执行。
- **A187**：1.2.9 必须通过全量 Python/Node、全部扩展 JavaScript 语法、双清单、冻结运行时、隔离安装器和当前电脑覆盖安装。Chrome 重载并刷新现场 Vimeo 作品页后，默认列表必须显示一个以规范作品页为来源的内容卡片，创建任务后能解析实际可用格式。

## X. 1.2.10 多视频页面候选恢复

- **A188**：确定性回归必须覆盖一个页面解析候选与多个独立完整媒体同处同一标签页、同一来源域名的情况，并证明旧的域名级分区会错误隐藏完整候选。
- **A189**：blob/MSE 播放器必须从自身向外按距离选择第一个不同于当前页的稳定同源内容链接；没有更近作品链接的主视频保留当前作品页。实现不得包含 Pinterest 或其他站点域名分支。
- **A190**：同标签页或同来源域名不能单独证明两个候选是同一内容。完整 MP4、音频、HLS 和 DASH 必须保持可见可选；只有结构已确认的传输分片默认进入技术区。
- **A191**：1.2.10 必须通过全量 Python/Node、全部扩展 JavaScript 语法、双清单、冻结运行时、隔离安装器和当前电脑覆盖安装。Chrome 重载并刷新多视频页面后，主作品与相关视频应分别显示，且不再因存在页面解析候选而清空列表。

## Y. 1.2.11 自动跟随系统代理

- **A192**：默认网络模式必须为“自动”。每个远程媒体任务开始时重新读取当前用户的 Windows 系统代理；环回、localhost 和非公网字面 IP 永远直连，Eagle、本机 API、配对与控制信号不得进入代理。
- **A193**：桌面主界面必须提供“自动（推荐）”“始终直连”“手动代理”三种模式。手动模式只接受带有效端口且不含账号密码的 HTTP/Mixed 地址；无协议的 `127.0.0.1:7890` 必须规范为 `http://127.0.0.1:7890`。设置持久化但不得修改 Windows 或代理软件配置。
- **A194**：同一任务选择的代理必须同时进入 FFmpeg/ffprobe 直链与 HLS/DASH 输入、yt-dlp 的 YouTube/通用页面解析和直接字幕请求。自动模式检测到系统代理时先使用代理；网络连接失败后最多切换到直连一次，手动模式不得静默绕过用户指定代理，任何路径都不得循环。
- **A195**：403、超时和连接失败必须提示当前下载进程与浏览器代理可能不一致，并指向“网络设置”；诊断只包含模式、是否启用、检测来源和不含认证信息的主机端口，不包含代理账号密码、Cookie、完整媒体地址或本地路径。
- **A196**：1.2.11 必须通过全量 Python/Node、全部扩展 JavaScript 语法、双清单、冻结运行时、隔离安装器四路径和发行包验证。测试至少覆盖 Windows 系统代理检测、本机绕过、代理参数传递、手动/直连持久化与单次线路切换上限。

## Z. 1.3.0 微信视频号本机捕获与统一下载

- **A197**：微信视频号捕获是桌面软件的显式可选来源，首次升级默认关闭。桌面必须显示关闭、准备、等待微信、捕获中、需要恢复和失败状态；浏览器扩展、IDM hook 和 Eagle 处理器不得启动或复制该服务。
- **A198**：服务连续开始/停止必须幂等，正常退出后三秒内释放线程和端口；完全本机的合成入口能够完成“开启、发现一条候选、停止”的纵向闭环，且现有普通媒体与 IDM 回归不变。
- **A199**：每台机器生成独立根证书和私钥；私钥只在本程序受限数据目录，发行包、Git、SQLite、日志和诊断均不得包含。安装、查询和卸载只按可验证指纹操作，不删除非本程序证书。
- **A200**：HTTPS 代理只终止明确的视频号页面主机与承载其内部 API 模块的 `res.wx.qq.com` 资源主机，其他 CONNECT 原样隧道；监听固定回环，拒绝开放代理，回环 Eagle/API 永远直连。请求头、正文、连接数和超时都有固定上限。
- **A201**：开始捕获前原子保存完整 WinINET 代理配置；停止、退出、异常恢复和卸载精确恢复原值。若当前代理已被外部程序改变，不覆盖新值，必须停止自动恢复并提示用户。
- **A202**：每条视频以结构化 objectId 建立 `wechat-channel:<objectId>`；标题、作者、封面、时长和内容地址只来自同一 feed。不得使用标签页标题、请求时间、大小或 CDN 路径猜身份。
- **A203**：同一 feed 的多质量、视频/音频流、签名轮换和 Range 分片只形成一个候选组；缺少可靠身份的资源默认进入紧凑诊断区且不可提交，不能制造大量同名卡片。
- **A204**：微信首页推荐和视频详情页的原操作栏必须出现“下载”按钮；普通点击只为可可靠绑定的当前 `objectId` 创建当前播放质量的统一计划，悬停下拉菜单显示 feed 实际提供的其他质量，以及同时具备非空 `encfilekey`/`token` 的原始视频，原操作栏不可定位时提供等价悬浮按钮。页面只能提交服务端已登记的 `objectId`/`variantId`，不能传任意 URL、下载、解密或调用 Eagle；无法唯一确认当前内容时必须拒绝而不是猜测。桌面视频号页继续显示同一会话的标题、封面、作者、时长、质量和预计输出并保留手动入口。
- **A205**：下载 URL、必要请求头、解密键和签名只存于当前进程内存，终态立即清理。SQLite 仅保存安全计划元数据；重启后非终态任务必须要求回到视频号重新提交，禁止猜测旧 URL。
- **A206**：质量目录按捕获到的实际结构生成任意数量档位；选择某档后最终 ffprobe 分辨率与 UI 一致。原始视频变体只允许逐字节保留非空 `encfilekey` 与 `token`，不得携带 `basedata`、`sign`、`svrnonce` 或 `X-snsvideoflag`；明确 `fileFormat` 档位保留原签名查询并只追加对应 `X-snsvideoflag`。信息不足时只显示自动，不虚构原始/最高或 1080p 等质量。
- **A207**：应用层加密只在存在明确密钥和加密长度时使用独立实现的流式解密；固定明密文向量、分块边界、明文尾部和错误密钥均有测试。该能力不是 DRM 绕过，DRM 仍明确阻断。
- **A208**：下载、解密、分轨合并和校验全部属于 `MediaCoordinator`。只有 ffprobe 证明存在可播放视频流、合理时长和非零文件后才完成；取消/失败只清理本程序计划专属临时文件。
- **A209**：视频号与普通浏览器媒体共用 `download_plans`、任务列表、进度、停止、重试、打开目录和 Eagle 队列，不得新建第二套下载状态机。下载、解密、合并、校验和导入阶段不能长期伪装为 0%。
- **A210**：仅下载完成后状态为 `completed_local`，重启仍可见并能直接补导 Eagle，不重新下载；重复补导幂等。Eagle 离线时保留等待并在恢复后继续。
- **A211**：Eagle 来源只写可靠分享/内容页地址；无可靠来源留空，不把 CDN、代理端点或本地地址写成来源。标题、封面和任务必须属于用户选择的同一 objectId。
- **A212**：固定秘密值不得出现在数据库、日志和诊断。诊断只含版本、状态、端口、证书指纹后八位、脱敏主机/路径类别、计数和错误码，并能区分至少六类常见失败。
- **A213**：标题与结构化数据经过安全 JSON/DOM 编码，不能闭合脚本或执行代码；超大响应、压缩炸弹、慢连接、非法头、伪造身份和候选洪泛均被有界拒绝，桌面 UI 保持可用。
- **A214**：上次运行异常时，仅当当前代理仍明确属于本程序才恢复；端口冲突、证书丢失、微信页面结构不兼容时安全降级，不得造成全局断网或静默截获。
- **A215**：1.3.1 版本在 `pyproject.toml`、`constants.py`、Chrome/Firefox 清单、安装器与健康响应一致；健康门验证视频号模块与静态脚本哈希，但未启用时不要求证书已安装。
- **A216**：全新安装、1.2.11 升级、强制失败回滚和卸载均验证代理、证书、程序与用户数据归属。升级先停止捕获并恢复代理；卸载只移除本程序指纹证书和拥有的设置。
- **A217**：用户二进制 ZIP 不含外部仓库二进制/证书/密钥/Gopeed、开发数据库、用户路径、Cookie、签名 URL、外置 Python/C#/构建源码或 source map；浏览器扩展仅保留压缩后的运行文件。GPL 对应源码进入同位置发布的独立源码 ZIP。README、CONTEXT、STATUS、TASKS、ACCEPTANCE、ARCHITECTURE、DECISIONS、SECURITY、DEVELOPMENT、安装回滚和第三方清单与最终行为一致。
- **A218**：提交前运行 DEVELOPMENT 中全部 Python/Node/JS/JSON、冻结运行时、隔离安装器四路径、发行构建、许可证和秘密扫描；新增测试覆盖 TLS、注入、候选、下载、解密、Eagle、恢复和安装。
- **A219**：在 Windows 微信桌面客户端对用户有权保存的公开内容完成原操作栏按钮出现、当前质量直接下载、下拉菜单其他质量与原始视频、悬浮后备、桌面手动入口、打开目录、补导 Eagle、下载并导入、停止捕获和网络恢复；原始视频需用脱敏的响应总大小和 ffprobe 与转码档对照，最终 objectId/标题/封面/时长/画面/质量一致并有脱敏证据。
- **A220**：A197–A219 每项至少有自动测试、截图、任务记录、ffprobe 或 Eagle 实际结果之一。任一核心闭环失败必须回到对应任务修复并重跑，不得以已知问题标记 1.3.0 完成。
- **A221**：主窗口在 900×600 和 Windows 缩放导致内容高于视口时，必须提供可见的纵向滚动入口，使当前标签的详情、操作区和全局底栏都可到达；鼠标位于 Treeview 或下拉框时不得抢走其自身滚动。
- **A222**：媒体任务、视频号候选、IDM 导入记录和网站规则列表都有明确刷新入口及与数据语义匹配的清理操作。媒体与 IDM 的批量“清除完成”只移除终态记录；用户对单条记录明确执行“清理记录”时可停止并移出该目标任务，但不得影响其他任务。候选清理不停止捕获；规则清空恢复默认关闭。任何列表清理都不得删除下载/预览文件、修改 Eagle 或从无来源记录推断网站。
- **A223**：单个标签页连续产生至少 400 条媒体候选时，昂贵的工具栏可见数量归组必须按标签页有界合并，不能随每条请求同步全量重算。popup 必须优先读取候选快照；侧栏不得为每个候选创建远程视频元素，只延迟加载静态封面并跳过离屏渲染，当前选中项仍可在检查区按需预览。确定性基准应证明一次突发只执行一次角标归组，并验证 400 行侧栏为 0 个远程视频、检查区最多 1 个。
- **A224**：桌面定时刷新不得在 Tk 主线程执行 Eagle HTTP 探测，也不得因无变化而整表重建或重复解码同一预览。仪表盘状态应合并数据库读取；系统代理检测、视频号证书和桥脚本身份应有可失效的短期缓存，视频号候选计数不得渲染完整候选。自动回归必须证明慢探测立即返回、第二个同类探测单飞、未变化行不产生控件写入、预览只解码一次且设置变更会失效代理缓存。
- **A225**：新版“导入 Eagle”动作必须明确显示成功后删除本机下载文件，并在计划中持久化独立 `delete_after_import=1`；覆盖安装后仍运行的旧扩展 worker 即使缺少该新字段，其明确的 `importToEagle=1` 也必须得到相同的新计划语义。全部 schema 5 旧计划迁移时默认保留。只有 Eagle API 成功、job 为 `imported`、plan/job 路径完全一致且解析后位于程序“已完成”目录时才删除并清空 `final_path`。仅下载、未勾选、等待/失败、重复跳过、目录外文件、IDM 和用户原文件必须保留；删除失败不得把已成功的 Eagle 导入改成失败，并应保留路径与可见错误。

### 1.3.1 当前证据状态（2026-07-26）

- A197–A218 与 A221–A225 已有实现和确定性自动证据；Python 全量 172 项、6 组 Node、17 个活动 JavaScript 语法、双清单、版本同步和安装器 C# 编译均已通过。候选证据包含卡片/富文本标题回退、当前播放/任意 `fileFormat` 动态规格选择、未知 variant 服务端拒绝、签名查询保真、500 项会话上限与当前 objectId 绑定；真实微信首次运行还暴露并修复了“页面注入成功但内部 feed 不经过 fetch/XHR”的入口缺口，新增 `res.wx.qq.com` 叶证书轮换、模块依赖缓存键、`finder*` 返回观察、方法身份、返回语义和整数诊断回归。生命周期证据包含启动中途失败、异常租约和应用退出时优先恢复系统代理；桌面回归证明慢 Eagle 探测不阻塞、表格增量更新、预览复用、仪表盘聚合及代理/视频号静态健康缓存。schema 6 回归证明旧计划默认保留，只有显式新计划在 Eagle 成功和归属复验后删除；未勾选、离线、目录外、IDM/用户文件和文件锁定均保留，异常退出后的待清理项可恢复。
- 当前 schema 6 源码的 172 项 Python、6 组 Node、17 个活动 JavaScript 语法、双清单和安装器编译已通过，A223–A225 专项门禁已通过。1.3.1 冻结健康门、冻结接收任务、隔离新装/更新/故障回滚/卸载、发行构建和本机覆盖安装均已通过；在线健康返回 `version=1.3.1`、`databaseSchema=6`、`extensionProtocol=1`、`mediaReady=true`、`youtubeResolverReady=true`、`downloadEngine=desktop_ffmpeg`。A219–A220 仍待真实 Windows 微信客户端完成候选、仅下载、ffprobe、补导/导入 Eagle、正常停止和系统代理精确恢复；未取得这些证据前 1.3.1 只能作为预发布候选。

## AA. 1.4.0 统一前端重构

- **A226**：前端重构后的入口、后端契约与回归测试必须保持一致；不得恢复已退役下载器、浏览器 FFmpeg、第三方路由或第二套 popup。
- **A227**：桌面主窗口必须提供固定的下载任务、视频号、IDM 导入、设置与诊断导航，顶部独立显示 Eagle、本机服务、Chrome 配对和版本。下载任务及视频号使用主从详情，设置内嵌配对、网站规则、网络与更新。
- **A228**：桌面在 1120×720 与最小 900×600 下均可到达当前页动作；任务动作必须按状态和最终文件归属启用。`completed_local/imported` 才能显示 100%，下载、合并、校验、等待 Eagle 与失败阶段不得提前显示完成。
- **A229**：扩展 660×574 popup 必须保持唯一“媒体 / 任务”运行时，显示当前页标题/域名、连接状态、媒体候选组、视频/音频选择、合法输出名、折叠技术信息与两个明确交付动作；任务页显示处理大小、阶段、时间及按状态可用的停止、重试、补导、目录和来源动作。
- **A230**：托盘必须继续由 `launcher/Launcher.cs` 的 Windows Forms 宿主管理，只提供显示窗口、网站规则、唤醒处理、检查软件更新和退出；Python 不得接管窗口过程。无来源、DRM、分片、代理、Eagle 离线、上下文失效和删除安全边界的语义不变。
- **A231**：参考图和实现截图必须在同一比较画布进行视觉 QA；保存 1120×720、900×600 与 660×574 的全屏/重点状态证据并记录问题修复历史。提交前通过 DEVELOPMENT 中全部 Python、6 组 Node、活动 JavaScript 语法、双清单 JSON 与 C# 编译。
- **A232**：1.4.0 的 `pyproject.toml`、`constants.py`、Chrome/Firefox 清单、安装器、托盘、版本资源和发行脚本必须一致。A219–A220 未完成时 GitHub Release 只能标记 prerelease，不得通过界面重构掩盖真实微信终验缺口。
- **A233**：1.4.1 深色桌面界面必须由 `idm_eagle_bridge.main:main` 的正式启动链路创建，并直接使用同一 `Database`、`LocalApiServer`、`MediaCoordinator`、视频号服务和 `ProcessingService`；视觉夹具不得进入运行时。产品 PNG 与全部 UI 图标必须同时进入 Python package-data 和 PyInstaller `onedir` 载荷。验收至少包含真实计划到任务卡/详情的投影测试、全量 Python/活动 JavaScript/清单 JSON、冻结入口启动、发行包资源检查，以及不激活用户窗口的后台截图。
- **A234**：下载任务详情的停止、重试、补导 Eagle、文件位置和来源网页五个动作必须始终可见，只能按 `_media_plan_view`、最终文件存在性和安全权限置灰，不得通过隐藏按钮掩盖空间或状态问题。复核必须覆盖 DESK-001–DESK-025、原生托盘五项入口、1120×760 与 900×600 后台截图，并确认界面入口仍连接真实业务方法。
- **A235**：无用代码清理只能删除全仓无引用、无清单/事件/反射/打包用途且已有正式路径替代的实现。旧独立网站规则/代理弹窗、视频号无调用命令轮询、前端死函数/选择器和重复/未使用图标不得回退；schema 迁移、安装清理、安全门禁、外部进程入口、第三方许可与确定性夹具必须保留。提交前须通过静态引用复核、全量 Python/Node/JS/JSON、C#/冻结构建和安装健康门。

### 1.4.0 当前证据状态（2026-07-26）

- A226–A231 已有实现、自动回归和截图证据：173 项 Python unittest、6 组 Node、全部活动 JavaScript 语法、双清单 JSON 解析通过；Tk 1120×720/900×600 与扩展 660×574 在确定性任务数据下完成交互和视觉检查，浏览器控制台无 error/warning。
- A232 的源码版本同步已完成；发行 ZIP 与 SHA-256 文件已随 GitHub `v1.4.0-rc.1` prerelease 发布。稳定更新通道未推广该候选版，A219–A220 保持待真实 Windows 微信客户端验证。

### 1.4.1 当前证据状态（2026-07-28）

- A233 已通过：真实数据库计划经正式 `LocalApiServer/MediaCoordinator` 投影到新版 `MainWindow` 的任务卡、选中态、标题和来源详情；UI 使用的产品 PNG 与 60 个图标已进入包资源。
- 185 项 Python unittest、6 组 Node、17 个活动 JavaScript 语法和 Chrome 清单 JSON 通过；临时 PyInstaller `onedir` 已确认包含产品图、关键导航/动作图标与视频号 bridge，冻结入口 `--help` 返回 0。视频号页从隐藏状态切入时只恢复当前可见分栏，1338×835 后台截图确认捕获控制栏未被压缩。A219–A220 仍保持待真实微信最终媒体验收。
- A234 已通过：逐页功能矩阵未发现空按钮或业务能力删除；下载任务五个状态动作已改为常驻显示，活动、本机完成和失败三种状态在 1120×760 / 900×600 的后台窗口截图中均可到达。185 项 Python、6 组 Node、17 个活动 JavaScript、两份活动清单、1.4.1 重建、安装哈希和本机健康检查通过。
- A235 已通过：静态复核已清空 Python 未使用 import、仅定义未引用的顶层定义/方法/常量和活动 CSS 未引用 class 候选；新增旧弹窗、重复图标、视频号 `/poll`、`eval` 与 `enqueue_command` 禁止回退测试。187 项 Python、6 组 Node、17 个活动扩展 JavaScript、视频号 bridge、两份清单、冻结发行、安装哈希和本机健康门通过。

## AB. 1.4.4 DPI、长列表与统一任务

- **A236**：Windows Per‑Monitor DPI 模式下不得使用负像素字号。96/144/192 DPI 必须保持相同逻辑窗口尺寸，点值字体在 200% 下行高至少增长 1.7 倍；900×600 紧凑窗口不得隐藏清理、分页或任务状态动作。
- **A237**：媒体和视频号无论事实源有多少条，单次最多创建 12 张卡片；IDM 最多投影 80 行。200 条媒体/视频号压力刷新分别低于 0.15 秒，分页不得删除记录、文件或活动状态。
- **A238**：浏览器、视频号页面按钮和桌面视频号候选创建的计划必须全部调用同一 `MediaCoordinator.create_plan`，写入同一 `download_plans`；已打开桌面窗口在一秒内投影并选中新任务，扩展任务页不得只显示由自身创建的内存任务。
- **A239**：点击“开始捕获”的 Tk 回调在证书查询延迟 250 ms 时仍须在 100 ms 内返回。桌面媒体/视频号/IDM 和扩展任务页均有清理入口；服务端只清终态记录，不删除下载/预览文件、不修改 Eagle、不停止活动任务。
- **A240**：1.4.4 必须同步 `pyproject.toml`、常量、双扩展清单、安装器、托盘、版本资源与发行脚本，并通过全量 Python、6 组 Node、全部活动 JS 语法、双清单 JSON、900×600 视觉 QA、冻结运行时、隔离安装器、签名更新清单、GitHub Release 和当前电脑覆盖健康门。

### 1.4.4 当前证据状态（2026-07-28）

- A236–A239 已通过：96/144/192 DPI 逻辑尺寸与点值字体回归、媒体/视频号各 12 张卡和 IDM 80 行的有界投影、统一计划通知、250 ms 慢证书预检非阻塞，以及桌面/扩展终态安全清理均有自动证据。200 条媒体与视频号刷新分别约 0.08 秒和 0.03 秒；900×600 三页截图未出现工具栏或列表溢出。
- A240 的版本同步、194 项 Python、6 组 Node、全部活动 JavaScript 语法、双清单 JSON、冻结运行时、隔离新装/升级/故障回滚/卸载、发行 ZIP 和当前电脑覆盖健康门均通过。当前安装版本、扩展版本均为 1.4.4，数据库 schema 6 且覆盖前后数据库 SHA-256 一致。现有更新公钥对应的离线私钥不在本机，故签名 `update.json` 和稳定通道推广仍保持未完成；不得用新密钥或无签名清单冒充通过。

## AC. 1.4.5 实机可读性

- **A241**：自动 UI 比例必须同时考虑窗口 DPI 和屏幕像素密度回退。1920×1080/96 DPI 为 1.0，2560×1440/96 DPI 为约 1.333，3840×2160/96 DPI 为 2.0；Windows 144/192 DPI 分别至少为 1.5/2.0，二者取较大值而不得相乘。
- **A242**：Windows 存在微软雅黑 UI 时，正文、强调和粗体必须使用同一字体族；正文不少于 11pt，强调使用真实 `bold` 字重，不得混用 OPPOSans B/H 制造不同中文宽度和行高。按钮、徽章、导航和行高必须随同一比例变化。
- **A243**：IDM 超长文件名、来源和错误说明必须按实际字体像素宽度显示明确省略号，完整值保留在详情。媒体、视频号、IDM、设置和诊断在 900×600 下不得出现不可达操作或工具栏溢出；1120×720 三数据页须提供视觉证据。
- **A244**：1.4.5 必须通过 197 项 Python、6 组 Node、活动 JavaScript 语法、双清单、500/200/200 压力门、冻结运行时、隔离安装器、当前电脑覆盖、Git 推送和 GitHub 发行。稳定自动更新仍必须使用现有公钥对应的原离线私钥签名。
- **A245**：所有列表清理必须保持数据库关联完整。自动历史清理和批量终态清理应先解除计划、IDM job 与来源事件的外键关系，再清除无引用的流、分组和会话；单条媒体/IDM 清理必须保留本机文件与 Eagle 内容。超过 999 条记录时仍可批量清理；扩展清空当前媒体后，清理前已启动的异步回调不得把旧候选重新加入。维护清理失败不得终止正常任务处理线程。
- **A246**：Windows 主窗口标题栏必须使用与深色外壳一致的深色标题背景和浅色标题/系统按钮；不得通过无边框自绘牺牲系统菜单、最小化、最大化、关闭、拖动、双击最大化、贴边布局或任务栏行为。旧版 Windows 不支持标题栏颜色属性时应安全回退，不影响窗口启动。
- **A247**：任意 HTTP(S) 单页应用通过 History API、查询参数或片段地址切换内容时，扩展必须作废上一页面的在途识别与去重上下文，按设置清理旧候选，重新扫描当前 DOM，并同步刷新 popup 的标题、地址和候选。实现不得依赖具体网站域名；重复的同一地址事件不得重复清理。
- **A248**：扩展独立窗口必须跟随最近活动的普通 HTTP(S) 标签页。独立窗口自身或其他扩展页面获得焦点时不得覆盖网页标签身份；切换浏览器标签、切换普通窗口或当前标签页发生地址跳转后，独立窗口应自动更新页面信息和候选，不再固定使用创建窗口时的旧标签。
- **A249**：Tk 主线程只允许执行有界控件投影和对话框。诊断导出、批量历史清理、Eagle 来源更新、视频号操作和预览读取必须在后台执行，并通过有界 Queue、最新状态合并和代次校验返回；退出后不得遗留轮询回调。1000 条 IDM 与媒体压力场景各连续刷新 200 次时，普通/强制刷新最大值均应低于 50ms、待处理测试回调为 0，且诊断导出必须包含默认阈值 50ms、容量 256 的低噪音性能快照。
- **A250**：开启视频号捕获前若 WinINET 使用 PAC/WPAD，必须保留其 URL/自动发现配置，并由 Windows WinHTTP 按普通 HTTP 与 HTTPS CONNECT 的目标 URL 解析上游；手动 HTTP/Mixed 代理继续固定串联，`DIRECT`/TUN 决定继续交系统网络栈。PAC 预检失败或返回不支持的代理类型时必须在改写 WinINET 前失败，不能让微信或其他应用静默断网；停止仍精确恢复原配置。
- **A251**：扩展候选侧栏在点击候选、切换该候选质量或更新批量选择状态时，可以重绘选中态，但必须保留用户操作前的列表滚动位置；内容缩短时只钳制到新的最大范围。首次进入或明确跟随最新项时仍允许主动滚到底部。自动回归必须模拟子节点替换导致 `scrollTop=0` 的浏览器行为，并证明恢复、缩短钳制和跟随最新例外。
- **A252**：1.5.0 必须同步 `pyproject.toml`、常量、双扩展清单、安装器、启动器、版本资源与发行脚本，并通过全量 Python、6 组 Node、全部活动 JavaScript 语法、双清单 JSON、冻结运行时、隔离安装器四路径、签名更新清单、当前电脑覆盖健康门、Git 推送和正式 GitHub Release。覆盖安装必须保留用户数据库、任务、网站规则、浏览器配对和未处于捕获状态的可信证书；外部证书工具超时必须终止子进程并安全中止，不能永久等待或留下孤儿进程。

### 当前工作区视频号原始画质证据（2026-08-01）

- A204/A206 的确定性回归已证明：原始视频变体只逐字节保留非空 `encfilekey` 与 `token`，编码值不被重编码；明确 `fileFormat` 变体继续保留完整捕获查询并追加 `X-snsvideoflag`；缺少原始凭据的基础地址显示自动质量。
- 用户真实微信会话已确认原始画质修复有效；响应大小和 ffprobe 的脱敏发布证据尚未单独归档。A250 的 PAC 配置、启动继承、失败保护、普通 HTTP 与 HTTPS CONNECT 五条确定性回归已通过；当前 `127.0.0.1:7890` VPN 上游的临时回环访问也已返回视频号入口并完成跳转。加入证书工具有界超时后，全量 Python 251 项、6 组 Node、18 个活动 JavaScript 语法与双清单 JSON 已通过；真实微信候选与停止恢复仍待用户复验。
