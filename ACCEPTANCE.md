# 当前验收标准

本文件只描述当前版本的有效验收条件。

## 版本与发行

- 品牌名必须为“留底”，产品全称必须为“留底下载器”，组件名称必须为“留底桌面端”“留底浏览器扩展”“留底安装器”；产品全称后不得增加其他词。
- 桌面标题栏必须显示 `留底下载器 v{当前版本} by阿毅i`。
- `pyproject.toml`、`constants.py`、Chrome/Firefox 清单、安装器、启动器和发行脚本的版本必须一致。
- 用户 ZIP 只能包含单文件安装器、使用说明、许可证、二进制清单和源码获取说明。
- 用户可见发行目录不得暴露 `.py`、`.pyw`、`.cs`、`.ps1`、`.spec`、`.toml`、source map 或临时构建目录。
- FFmpeg、FFprobe、yt-dlp、Deno、扩展运行文件和必要许可证必须完整进入安装载荷。
- 发布前必须通过全量测试、冻结运行时、隔离安装、覆盖升级、故障回滚、卸载和载荷扫描。
- 1.6.3 及后续稳定自动更新必须只读取 `Ly233ly/download_video` 的 GitHub 正式 Release：标签必须是更高的严格语义版本，Windows 资产名称、资产 API 身份、公开下载地址、GitHub `sha256` 摘要和大小必须唯一且完全匹配。
- Release 元数据不得跟随跳转；资产下载只允许 GitHub API 直接响应，或一次跳转到 `release-assets.githubusercontent.com` 的 HTTPS 443 地址。不得携带仓库凭据到资产域名。
- 下载必须验证响应大小、实际字节数和 SHA-256；ZIP 必须限制文件数、单文件/总大小和压缩比，并拒绝绝对路径、盘符、UNC、`..`、Windows 保留名、符号链接、大小写冲突、路径类型冲突、多重或深层安装器。失败必须清理临时文件。
- 1.6.2 不认识 GitHub API 更新机制，必须手动覆盖到 1.6.3 一次；1.6.3 应能发现和安全安装 1.6.4 及后续正式版本。

## 浏览器媒体

- 扩展按“媒体候选组”展示内容；同一视频的画面、音轨和不同质量不得拆成无关项目。
- 下载前必须显示可识别的标题、预览、媒体类型、清晰度、音轨和预计输出。
- 原始 URL 只能作为展开后的技术信息，不能代替用户可读信息。
- 普通直链、HLS、DASH、分离音视频和页面解析任务统一提交给桌面端处理。
- 固定字节分片、预加载资源和无法归属具体内容的请求默认隐藏且不可误提交。
- 候选很多时，点击底部候选、切换质量或批量选择不得把列表滚回顶部。
- 抖音等单页应用切换视频时必须保留当前标签已捕获候选；只有完整文档导航才按自动清理设置清空。
- 完整文档导航的旧候选清理必须发生在新文档媒体入库之前；不得通过异步闹钟回调迟到删除已经显示的新候选。Manifest V3 Worker 冷启动时必须暂存导航边界，并在接收第一个新候选前完成清理。
- 任务状态、进度和错误必须来自桌面端真实状态；未完成任务不得显示 100%。
- Eagle 明确不可用时，单个与批量主按钮必须变为“下载到电脑”，不得让用户先安装 Eagle；Eagle 补导按钮必须明确禁用并说明文件会保留。
- 新安装的扩展必须无需人工输入配对码即可连接；桌面软件不得显示六位配对码。
- 扩展找不到桌面软件时必须显示“自动连接本机软件”入口和明确启动提示；令牌丢失时只允许已登记的同一个扩展来源自动恢复，网页和其他扩展不得冒领。
- 保留配对令牌但桌面软件不可达时，所有依赖桌面的下载、导入、任务和设置动作必须禁用；扩展应继续低频探测并在桌面恢复后自动重新连接。
- 健康、任务与候选的异步旧响应不得覆盖更新的连接状态或实时候选；popup 关闭后不得重建轮询和提示定时器。
- 快速切换标签页时，旧标签页的候选、站点状态和工具状态不得覆盖当前页面；快照或预览临时超时不得清空已显示候选。
- popup、本机 API、自动配对和响应正文解析必须有协调后的有界超时；前端超时不得早于后台重试最坏预算而造成任务已创建却提示失败。
- DRM 内容只识别并明确提示，不实现绕过。

## 本机下载与校验

- 所有远程下载、音视频合并、FFprobe 校验和文件落盘均由本机桌面端完成。
- 分离音视频默认使用 FFmpeg streamcopy；只有容器或编码不兼容并且用户明确选择时才转码。
- 下载上下文只按任务最小化使用，不把 Cookie、Authorization 或签名 URL写入普通日志。
- 页面解析使用 Cookie 和 Authorization 识别页面后，不得把页面凭据转发给不同主机的媒体 CDN；同主机凭据也不得超过 FFmpeg 的安全请求头预算。
- 网络错误只能执行有上限的重试；不得无限循环或静默切换到不受控第三方服务。
- 临时数据库、文件系统或 Eagle 协议异常不得永久杀死唯一后台处理线程；下一轮唤醒必须仍可继续处理。
- 健康探测的并发刷新必须复用同一轮外部工具探测，不得为每个请求重复启动整组子进程；单个外部工具探测必须有短超时。
- 用户在校验阶段停止任务后，完成状态不得覆盖取消状态；程序创建的临时或最终输出必须按归属安全清理。
- 仅下载完成后必须显示最终路径，并提供打开所在文件夹和后续补导 Eagle 的入口。
- 桌面下载任务列表不得显示页码或翻页按钮；当前读取到的任务必须在同一个滚动列表中连续访问。

## Eagle 与文件安全

- Eagle 导入和来源更新只调用官方本地 Web API。
- Eagle 是可选能力。浏览器和微信视频号的下载、合并、校验、打开文件、停止、重试和记录清理不得依赖 Eagle。
- 本机健康接口必须公开 Eagle 可用性；Eagle 明确不可用时，新媒体计划必须关闭 `importToEagle` 和 `deleteAfterImport`，即使旧扩展请求了导入也要安全降级并保留文件。
- 只有 IDM 导入任务或已经完成本机下载后的补导动作可以等待 Eagle；Eagle 恢复后无需重新下载即可继续导入。
- 桌面顶部状态必须说明“Eagle 未连接 · 下载可用”；视频号和媒体任务的 Eagle 按钮按能力禁用，本机下载按钮保持可用并成为主操作。
- 对已导入 IDM 项目补写来源需要 Eagle；未连接时必须阻止远程更新并保留本机记录。其他来源记录、打开文件、打开网页和清理记录仍应可用。
- 同一已完成计划的并发补导必须原子复用同一任务，不得创建孤立或重复导入任务。
- 相同内容被 Eagle 去重后，媒体计划必须进入可解释的完成状态并强制保留本机文件，不得永久卡在等待导入。
- IDM 文件和用户原下载文件不得移动、删除或修改。
- 只有用户明确选择“导入成功后删除”，Eagle 官方 API 已确认成功，并且计划路径、任务路径和“已完成”目录归属全部匹配时，才可删除程序创建的最终副本。
- Eagle 失败、文件锁定、目录外文件、用户文件和 IDM 文件必须保留。
- 自动删除前必须同时验证计划最终路径、关联任务文件路径和程序“已完成”目录三者一致；任一不一致都必须拒绝删除。

## 缓存管理

- “文件管理”必须分别展示程序临时下载、任务预览和旧版下载日志的总占用，并提供手动清理入口。
- 用户必须能设置 `0-365` 天的自动缓存保留期；`0` 表示关闭自动清理，且不得影响手动清理。
- 手动清理只删除未被活动任务使用的程序缓存；自动清理还必须满足保留期已经到期。
- 缓存清理不得进入“已完成”目录，不得删除活动任务缓存、IDM 原文件、用户文件或无法确认安全边界的链接与目录。
- 清理删除任务预览后必须同步清除数据库中的失效预览路径；清理失败不得阻塞下载和 Eagle 导入队列。

## IDM

- IDM hook 必须快速返回，只负责入队、合并活动任务和发送唤醒信号。
- 等待 Eagle 的 IDM 任务占满单批处理上限时，不得饿死已经到期且通过归属校验的维护工作，也不得阻塞独立媒体下载线程。
- 程序创建并关联媒体计划的 MP3、M4A 等音频可以进入 Eagle；仅独立 IDM hook 输入继续遵守启用的文件类型规则。
- 有可靠来源时写入 Eagle 来源；没有来源时仍然导入，不猜测网址。
- 网站关闭只能来自扩展明确发送的 `site_disabled` 或 `ignore` 事件。
- 安装器发现 IDM 已配置其他杀毒软件路径时不得覆盖。

## 微信视频号

- 捕获默认关闭，只能由用户明确启动；证书确认必须由用户完成，本机代理只监听回环地址。
- 当前内容必须由强证据唯一绑定；地址、卡片、播放元素、媒体或封面证据冲突时拒绝。详情接口只允许 `data.object` 的唯一直接主对象激活，关联推荐不能抢占当前项。
- 共用 CDN 路径必须以内存 `encfilekey` 区分；`blob:` 绑定必须随播放器加载、清空或媒体变化失效，不能回退到最新候选。
- 页内、桌面和后台缺省提交统一优先明确 `deliverySpec` 画质；原始视频必须具备核心凭据和 feed 文件大小，完整地址回退必须严格验证身份与实际字节数。
- PAC/WPAD、系统 HTTP/Mixed 代理和可达的留底备用端口应作为捕获上游；任何不安全配置必须在改写 WinINET 前失败。停止、退出、升级和卸载必须恢复程序仍拥有的代理快照并清除租约。
- Eagle 不可用时页内下载必须安全降级为本机保留，不得拒绝下载或删除文件。
- 自动与真实验收的完整场景、错误码和秘密不落盘要求统一以 [微信视频号技术文档](docs/WECHAT_CHANNELS.md) 为准。

## 隐私与本机边界

- 媒体、Cookie、签名、请求头和解密信息不得上传到第三方服务。
- 配对令牌、网站规则、任务记录、用户路径和开发机数据不得进入发行包。
- 本机 API 只监听回环地址，并验证扩展来源、配对状态和随机令牌。
- 自动恢复令牌必须精确匹配数据库已登记的扩展 Origin，并在签发新令牌时立即使旧令牌失效。
- 安装、更新、回滚和卸载只能操作能够验证归属的程序文件、注册表项、证书和代理配置。

## 桌面界面

- 所有声明为圆角的自绘按钮、卡片、导航选中块、下拉框、徽标、滚动条和进度条必须使用连续且抗锯齿的圆弧，不能用直线切角、非抗锯齿 Canvas 曲线或可见短线段模拟圆角。
- 自绘控件首次映射时必须直接排入最终绘制，不能依赖点击、悬停或取得焦点后才显示正确圆角；圆角容器的矩形内容层不得覆盖四角的抗锯齿表面或焦点边框。
- 圆角几何必须在亮色、深色、高 DPI、窗口缩放和禁用状态下保持一致；放大检查边缘必须存在逐级混色像素而非实色阶梯，并且不得增加无界 Canvas 项或明显拖慢刷新。
- 四角抗锯齿图块与水平、垂直直边必须在 Tk 的排他右/下坐标边界完整重叠；按钮底边和右边不得出现仅角部着色、中央漏出父背景的断口。
- 顶部连接状态点必须是中心和水平、垂直方向均对称的抗锯齿正圆，不得使用小尺寸非抗锯齿 `Canvas` 椭圆形成方块。
- 首次启动必须直接使用数据库记录的当前主题，不得沿用模块加载时的旧主题默认色；一次主题切换后，状态点、徽标及所有缓存圆角图块必须同步重绘，不得残留旧主题方形底色。
- 从未保存过主题的首次安装必须以浅色启动；用户切换后必须持久化最后选择，升级或后续启动不得重置为浅色。
- 页面使用的自定义 ttk 样式必须显式配置当前主题的前景与背景；首次直接进入深色主题时不得回退到 Windows/Tk 的浅色默认样式。
- 周期状态刷新不得覆盖用户尚未保存的网络代理输入；持久化失败必须留在当前页面并给出可恢复提示。
- 任务和视频号卡片必须支持鼠标、Enter 与 Space，鼠标点击后应取得焦点；自绘按钮、卡片和导航必须显示连续清晰的键盘焦点状态。
- 大量任务下刷新和暖切页不得随历史记录线性恶化；任务列表、摘要和状态传播查询必须使用有界集合或索引。

## 当前人工复验

- 2026-08-21 已完成用户实际代理线路下的视频号候选、Morph 目标及连续 5 条相邻视频下载、FFprobe、秘密不落盘和停止恢复复验；后续修改捕获、代理或当前绑定时必须重复。
- 在 Chrome 实际工具栏 popup 中确认长列表点击底部候选不会回跳。
- 从已发布 1.6.2 手动覆盖到 1.6.3，确认数据库、任务、设置、代理和扩展状态保留；发布后用真实 GitHub API 和公开资产完成一次端到端下载/解压验证，并确认 1.6.3 报告已是最新版。
