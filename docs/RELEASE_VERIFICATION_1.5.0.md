# 1.5.0 发布验证记录

验证日期：2026-08-08

## 发行载荷

- ZIP：`download-transfer-station-1.5.0-windows-x64.zip`
- 大小：147690676 字节
- SHA-256：`384cd962cbea6ebfa5032db966f7919d6ad9c3e597d0ac621e2f6476cf411809`
- 用户 ZIP 只包含单文件内嵌安装器、使用说明、许可、二进制清单和源码获取说明，不包含外置 `app`、项目源码或 source map。
- 独立 GPL 对应源码包：`download-transfer-station-1.5.0-source.zip`，27235455 字节，SHA-256 `b741c5e1e165661dd9a4779b1abf43fc29f98aee3effa2cbeed7785a669c8bbd`。
- 数据库结构：6
- 扩展协议：1

## 自动验证

- Python：251/251 通过。
- Node：6 组扩展逻辑测试通过。
- 活动扩展 JavaScript 语法与 Chrome、Firefox 双清单解析通过。
- PyInstaller 冻结运行时通过。
- 隔离安装器的全新安装、覆盖升级、故障回滚和卸载四路径通过。
- Python 以优化级别 2 冻结，运行载荷扫描不存在 `.py`、`.pyw`、`.cs`、`.ps1`、`.spec`、`.toml` 或 `.map`；17 个扩展 JavaScript 均转换为压缩运行文件。

## 本机覆盖安装

- 安装目录：`C:\Users\MSI\AppData\Local\IDM-Eagle自动导入助手`
- 注册表 `DisplayVersion` 与健康接口均为 1.5.0。
- 健康接口报告媒体、YouTube 与桌面 FFmpeg 就绪，视频号捕获默认关闭。
- 数据库 `integrity_check` 为 `ok`，结构版本为 6。
- 覆盖前后的设置、网站规则、下载计划、任务和导入指纹数量一致；新增来源事件来自升级后的正常启动与捕获检查。
- 启动器、IDM hook、后端、扩展清单和 UI 脚本哈希与发行载荷一致。
- 不存在残留更新备份或视频号代理租约；既有可信视频号证书得到保留。

## 安装挂起回归

首次覆盖升级暴露出旧版清理命令调用 `certutil -delstore` 时可能无限等待。1.5.0 已将证书命令限制为 15 秒；安装器等待超过 20 秒会终止清理子进程并中止升级。没有活动代理租约的覆盖升级不再无条件删除可复用证书。修复后重新生成完整发行包，并重复通过全部自动验证与本机覆盖安装。

## GitHub 发布状态

发行 ZIP 与 SHA-256 已就绪。稳定发布仍需项目原有的 RSA 更新签名私钥生成 `update.json`；该私钥不在工作区、环境变量、GitHub Secrets 或本机常用目录中。为避免旧版客户端验签失败，不生成替代密钥，也不发布伪造或未签名的稳定更新清单。代码可正常推送，GitHub `v1.5.0` 先保持草稿，恢复原有私钥后再完成稳定发布。
