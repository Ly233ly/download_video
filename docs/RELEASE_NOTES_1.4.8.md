## 主要改进

- 修复软件启动后主窗口无响应（卡死）的问题：深色标题栏不再同步处理待定界面回调，避免与动态换行控件的尺寸刷新互相触发，窗口启动后立即可以操作。
- 后台下载服务、IDM 唤醒、微信视频号捕获与 Eagle 导入链路不受影响。

## 兼容性

- 数据库结构保持为 6，浏览器扩展协议保持为 1。
- 覆盖安装会保留现有下载任务、浏览器配对、软件设置和用户数据库。
- Chrome 与 Firefox 扩展版本同步升级为 1.4.8。

> 建议直接运行“一键安装.exe”覆盖升级。升级完成后如浏览器扩展没有立即刷新，可在扩展管理页重新加载一次扩展。
