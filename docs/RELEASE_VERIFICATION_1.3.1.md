# 1.3.1 发布验证报告

验证日期：2026-07-26

发布类型：预发布候选

## 本轮范围

- 浏览器大量媒体候选性能：角标归组按标签页有界合并，popup 优先读取候选快照，侧栏不再为全部候选创建远程视频解码器。
- 桌面性能：Eagle 探测后台化、表格增量更新、预览复用，并缓存代理与视频号静态健康检查。
- Eagle 导入后清理：浏览器、批量、补导和微信视频号统一在 Eagle 成功后删除本程序创建的最终副本；仅下载、失败、重复、目录外文件、IDM 和用户文件保留。
- 旧扩展 worker 兼容：缺少 `deleteAfterImport`、但明确发送 `importToEagle=1` 的新计划使用相同清理语义；schema 5 历史计划迁移仍固定为保留。

## 自动验证

- Python unittest：172 项通过。
- Node 逻辑：6 组通过。
- 活动扩展 JavaScript：17 个文件通过 `node --check`。
- Chrome/Firefox 清单解析与 1.3.1 版本同步通过。
- 数据库健康门：schema 6。
- 冻结运行时：1.3.1、扩展协议 1、`desktop_ffmpeg`、FFmpeg/ffprobe、yt-dlp/Deno 和视频号静态桥均就绪。
- 冻结 IDM 接收模式：退出码 0，唯一任务持久化成功。
- 隔离安装器：全新安装、成功升级、故障回滚和卸载全部通过。
- 本机覆盖安装：在线健康返回 1.3.1，升级备份在健康门通过后清除。
- CodeGraph：已同步，107 个文件、1,992 个节点、2,971 条边。

## 性能与安全证据

- 400 条候选角标归组由累计约 4.45 秒降为一次约 30 毫秒。
- 400 行 popup 夹具的侧栏远程视频从 400 个降为 0 个，只保留当前检查区 1 个预览。
- 清理前复验 Eagle 成功、job 为 `imported`、plan/job 路径一致，并要求解析后的路径位于本程序“已完成”目录。
- 删除失败只记录清理错误，不回滚已经成功的 Eagle 导入；异常退出后的待清理项可在下一次循环恢复。

## 发行物

- 文件：`下载中转站-1.3.1-Windows-x64.zip`
- 大小：155,347,539 字节
- SHA-256：`860441489793ed6789fdb93f4fb7c2d200c63e38c3bfc3958fe6e5ca92cb82ad`
- 固定工具：PyInstaller 6.21.0、FFmpeg 8.1.2、yt-dlp 2026.06.09、Deno 2.8.1。

## 未完成的真实终验

A219–A220 仍需在真实 Windows 微信客户端完成当前/其他质量、仅下载、ffprobe、补导/导入 Eagle、正常停止和系统代理精确恢复的最终闭环。因此 1.3.1 发布为预发布，不标记为稳定版。
