# 当前状态

## 产品

- 品牌名：`留底`
- 产品全称：`留底下载器`
- 产品说明：`免费开源的 Windows 本机媒体下载与归档工具`
- 宣传口号：`想留的，留个底。`
- 组件：`留底桌面端`、`留底浏览器扩展`、`留底安装器`
- 当前版本：`1.6.2`
- 平台：Windows 10/11 x64
- 数据库结构：`6`
- 扩展协议：`1`
- 下载引擎：本机 FFmpeg、FFprobe、yt-dlp 和 Deno
- 浏览器扩展：Chrome、Edge、Firefox

## 当前能力

- 浏览器发现并展示媒体候选组，桌面端统一执行直链、HLS、DASH、分离音视频和页面解析下载。
- 下载前展示标题、预览、媒体类型、质量、音轨和预计输出。
- 支持仅下载、导入 Eagle、完成后补导，以及显式选择的“Eagle 成功后删除程序副本”。
- Eagle 是可选能力：未安装或未启动时，浏览器单个/批量任务和视频号仍完整下载到本机；扩展与桌面自动切换主操作，启动 Eagle 后可补导。
- IDM 完成事件快速入队；无来源文件仍可导入，有可靠来源时才写入 Eagle。
- 微信视频号捕获支持当前质量、其他实际质量和有效凭据下的原始视频。
- 视频号捕获支持 Windows HTTP/Mixed 系统代理和 PAC/WPAD 上游，并在停止、退出、升级和卸载时恢复程序拥有的代理设置。
- 桌面和扩展使用统一任务状态；未完成阶段不会提前显示 100%。
- 浏览器扩展安装后自动连接桌面软件；桌面端不再显示六位配对码，同一扩展的令牌丢失后可按来源校验自动恢复。
- 桌面“文件管理”可统计并手动清理临时下载、任务预览和旧版日志，也可设置 `0-365` 天的自动缓存保留期。

## 本轮修复

- 视频号已完成当前内容绑定、连续刷视频、明确画质默认选择、原画严格验真和代理/VPN 兼容修复。真实 Morph 52 秒视频及连续 5 条相邻视频均由页内按钮完成下载并通过 FFprobe，未串片；完整实现、故障矩阵和验收证据统一见 `docs/WECHAT_CHANNELS.md`。
- 普通任务卡和视频号卡片高度按真实三行内容及圆角安全内边距调整，不再发生正文请求高度超过容器、底行或进度条被裁切的问题。
- 静默安装和版本升级完成后在后台启动，不弹出主窗口；普通手动安装仍显示正常引导。
- 自绘圆角控件改为直接排入首次绘制，不再多等待一轮空闲回调；按钮、导航和选择控件首次打开即显示最终形态。圆角容器按半径计算安全内容边距，列表卡片内层矩形不再遮住外层圆弧。
- 共用圆角绘制层已按 Tk Canvas 的排他右/下坐标语义修正直边终点，四角图块、水平直边和垂直直边逐像素连续闭合；按钮底边及右边不再漏出白色父背景。浏览器连接状态值也已显式绑定当前主题，首次深色启动不再出现浅色状态条。
- 主题启动语义已锁定：没有主题记录的首次安装使用浅色；用户切换后写入数据库，升级和后续启动继续使用最后选择。
- 扩展点击候选时只原位更新选中状态，不再重建滚动列表；滚动到底部点击最后一项不会向上回跳。
- 页面解析凭据不会再跨主机传给媒体 CDN，修复抖音超长 Cookie 触发 FFmpeg `Invalid argument`；同类错误现在显示准确原因。
- SPA 视频切换只重置发现上下文，不再清除当前标签的既有候选；完整页面导航仍遵循自动清理模式。
- 完整页面导航不再通过迟到的 `alarms.get` 回调删除候选；高频模式在主框架 `onBeforeNavigate` 同步清理，默认模式在 `onCommitted` 清理。Worker 冷启动期间的导航边界会保留到快照就绪，并在首个新媒体入库或弹窗读取前冲洗。
- 桌面“浏览器连接”页已移除配对码；扩展未连接或软件未启动时会显示“自动连接本机软件”，并给出明确状态。
- 用户 ZIP 只保留单文件安装器、说明、许可、二进制清单和源码获取说明；运行载荷嵌入安装器。
- 缓存清理与最终文件清理分离；活动任务、“已完成”目录、IDM 原文件、用户文件和不安全链接不会被缓存清理删除。
- 下载任务页面已移除分页控件，最多 200 条当前任务直接在左侧滚动列表中连续显示。
- 桌面共用圆角已绕过 Tk 8.6 Canvas 在 Windows 上缺少抗锯齿的问题，改用 8×8 子像素超采样并缓存四角图块；按钮、卡片、导航、下拉框、徽标、滚动条和进度条不再显示斜切角、分段圆弧或实色阶梯锯齿。
- 顶部三个连接状态点已改为对称的 8×8 子像素超采样正圆；主题颜色在控件实例化时解析，不再被模块加载期默认值锁定。单次亮暗主题切换会重建状态点、状态徽标和全部图片型圆角表面。
- 本机健康接口新增 `eagleAvailable`，桌面、扩展和视频号页内按钮使用同一能力来源；Eagle 不可用时旧扩展的导入请求也会被桌面 API 强制降级为本机保留任务，并关闭删除标记。
- 扩展单个/批量操作在无 Eagle 时以“下载到电脑”为主按钮；任务补导、桌面媒体补导、视频号 Eagle 交付、IDM 重试和已导入来源更新都会明确禁用，停止、重试下载、打开文件、来源网页与记录清理保持可用。
- 浏览器在线导入、视频号页内“下载”和无 Eagle 后的任务补导统一默认保留本机副本；只有桌面中明确标注“完成后删除本机副本”的视频号按钮会设置安全删除意图。
- 补导改为数据库原子占有计划，解决并发点击留下孤立任务或重复导入；保留期删除同时校验计划路径、任务路径和程序“已完成”目录，拒绝目录外文件。
- 后台导入服务可从临时数据库、文件系统、Eagle 断连、HTTP 协议和非法编码异常中继续下一轮；IDM 满批次不再饿死安全清理。
- 程序媒体任务的 MP3/M4A 不再被 IDM 独立文件类型规则误伤；重复内容会终结为 Eagle 已有内容并保留本机文件，不再卡在等待导入。
- 扩展通过最新请求代次消除健康、任务和候选旧响应覆盖；桌面断连时全部依赖动作关闭并持续自动重连，popup 卸载清除在途请求和定时器，批量仅下载防止双击重复创建。
- 桌面/视频号补齐能力回调异常、缺省安全降级、IDM 原文件丢失、离线来源同步和打开文件/网页失败等按钮与错误路径。
- 第二轮扩展时序审计修复快速切页旧响应、站点开关永久加载、卡住的响应正文、快照超时清空候选、批量按钮重复提交和 popup 关闭后重绘；候选处理 400 项降低约 60%，1000 项降低约 62%。
- 第二轮桌面 UI 审计修复周期刷新覆盖未保存网络设置、卡片缺少 Space 激活与焦点环、按钮/下拉框焦点丢失、损坏保留期配置崩溃和设置保存异常冒泡；圆角覆盖率缓存降低压力场景首次渲染开销。
- 健康探测改为 single-flight 并行探测，避免并发刷新启动重复子进程；任务列表、摘要、状态传播和轮询补齐索引，5 万计划列表由约 75.93 ms 降至 2.74 ms。
- 修复停止与校验完成竞态、GET/健康接口异常断连、Eagle 404 响应资源泄漏、无 Eagle IDM 批次重复超时和无界来源事件匹配。

## 验证结果

- 2026-08-21 视频号定向验证：115 项 Python 视频号/扩展测试和 Node 页面 bridge 测试通过；Morph 目标与连续 5 条相邻视频真实下载、FFprobe、秘密检查和代理恢复通过，独立验收 PASS。
- Python：362/362 通过，包含视频号、下载、代理、安全、数据库、UI 和安装边界。
- Node：6 组扩展逻辑测试通过；17 个活动 JavaScript 文件语法及 Chrome、Firefox 双清单解析通过。
- 桌面 UI：82 项定向测试通过；亮暗主题主要页面、首次绘制、主题持久化、圆角边界与卡片截图通过。标准启动约 237 ms，暖刷新均值约 3.49 ms；200 条任务压力启动约 1.455 s，暖刷新均值约 3.32 ms。
- 后端：16 线程并发读写 800 轮、0 错误；8 路并发健康探测由 32 个子进程/214.6 ms 降为 4 个/57.4 ms。
- 留底桌面启动器与留底安装器 C# 编译通过。
- 改名后的 PyInstaller 冻结运行时、隔离新装、覆盖升级、失败回滚、卸载和当前电脑覆盖安装全部通过。
- 1.6.2 正式构建未使用跳过测试选项；PyInstaller 冻结运行时、隔离新装、覆盖升级、模拟失败回滚和卸载全部通过。
- 本机当前运行 `留底下载器 v1.6.2 by阿毅i`；注册表、启动器、后台、扩展均为 1.6.2，数据库结构 6、扩展协议 1，媒体与 YouTube 解析工具就绪。
- 覆盖安装前后数据库 SHA-256 和记录数一致：41 条下载计划、3 条网站规则、8 条设置；数据库完整性为 `ok`，无更新备份残留。
- 1.6.2 Windows ZIP、源码 ZIP、安装器、已安装后台和公开资产的精确大小与 SHA-256 统一见 `docs/RELEASE_VERIFICATION_1.6.2.md`。

## 代码与发布位置

- GitHub：<https://github.com/Ly233ly/download_video>
- GitHub Release：`v1.6.2`，附带 Windows ZIP、源码 ZIP 和两份 SHA-256 校验文件；公开资产已通过 GitHub API 和重新下载复核。
- 用户安装包不把项目源码作为外置运行文件暴露；对应源码由公开仓库提供。

## 尚未完成

- Chrome 实际工具栏 popup 的长列表底部点击仍需最终人工复验。
- 稳定自动更新清单仍缺少项目现有 RSA 私钥；不得用新密钥替代。普通 GitHub Release 不受影响。
