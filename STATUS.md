# 当前状态

## 产品

- 当前版本：`1.5.0`
- 平台：Windows 10/11 x64
- 数据库结构：`6`
- 扩展协议：`1`
- 下载引擎：本机 FFmpeg、FFprobe、yt-dlp 和 Deno
- 浏览器扩展：Chrome、Edge、Firefox

## 当前能力

- 浏览器发现并展示媒体候选组，桌面端统一执行直链、HLS、DASH、分离音视频和页面解析下载。
- 下载前展示标题、预览、媒体类型、质量、音轨和预计输出。
- 支持仅下载、导入 Eagle、完成后补导，以及显式选择的“Eagle 成功后删除程序副本”。
- IDM 完成事件快速入队；无来源文件仍可导入，有可靠来源时才写入 Eagle。
- 微信视频号捕获支持当前质量、其他实际质量和有效凭据下的原始视频。
- 视频号捕获支持 Windows HTTP/Mixed 系统代理和 PAC/WPAD 上游，并在停止、退出、升级和卸载时恢复程序拥有的代理设置。
- 桌面和扩展使用统一任务状态；未完成阶段不会提前显示 100%。

## 本轮修复

- 原始视频请求只保留 `encfilekey` 和 `token`，不再携带会触发转码档的上下文参数。用户已确认原始画质问题解决。
- 视频号本机捕获代理可以继续使用 VPN/PAC 上游；预检失败不会改写当前 Windows 代理。
- 扩展候选重绘后恢复并钳制原滚动位置，点击底部候选不再自动回到顶部。
- 用户 ZIP 只保留单文件安装器、说明、许可、二进制清单和源码获取说明；运行载荷嵌入安装器。

## 验证结果

- Python：251 项通过。
- Node：6 组扩展逻辑测试通过。
- 活动 JavaScript 语法与 Chrome、Firefox 双清单解析通过。
- PyInstaller 冻结运行时通过。
- 隔离安装器的新装、升级、故障回滚和卸载通过。
- 当前电脑覆盖安装与健康检查通过，数据库完整性为 `ok`。
- 发布 ZIP：`download-transfer-station-1.5.0-windows-x64.zip`
- SHA-256：`384cd962cbea6ebfa5032db966f7919d6ad9c3e597d0ac621e2f6476cf411809`

## 代码与发布位置

- GitHub：<https://github.com/Ly233ly/download_video>
- `main` 包含当前源码、媒体工具和通过 Git LFS 保存的 Windows 发布包。
- 用户安装包不把项目源码作为外置运行文件暴露；对应源码由公开仓库提供。

## 尚未完成

- 用户实际 VPN 环境下的视频号候选、下载和停止恢复仍需最终人工复验。
- Chrome 实际工具栏 popup 的长列表底部点击仍需最终人工复验。
- 稳定自动更新仍缺少项目现有 RSA 私钥；不得用新密钥替代。
