# 当前进展

更新时间：2026-07-31

## 当前版本

工作区源码、GitHub 稳定发行版和当前电脑安装版均为 1.4.8。1.4.8 修复 1.4.7 引入的启动后主窗口卡死问题：深色标题栏不再同步处理待定界面回调，避免与动态换行控件的尺寸刷新互相触发。正式包已通过 239 项 Python 回归、隔离安装、覆盖升级、故障回滚、卸载和完整冻结运行时验证；数据库结构保持为 6，扩展协议保持为 1。本机覆盖升级后任务、浏览器配对、设置和数据库记录完整保留，核心文件与发布载荷逐项一致。

## 浏览器页面跟随修复

- 已补齐通用单页跳转生命周期：History API、查询参数和片段地址改变都会作废旧页面异步识别、重置页面发现缓存并分阶段重新扫描，不包含 B 站或其他站点域名分支。
- popup 会在当前标签地址变化后重新读取标签标题、地址、候选和发现状态，自动播放切到下一内容时无需刷新网页。
- 独立窗口不再把创建时的 `tabId` 永久写死；后台只记录最近活动的 HTTP(S) 网页标签，插件窗口自身获得焦点不会覆盖该记录，切换网页标签后独立窗口自动跟随。
- 扩展专项 29 项、全量 Python 225 项、6 组 Node 脚本和双扩展清单解析均已通过。

## 桌面 UI 拖动与重绘性能

- 所有 Canvas 圆角控件改为合并重绘和状态去重，连续拖动/缩放不再为同一控件反复删除并新建图层。
- 动态换行、预览比例、表格列宽和滚动区域的尺寸事件已统一节流；无内容溢出时不显示滚动条。
- 窗口交互期间暂停后台刷新，停止后一次性恢复布局、数据同步和 Windows 客户区重绘。
- 媒体任务卡与视频号候选使用原位增量更新，媒体、视频号和 IDM 详情只在数据实际变化时更新。
- 1070×985 与 900×600 连续缩放截图未再出现文字重影、重复标签或相邻双滚动条；最终全量回归为 236 项通过、2 项按环境设计跳过、20 个子场景通过。正式载荷已覆盖当前安装版。

## 桌面全面性能审计

- 五个页面改为首屏按需创建、空闲时分批预热；设置子页继续按需创建。首屏控件由 402 个降到 223–256 个，媒体/IDM 压力启动分别下降 40.2% 和 53.6%。
- 全局刷新改为只投影当前页，隐藏页面不读取候选或重建列表；媒体顶部计数使用轻量只读汇总，列表读取不再顺带写数据库。
- 每窗一个滚轮路由取代每滚动区顶层绑定，滚轮可从卡片子控件正确路由；高分辨率触控板增量先累积再滚动。图标按需解码，缩略图缓存限制为 64 项。
- 1000 条 IDM 压力下强制刷新 P95 从 197.6ms 降至 12.9ms；200 次刷新后无 Tk 回调积压，连续缩放无残影。窄详情页的标题和操作组会自动换行，避免拥挤。
- 诊断导出、10000 条历史清理和 Eagle 来源更新已移出 Tk 主线程；媒体变更、更新进度、视频号预览和操作结果均使用有界/合并队列与代次校验。视频号操作、后台维护和性能心跳的 after ID 会在退出时统一取消。
- 低噪音性能监测只在回调或 Tk 心跳延迟超过 50ms 时保留最近 256 条证据，诊断导出可带出页面、操作、线程数和关键 Queue 长度；可通过环境变量关闭或调整阈值。
- 最新 1000 条 IDM 场景普通刷新 P50/P95/最大为 10.5/19.4/29.1ms，媒体页为 6.2/7.9/14.4ms；10000 条诊断导出和清理的 Tk 启动回调分别为 0.46ms 和 0.16ms。全量回归为 236 项通过、2 项按环境设计跳过、20 个子场景通过。完整数据见 [桌面软件全面性能审计](docs/PERFORMANCE_AUDIT_2026-07-29.md)。

## 1.4.5 自适应缩放与字体可读性

- 列表清理一致性已完成源码级复核：桌面媒体、IDM、视频号、网站规则和扩展媒体/任务列表均按各自数据语义清理。媒体与 IDM 支持单条“清理记录”，批量按钮仍只清终态；清理保留本机文件、预览和 Eagle 内容。
- 数据库清理改为按外键安全顺序解除计划、job 与来源事件关联，并回收无引用的流、分组和会话；超过 SQLite 单次参数上限的大列表不再清理失败。扩展使用标签页清理代次阻止旧异步识别结果回弹。
- Windows 主窗口已使用 DWM 深色标题栏，与 `#0D0F16` 应用外壳连续；真实窗口截图确认标题、图标和原生最小化/最大化/关闭按钮清晰可见。原生窗口样式位测试确认系统菜单、缩放边框和三项窗口控制均保留。
- 当前源码最终全量自动回归为 236 项通过、2 项按环境设计跳过、20 个子场景通过；扩展活动 JavaScript 语法检查通过。全部修复已进入 v1.4.5 稳定版并覆盖当前电脑。
- 有效 UI 比例取 Windows DPI 与相对 1920×1080 的屏幕像素比例较大值；4K/100% 自动使用 200%，Windows 用户缩放仍优先且不叠乘。
- 正文由 10pt 提升为 11pt，所有中文层级统一使用微软雅黑 UI，强调层级通过真实 `bold` 字重表达；导航、按钮、徽章、行高和弱化文字对比度同步提高。
- IDM 表格按当前列与字体的实际像素宽度加入省略号，完整文件名、来源和错误说明仍在详情区，不以硬裁剪制造列间粘连。
- 197 项 Python、6 组 Node、活动 JavaScript 语法和双清单通过。1120×720 三个数据页与 900×600 全五页完成视觉检查；500/200/200 全刷新约 0.112 秒，控件上限保持 80/12/12。
- `v1.4.5` 稳定发行验证见 [发布验证报告](docs/RELEASE_VERIFICATION_1.4.5.md)。2026-07-29 最终 ZIP 为 155,857,576 字节，SHA-256 为 `c5bd1287d0d52f14fe3476fd114dde088e78a1dd7988884c62f189c80142b13f`；签名 `update.json` 已通过仓库公钥验证，GitHub `latest`、发布资产和当前电脑安装版均已核验为 1.4.5。

## 1.4.4 DPI、性能与任务一致性

- Tk 字体改用正点值并按当前窗口 DPI 设置 `tk scaling`；生产窗口、最小尺寸、主要分栏、行高、侧栏和图标使用统一比例。
- 媒体/视频号卡片投影固定为每页 12 条，IDM 为每页 80 条；压力基准分别降至约 0.08 秒、0.03 秒，三列表初始化约 0.34 秒。
- `MediaCoordinator` 在创建和清理计划后发送进程内变更通知，视频号页面按钮和浏览器提交都立即刷新同一桌面任务事实源；扩展任务页空闲时两秒同步一次。
- 视频号证书预检在后台线程执行；扩展新增认证 `/api/media/clear` 链路和明确确认，复用桌面已有的终态安全清理。
- 194 项 Python、6 组 Node、全部活动 JavaScript 语法、双清单 JSON、900×600 三页视觉检查、冻结运行时和隔离安装器均通过；当前电脑已由 1.4.3 覆盖到 1.4.4，数据库 SHA-256 和字节数保持不变。
- GitHub 可先发布不进入稳定更新通道的 `v1.4.4-rc.1`。本机未保存客户端现有公钥对应的离线私钥，因此不得伪造或改签 `update.json`；稳定 `v1.4.4` 只剩恢复原私钥后生成签名清单并推广这一项发布门。

## 1.4.3 发布收口

1.4.3 在 1.4.2 的更新清单 404 兼容基础上，合并本轮深色桌面 UI、真实功能接入、按钮状态复核和无用代码清理。发行脚本、冻结运行时测试与隔离安装器测试改为从当前源码或载荷读取版本号，不再把 1.4.0 写死到健康门和证据文件。

`download-transfer-station-1.4.3-windows-x64.zip`、同名 SHA-256 文件和经仓库公钥验证的 `update.json` 已随 GitHub `v1.4.3` 正式 Release 发布。客户端已从 `releases/latest/download/update.json` 发现 1.4.3，并完整下载、校验和解压 155,791,883 字节 ZIP；当前电脑也已覆盖到 1.4.3 并通过健康门。最终证据见 [1.4.3 发布验证报告](docs/RELEASE_VERIFICATION_1.4.3.md)。

## 1.4.1 深色桌面界面正式接入

正式入口 `idm_eagle_bridge.main:main` 继续创建真实 `Database`、`ProcessingService` 与 `LocalApiServer`，随后把同一对象传给新版 `MainWindow`。界面的任务列表、详情、停止、重试、补导、视频号捕获、IDM 队列、设置和诊断均沿用真实服务 Seam；确定性视觉夹具只存在于 `tests/visual_ui_fixture.py`，不进入用户运行时。

产品 PNG 与 21 个实际使用的 UI 图标位于 `src/idm_eagle_bridge/assets`，并加入 wheel/package-data 与 PyInstaller `onedir` 数据载荷；39 个未使用状态组合和仓库根 60 个重复副本已清理。真实对象接线专项测试确认数据库计划能投影到新版任务卡和详情。后台 `PrintWindow` 截图见 `docs/visual-qa/dark-redesign/phase-3/production-integration-1120x720.png`，测试过程不监听键盘、不激活窗口。

当前电脑已于 2026-07-28 使用正式 `--update` 覆盖模式替换为本轮 1.4.1 载荷。安装器健康门返回 `version=1.4.1`、schema 6、扩展协议 1、`mediaReady=true`、`youtubeResolverReady=true`、`downloadEngine=desktop_ffmpeg` 和有效视频号 bridge 哈希；启动器、后台、扩展清单及关键 UI 资源与新发行载荷 SHA-256 一致。用户数据库保留，并在覆盖前生成哈希一致的本机备份。

实机发现视频号页初次从隐藏页切入时，Tk 会把分栏位置压为 0，导致捕获控制栏完全不可见。现已改为只在页面显示后恢复当前页分隔位置，并增加“隐藏页面不得被响应式更新压缩”的回归。1338×835 后台截图确认捕获、刷新、清空和候选列表恢复；当前电脑已再次覆盖安装并通过新后台哈希与健康门。

媒体任务的页面标题可能包含来源页面写入的换行和整段说明，旧投影会保留这些强制换行并把状态、大小和时间挤出固定卡片。现有单行投影会先折叠空白，再按卡片/详情宽度显示省略号；完整标题仍保留在详情完整信息与悬停提示。长标题压力截图和回归通过，修复载荷已覆盖当前电脑。

## 1.4.0 统一前端与状态动作

桌面主窗口改为固定左侧导航与顶部健康状态，下载任务和视频号采用主从详情；IDM、浏览器配对、网站规则、网络、更新和脱敏诊断均进入同一窗口。任务动作由状态和最终文件归属共同决定，未完成阶段最多显示 99%，`completed_local/imported` 才显示 100%。托盘菜单文案收敛为显示窗口、网站规则、唤醒处理、检查软件更新和退出。

Chrome/Firefox popup 保留唯一“媒体 / 任务”运行时，以真实产品图标、暖灰表面和紫灰强调色统一品牌；候选检查器新增安全输出名规范化、折叠技术地址、当前页标题/域名和任务处理大小/时间/来源动作。扩展仍不包含浏览器下载器、FFmpeg/WASM、第三方路由或旧工具箱。

EXT/DESK/CORE 迁移矩阵已写入 `docs/FRONTEND_REFACTOR_FEATURE_INVENTORY.md`。确定性 Tk 与浏览器夹具覆盖真实控件、候选、计划和任务状态；1120×720、900×600 与 660×574 截图已和参考图同画布比较。当前 173 项 Python unittest、6 组 Node、全部活动 JavaScript 语法和双清单解析通过；视觉 QA 无 P0/P1 遗留。

源码提交 `615fa94` 已推送到 `main`，发行 ZIP 与 SHA-256 文件已上传至 GitHub `v1.4.0-rc.1` prerelease。稳定更新通道的签名 `update.json` 未修改；发行证据见 `docs/RELEASE_VERIFICATION_1.4.0.md`。

## 1.3.1 浏览器性能、桌面性能与 Eagle 导入后清理

已完成 `ltaoo/wx_channels_download` 最新源码的行为研究与架构索引，确认其核心链路是微信桌面客户端、受信任 HTTPS 代理、页面 JavaScript 注入、feed/媒体捕获和本机下载。1.3.1 不复制其 Go/JavaScript、SunnyNet、Gopeed、证书、图标或 UI，而以 Python 3.11+ 标准库和本项目自有 JavaScript 独立实现，并接入现有 `MediaCoordinator`、SQLite 任务、FFmpeg、Eagle 和桌面任务页。

完整计划位于 `docs/WECHAT_CHANNELS_PLAN.md`，任务 96–103 与 A197–A220 已建立。Python 标准库已实现每机 RSA 根证书、回环 HTTPS 代理、完整 WinINET 快照/精确恢复、页面 bridge、`objectId` 候选归组、封面预览、任意实际质量选择、ISAAC-64 前缀流式解密、统一任务进度、FFmpeg/ffprobe 和 Eagle 交付。代理会串接或绕过开启前已有系统代理，非目标 CONNECT 与普通 HTTP 透明转发，连接数、头、正文、压缩响应和超时均有上限。

按最新交互要求，页面 bridge 现在直接把自有“下载”按钮加入微信首页推荐和详情页的原操作栏，找不到操作栏时使用悬浮后备；普通点击提交当前播放质量，悬停菜单选择其他实际质量。内部 finder 插桩同时传递方法身份，页面优先用 URL objectId、媒体主机+路径和唯一同内容标题绑定当前候选；无法唯一确认时拒绝创建。页面只传本次会话已登记的 objectId/variantId，计划仍由 `MediaCoordinator` 创建并默认下载后导入 Eagle。当前 172 项 Python、6 组 Node、17 个活动 JavaScript 语法与双清单解析已通过，仍需真实微信界面和最终媒体终验。

首次真实微信运行已完成根证书确认、页面 TLS/脚本注入和正常停止后的原系统代理精确恢复，同时复现“页面显示已注入但候选为 0”。现场与上游行为研究证明完整 feed 只在 `res.wx.qq.com` 资源模块内部 `finder*` API 返回对象出现；现已扩展明确资源主机 TLS、原根证书下叶证书配置轮换、页面/ES 模块会话缓存键、六类内部返回观察和无秘密整数诊断，未复制外部项目代码或下载器。

当前 172 项 Python unittest、6 组 Node、17 个活动 JavaScript 语法、双清单和版本同步门禁通过；新增回归证明内部方法改写前后返回对象与方法身份一致、页面提交所选实际 variant、未知 variant 被服务端拒绝。纵向证据会现场生成 MP4、加密、HTTP 下载、解密、FFmpeg 封装并以 ffprobe 校验 320×240，最终状态为 `completed_local`。数据库秘密扫描证明签名 URL、Cookie 和 decodeKey 不落盘；卡片/富文本标题、动态质量、签名查询保真、候选洪泛上限以及系统代理优先恢复顺序已有回归证据。主窗口现已加入可见纵向滚动，最低高度下仍可到达标签详情、操作区和底栏；媒体、视频号、IDM 与网站规则四类列表均可刷新，并按各自语义清理终态记录、候选或规则。

schema 6 已加入显式“Eagle 成功后删除本机下载文件”：扩展、批量、补导、视频号桌面勾选和微信页面入口都显示删除说明，新计划的导入动作写入 `delete_after_import=1`，旧计划迁移固定为 0。覆盖安装不能替 Chrome 重启旧 service worker，因此桌面也把旧扩展明确发送的 `importToEagle=1` 视为同一清理选择；“仅下载”或显式关闭仍写 0。处理器只在 Eagle 官方 API 成功后删除 plan/job 同路径且位于本程序“已完成”目录的最终文件，随后清空 `final_path`；IDM/用户文件、未勾选、仅下载、重复、等待、失败和目录外文件均保留。文件锁定时 Eagle 导入仍保持成功，任务记录可见清理错误和原路径；若成功导入后恰好异常退出，下一次后台循环会恢复尚未执行且没有安全错误的清理。安装器健康门已同步到 schema 6，并通过 C# 编译检查。

桌面刷新性能已全面收敛：最长 4 秒的 Eagle HTTP 健康探测移出 Tk 主线程并以单飞后台 Module 执行；四类表格共用增量投影 Seam，只修改变化行；预览按文件身份复用 PhotoImage。仪表盘三个 SQLite 视图合并为一个 Interface，系统代理状态缓存 5 秒且设置变更立即失效，视频号证书/桥脚本身份缓存 60 秒，候选计数不再构造完整视图。500 行 Tk 表格在无变化时 30 次刷新由约 66 毫秒降为约 33 毫秒；相关 6 项新增回归和全量门禁均通过。

浏览器大量媒体性能回归已修复：400 条候选的旧角标路径逐条全量归组累计约 4.45 秒，新路径按标签页只排队一次、约 30 毫秒；popup 400 行夹具的侧栏远程视频从 400 个降为 0 个，只保留当前检查区 1 个，首屏由约 101 毫秒降为约 68 毫秒。候选快照现在先于发现恢复与连接检查发出，侧栏封面使用延迟加载和离屏渲染跳过。1.3.1 的 172 项 Python、6 组 Node、17 个活动 JavaScript 语法、双清单、冻结运行时、冻结接收、隔离安装器全新安装/升级/故障回滚/卸载和本机覆盖安装均已通过；在线健康返回 1.3.1、schema 6、扩展协议 1、媒体与 YouTube 工具就绪，更新备份已在健康门通过后清除。真实微信中的当前/其他质量下载、Eagle 与最终画面核对尚未完成，因此 A219–A220 仍保持未完成，1.3.1 只能作为预发布候选。

## 1.2.11 自动跟随系统代理

根因不是接收者缺少 FFmpeg 或运行库，而是浏览器与桌面下载进程使用了不同出口：Chrome 由系统代理或浏览器代理接管，FFmpeg/yt-dlp 未显式配置代理时仍直连。1.2.11 在每个任务开始时读取 Windows 当前用户系统代理，并把所选线路统一传给直链、HLS/DASH、YouTube/通用页面解析和字幕下载；本机 Eagle 与助手接口永远绕过代理。

主界面新增网络设置，支持自动、直连和手动 HTTP/Mixed 端口。自动模式检测到系统代理时优先使用，网络失败只切换直连一次；手动模式不会静默绕过。403、超时与连接失败会给出代理操作提示，诊断格式升级为 3，只记录脱敏代理状态。113 项 Python、17 个扩展 JavaScript、双清单、冻结运行时和隔离安装器四路径全部通过。最终 ZIP 为 155,234,592 字节，SHA-256 `1d60e32d520693e9e0981d2eb464b850b4cce5efcd3a3d61fd33cacb4ba4c6ea`；源码、安装包与签名更新清单已随 GitHub v1.2.11 公开发布，详情见 `docs/RELEASE_VERIFICATION_1.2.11.md`。

## 1.2.10 多视频页面候选恢复

回归根因是 1.2.6 的展示分区把“同一个标签页和来源域名”当成内容身份：只要出现一个 `resolver=page`，同域的其他完整视频、音频、HLS 和 DASH 就可能被降为未关联技术资源。多视频作品页、推荐流和瀑布流因此只能看到少量候选。

1.2.10 删除这条宽泛隐藏规则。完整媒体必须保留，只有已确认的传输分片默认隐藏；`blob:`/MSE 视频从自身向外逐层寻找最近的稳定作品链接，主视频保留当前作品页，相关视频分别绑定自己的内容页。实现只判断内容身份和 DOM 邻近关系，没有 Pinterest 或其他站点域名分支。

105 项 Python、5 组 Node、全部扩展 JavaScript、双清单、冻结运行时和隔离安装器四路径全部通过；当前电脑已覆盖安装，在线健康与安装目录扩展清单均为 1.2.10。最终 ZIP 为 155,219,249 字节，SHA-256 `2013cc2ad74c9c6cc286d74393500a5694a984ab133c29d9d5c4117ad31507dc`，源码副本不含 Python 缓存。详情见 `docs/RELEASE_VERIFICATION_1.2.10.md`；只剩用户在 Chrome 重载扩展后确认多视频列表。

## 1.2.9 结构化视频页面发现

现场 Vimeo `https://vimeo.com/746646949?fl=pl&fe=vl` 正常播放时，扩展捕获了 11 条播放器请求，但全部进入未关联技术区。桌面固定 yt-dlp 能解析该作品从 240p 到 4K 的完整格式，证明故障在浏览器内容身份发现而非下载器。

页面公开 `og:type=video.other`、`twitter:card=player`、`og:url=https://vimeo.com/746646949` 和带视频 ID 的播放器地址；旧永久链接规则只接受 `/video/<id>`、`/pin/<id>` 等结构，不接受根路径数字 ID，且播放器内部结构化清单没有成功送达。1.2.9 新增通用结构化视频元数据入口，只接受明确视频声明与非首页的同源规范地址，不包含 Vimeo 域名分支。最终测试和发行结果见 `docs/RELEASE_VERIFICATION_1.2.9.md`。

105 项 Python、候选 Node 回归、全部扩展 JavaScript、双清单、冻结运行时和隔离安装器四路径全部通过；本机 yt-dlp 实测列出该作品 240p–4K 格式。当前电脑已覆盖安装，在线健康和安装目录扩展清单均为 1.2.9。最终 ZIP 为 165,422,592 字节，SHA-256 `4d924791c23dc34683ff8317fcd53553c625b79d3c90ce0c6cd92d29dccbc671`；只剩用户重载 Chrome 扩展后确认现场内容卡片与创建任务。

## 1.2.8 通用页面分轨格式兼容

现场 Pinterest Pin `72690981480504869` 只提供五档 H.264 HLS 视频轨与一条扩展名为 MP4 的 AAC 音轨。1.2.7 的固定选择器只接受 M4A 独立音轨，且页面没有合并好的 MP4，因而在下载前零字节失败。1.2.8 保留 MP4+M4A 优先级，增加 MP4+MP4 及逐级通用回退，不包含 Pinterest 域名分支。

本机真实解析已选中 720×900 H.264 视频和 AAC 音频，FFmpeg streamcopy 得到 12.194 秒、2,464,356 字节的有效 MP4。105 项 Python、5 组 Node、17 个扩展 JavaScript、双清单、冻结运行时和隔离安装器四路径全部通过；当前电脑已覆盖安装，在线健康为 1.2.8，安装目录扩展清单也为 1.2.8。最终 ZIP 为 165,416,683 字节，SHA-256 `2447b5c60216099954d2c2cda9fe7de81f3f4e5418d0d6d9fc55f86b3ef730d5`。详情见 `docs/RELEASE_VERIFICATION_1.2.8.md`。

## 1.2.7 通用信息流发现生命周期

现场正在播放的视频具有独立永久链接 `x.com/xilo2991/status/2078995639690723495`、两行正文、poster 和约 22 秒时长；纯候选算法能正确得到永久链接和标题，但 popup 只显示网络清单，证明漏检发生在发现生命周期而不是站点元数据解析。

根因有两个通用条件：全页 `MutationObserver` 使用尾随防抖，持续变化会不断清除 250 ms 定时器；扩展重载后，保持打开的网页不会自动获得新内容脚本，而新 service worker 仍能捕获网络请求，于是出现很多清单、没有内容卡片。1.2.7 改为不可饿死的合并调度，并让 popup 主动探测当前标签页；接收端不存在时仅向该 HTTP(S) 标签重新注入两个发现脚本并立即扫描。实现与测试不包含 X 域名判断。

105 项 Python、5 组 Node、17 个扩展 JavaScript、双清单、冻结运行时和隔离安装器四路径全部通过；当前电脑已覆盖安装，在线健康为 1.2.7，六个相关扩展文件与源码哈希一致。最终 ZIP 为 164,410,836 字节，SHA-256 `7fe6aebce831a3f074f55f68004803d2806edb43b2f8c22f91d4128ab489affe`。A180 只剩用户在 Chrome 保护页确认扩展重载后，对保持打开的现场页面执行 popup 自动恢复点验。详情见 `docs/RELEASE_VERIFICATION_1.2.7.md`。

## 1.2.6 通用信息流内容绑定

现场 X 首页 DOM 只有 4 个视频播放器、其中 2 个处于可视区域，但旧 popup 显示 18 个媒体组并另藏 42 个播放分片。每个真实播放器都能从最近 `article` 取得独立状态永久链接、正文、poster 和时长；候选爆炸来自 blob 与 HLS 网络地址无法直接相等，孤立清单各自成组并用标签页标题回退。

1.2.6 以通用内容容器为身份边界：稳定永久链接形成 `resolver=page` 内容候选，标题从语义说明和可读正文中选择并排除计数、时间轴和控件噪声。同标签页/来源域已有这些可信候选时，无法绑定具体内容的 HLS/DASH/音视频请求默认隐藏为不可提交技术项；没有可信替代入口的网站仍保留唯一清单。工具栏角标改用与 popup 相同的分区结果。

105 项 Python、5 组 Node、17 个扩展 JavaScript、双清单、冻结运行时和隔离安装器的全新安装/更新/故障回滚/卸载全部通过；当前电脑已覆盖安装，在线健康为 1.2.6，五个本次修改的扩展文件与源码哈希一致。最终 ZIP 为 164,401,138 字节，SHA-256 `2e2672e9da3b217985c2259622be50c0587f86bed1d5fc6efb7a61d6114d0ccd`。Chrome 安全边界不允许自动操作扩展管理页，A176 仅保留用户手动重载后对现场 popup 数量、卡片身份与技术区的最终点验。详情见 `docs/RELEASE_VERIFICATION_1.2.6.md`。

## 1.2.5 抖音逐视频标题

现场当前 `feed-item` 为 `7662692425235828009 / @热话动漫 / 这一集的瑞克…`，下一条为 `7653805516250025262 / @木板解说 / 第7集：深度拆解…`。两项 DOM 信息完整；同名问题来自 blob/MSE 原始网络请求无法与播放器 `currentSrc` 精确匹配后回退 `webInfo.title`。

1.2.5 为每个已加载 `feed-item` 生成独立 `douyin:<videoId>` 候选，标题只来自自己的作者和文案。无法绑定某个视频 ID 的底层请求标记为技术分片并默认隐藏，既不抢占内容列表，也不可提交。105 项 Python、5 组 Node、17 个扩展 JavaScript、双清单、冻结运行时、隔离安装器和当前电脑覆盖安装全部通过；在线健康为 1.2.5，四个修改的扩展核心文件与源码哈希一致。最终 ZIP 为 164,388,754 字节，SHA-256 `8eba9f8ad0c080a76077c2909213210a31c256035c63a96b52140692a5ecc984`。现场页面重载后仍确认两个独立视频 ID、文案和时长；工具栏 popup 的最终肉眼下载一致性保留给用户点验。详情见 `docs/RELEASE_VERIFICATION_1.2.5.md`。

## 1.2.4 抖音当前主视频适配

现场证据显示同一精选页同时保留 223.237 秒的视口内 HTTP 主播放器和 49.041 秒的屏外 blob 预加载播放器。旧通用逻辑跳过有 HTTP 地址的播放器、只为 blob 建页面候选，因此既选错 0:49，又把 yt-dlp 不支持的 `/jingxuan` 导航页交给桌面；任务错误中的 `[媒体地址]` 只是安全脱敏，不是实际占位符。

1.2.4 按视口、播放状态、进度和面积选择唯一主播放器；明确 `modal_id` 规范为 `/video/<id>`，直接流和页面候选统一用 `douyin:<videoId>`。当前主播放器有 HTTP 流时直接下载，没有时才走固定 yt-dlp/Deno 页面解析；旧无内容 ID 的 `/jingxuan` 解析候选会从列表移除。自动回归、发行构建、安装器和现场 3:43 下载闭环的最新结果记录在 `docs/RELEASE_VERIFICATION_1.2.4.md`。

## 1.2.3 通用页面解析与 Instagram 分片修复

现场 Instagram 页面共观测到 121 个视频请求，地址使用 `bytestart/byteend`。失败任务的 172,979 字节与 `886–173864` 区间完全相等，证明旧版把单个播放分片当作完整 MP4；去掉这两个传输参数并保留其他签名参数后，同一地址可按标准 Range 返回文件数据。

1.2.3 在扩展和桌面增加双层通用区间门禁；Instagram 元数据适配只负责按资产/轨道归组和估算完整大小。blob 播放器使用视频附近的同源稳定内容链接形成 `resolver=page` 候选，附真实当前帧、标题、时长和尺寸；链接规范化回归覆盖 Instagram、TikTok、抖音、X、Reddit、Facebook Watch 与 Pinterest。桌面解析器只接受公网 HTTP(S) 页面，Cookie 最多 16 KiB、只进任务内存和任务专用临时 cookie 文件，解析结束必删。存在页面解析候选时，重建分片地址只在技术分片开关中出现，默认列表不再堆叠小文件假视频。

102 项全量回归、4 组 Node 逻辑、全部扩展 JavaScript、双清单、冻结运行时和隔离安装器均通过；当前电脑已覆盖安装且在线健康门全绿。最终 ZIP 为 164,398,184 字节，SHA-256 `a0b038d7763fe087e59e908845182cb59fe39b5453fa0d8692f8a6c6ec0e8d9d`。Windows Computer Use 因无法高置信确认受保护 Chrome 扩展页 URL 而安全停止，因此 A163 只剩用户手动重载扩展后的 Instagram 现场下载一致性确认。

## 1.2.2 YouTube SABR 全画质

现场响应已确认包含 56 条 `adaptiveFormats`，覆盖 1440p、1080p、720p、480p、360p、240p、144p，但这些条目没有 `url` 或 `signatureCipher`，只有一个 360p progressive 条目带直链，并另给 `serverAbrStreamingUrl`。因此 1.2.1 只能安全显示 360p，问题不在 UI 筛选或去重。

1.2.2 把无直链的 SABR 元数据目录建模为 `resolver=youtube` 候选，popup 显示当前视频的全部唯一高度并默认选最高档。提交后桌面使用 yt-dlp 2026.06.09 + Deno 2.8.1 解析准确高度与最佳音轨，再把瞬时直链交给 FFmpeg。YouTube 请求 Cookie 只按当前标签页在扩展会话内暂存；桌面解析时生成任务归属 cookie 文件，解析完成或失败都立即删除，数据库、命令行、任务错误和诊断不含 Cookie。

97 项完整回归、冻结运行时、隔离安装器的全新安装/更新/故障回滚/卸载以及当前电脑覆盖安装均通过；在线 `/health` 为 1.2.2、`mediaReady=true`、`youtubeResolverReady=true`。最终 ZIP 为 164,373,275 字节，SHA-256 `7be6eed0ace15cced6d05b28ddf75cfe189a7d9ad3e9e6e73d56dc98c2a81864`。Chrome 受保护扩展页只能人工点击重载，因此只剩现场全画质目录与真实 1440p 下载闭环。

## 1.2.1 YouTube 结构化捕获

根因已复现：YouTube 使用 Media Source Extensions，DOM `video.currentSrc` 是页面同源占位地址，`webRequest` 只稳定暴露小型 `/videoplayback?...range=...` 探测，因而旧版显示四条约 7 KB 音频，无法形成 IDM 可见的 1440p–144p 目录。

1.2.1 新增独立 YouTube 主世界适配器，读取初始播放器响应并监听 `/youtubei/v1/player`，将带直接 URL 的 `adaptiveFormats` 按 videoId 归组、按 itag 区分，附带标题、缩略图、时长、分辨率、FPS、编码、码率和预计大小。通用归组同时修正同一路径不同 itag 被折叠、显式 Range 使用总文件大小时未判为分片，以及无组身份的小于 128 KB 音视频探测未隐藏的问题。95 项全量测试、JS/双清单、冻结运行时、隔离安装器、最终发行构建和当前电脑覆盖安装均通过；因 Chrome 禁止自动操作内部扩展页，只待用户点击一次“重新加载”后的现场页面确认。

## 1.2.0 职责收口与补导

修复前回归分别证明：候选仍按最新优先导致打开停在顶部、`segmentOnly` 当普通大卡展示、`MediaCoordinator` 没有本地完成文件补导 Interface、扩展仍装载下载/解析/录制/在线工具和旧图资源。修复后 `partitionGroups/defaultActiveGroupId`、`import_completed_plan`、`/api/media/import` 与覆盖安装清理清单均有自动门禁。

活动扩展从历史 96 文件工具箱收敛为必要文件，只保留普通/增强发现、B 站与 YouTube 结构化适配、内容预览、媒体候选组、认证提交和状态 UI。删除项被放入本机临时隔离目录以便本次工作恢复，未触碰用户下载、数据目录或 Eagle。

## 1.1.12 固定 Range 分片门禁

扩展通用解析 URL 查询参数、路径明文和 base64url 路径段中的 `range/bytes=start-end`；只有区间长度与已知候选大小一致时才判定固定分片。同组有 HLS/DASH 时分片不进入版本选择器，只有分片时组状态为 `segmentOnly`，计划创建前返回 `segment_only`。桌面 `MediaCoordinator` 同样在建库和重试执行前拒绝这类 URL，错误码为 `fixed_range_fragment`，避免旧扩展绕过。

修复前真实数据库稳定记录两条 `desktop_download_failed/2%`，定向 Node 与 Python 回归也分别失败；实现后扩展阻断和本机拒绝均通过。92 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器四路径和当前电脑覆盖安装均通过；在线 `/health` 为 1.1.12，安装目录关键扩展文件与三个可执行文件和发行包哈希一致。ZIP 为 93,415,850 字节，SHA-256 `298e46add8bd8699c4f4302c2f5f23073f0b6a04bf985a362aed0c385e7df5b3`。剩余 A144 为 Chrome 重载后的 Behance 现场闭环。

## 1.1.11 CDN 备用链接归并

归并仅限同一标签页和来源网站、同媒体类型，并要求响应 MD5/Digest/强 ETag 相同，或 URL 长路径相同且总字节数完全一致。文件大小本身不能作为归并依据；相同大小但路径/响应身份不同的媒体继续分开。显式播放器组与无元数据备用请求只有在强别名唯一指向该组时才合并，歧义时保持隔离。

修复前回归稳定复现为 2 个内容组；实现后变为 1 个内容组、1 个可下载版本，真实标题和预览随更丰富的播放器记录保留。不同路径的同大小媒体仍为 2 组，响应指纹相同但 CDN 路径不同的地址也能安全归并。91 项全量测试、冻结运行时、隔离安装器四路径和当前电脑覆盖安装已通过；在线 `/health` 为 1.1.11，安装目录关键扩展文件与三个可执行文件哈希一致。ZIP 为 93,404,556 字节，SHA-256 `29799b06ae5c695dfc8b08ea548da0b79b4d837b3d291900c64390d94181c39b`。剩余 A142 为 Chrome popup 肉眼确认和真实下载闭环。

## 1.1.10 候选自身直链预览

当前抖音页面现场只有 1 个可见 `<video>`，网络层却捕获 4 个媒体直链；因此 1.1.9 的精确匹配策略只能安全绑定 1 张页面播放器帧。新增 `previewMediaUrl(group, selection)` 只返回当前选择中的 `http/https` 视频 URL；popup 无静态帧时以 `muted + playsinline + preload=metadata` 的视频元素解码该 URL，并在元数据到达后定位到开头代表帧。切换候选版本会同步更换预览源，加载错误才显示占位图。

修复前 Node 回归因缺少 `previewMediaUrl` 失败；实现后候选自身 URL、版本切换和音频排除均通过。91 项全量 unittest、全部扩展 JavaScript 语法和双清单已通过。1.1.10 冻结运行时、隔离安装器的新装/升级/故障回滚/卸载和当前电脑覆盖安装均通过；在线 `/health` 为 1.1.10、协议 1、schema 5、`desktop_ffmpeg` 且媒体工具就绪，安装目录三份预览修复文件及三个可执行文件哈希一致。最终 ZIP 为 93,394,951 字节，SHA-256 `b4def98ef97ec47d5406503dbec3a19c09132c128520b92d174eaa9eb366d8b4`。剩余 A140 为 Chrome 手动重载后的多候选画面与下载内容闭环。

## 1.1.9 媒体身份一致性

现场数据库记录所选流 `duration=2713.034`，最终文件 FFprobe 实测 `708.141475`；本机预览首帧与 popup 所见内容不同，证明错配在创建方案前发生。根因是内容脚本在请求 URL 无法匹配任何 `<video>` 来源时，仍把当前播放/可见播放器作为回退，把它的帧、时长和稳定组键贴到后台预加载请求。

新增 `resolveVisualMatch` 以“同主机 + 同路径、忽略签名查询”匹配请求与播放器；未匹配请求返回 `none`，后台禁止复制其视觉身份。桌面 `MediaCoordinator` 对非清单直链比较声明时长与 FFprobe 输出时长，超过 8% 或 3 秒容差时以 `output_duration_mismatch` 失败并删除程序临时输出。回归在修复前分别因缺少匹配函数、旧回退逻辑和错误文件被正常交付而失败，修复后通过。

91 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器的新装/升级/故障回滚/卸载和当前电脑覆盖安装均已通过。当前 `/health` 为 1.1.9、协议 1、schema 5、`desktop_ffmpeg` 且媒体工具就绪；安装目录三个身份修复扩展文件和三个可执行文件与源码/发行包哈希一致。最终 ZIP 为 93,384,445 字节，SHA-256 `609e2dd3673f8a454d3106ac597368825145f8a36f7d982471c1809cce0fe969`。Chrome 安全策略禁止自动进入扩展内部页，仍需用户手动重载后完成 A138 的真实同内容下载。

## 1.1.8 认证读取传输兼容（实机内容闭环未通过）

旧现场可稳定复现为：相同令牌携带正确扩展 Origin 的 GET 返回 200，缺少 Origin 的 GET 返回 401；Chrome 实机的配对 POST 成功后马上触发 401。新增回归先证明认证 POST 媒体健康接口为 404、扩展仍调用 GET，实现后健康和任务列表 POST 均通过。创建、停止、重试和打开目录原本已经使用同一 POST 认证语义。

90 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器的新装/升级/回滚/卸载和当前电脑覆盖安装均已通过。当前安装的认证 POST 健康接口返回 200，五个扩展关键文件与源码哈希一致；真实抖音任务虽完成为 `imported/100`，但内容身份错误，A136 未通过，由 1.1.9 接替。1.1.8 ZIP 为 93,372,384 字节，SHA-256 `d0d1ae902a63e1a4155179561a87d2e074586627158f0be3ec16cdf0aac15571`。

## 1.1.7 状态写入原子性（实机未通过）

配对令牌、`pendingEvents`、`lastPlanId` 和 `lastPlanStatus` 不再由互相独立的 `get → merge → set` 操作更新。`createStateUpdateQueue` 保证每次操作读取前一个操作提交后的最新快照；函数式更新让来源事件追加/消费和令牌条件清除在同一临界区计算。并发 Node 回归先因缺少队列失败，实现后稳定通过。

89 项全量测试、扩展语法与双清单、冻结运行时、隔离安装器的新装/升级/回滚/卸载和当前电脑覆盖安装均已通过；真实 Chrome 配对仍因受认证 GET 401 失败，A134 未通过，由 1.1.8 接替。1.1.7 ZIP 为 93,369,775 字节，SHA-256 `b4c8bbbca54e192889f7e95a014915abf70e5a0c096ef7d036de1845d35a1bbd`。

## 1.1.6 配对竞态与持久连接（实机未通过）

当前 Chrome `Default` 配置加载仓库开发副本，扩展 ID 为 `phochakdmmmehfamipieafeiigencilf`。扩展存储最近记录呈现 43 字符令牌与空令牌交替；最后一个非空令牌的 SHA-256 与服务端当前 `extension_token_hash` 完全一致，服务端 Origin 也与该扩展 ID 一致，证伪了令牌内容和 Origin 不匹配。真正断点是配对前请求的迟到 401 覆盖了新状态。

`EagleBridgeAuthLogic.unauthorizedAction` 已独立覆盖无令牌旧请求、旧令牌请求、当前令牌确实失效三类情况。设置页保存令牌后调用受认证健康接口并再次读取认证状态。真实 Chrome 重载后仍发现来源/任务状态的旧写入可覆盖令牌，因此 1.1.6 不通过 A132，由 1.1.7 接替。

## 1.1.5 动作真实性与配对状态

当前 Chrome `Default` 配置确认加载路径为 `<仓库>\chrome-extension`，扩展 ID 与桌面记录的配对 Origin 一致；仓库 `bootstrap.js` 为空是防止本机密钥进入源码/发行源的预期安全行为。正式安装目录副本的 64 字符一次性凭据与本机恢复文件哈希匹配且未过期。点击故障发生后 SQLite `download_plans` 仍只有 8 条，最新仍是旧的 `completed_local/100` Tmall 任务，证明请求在写入计划前已被前端拦截。

代码已增加可独立测试的 `startValidatedTask` 边界：无效校验不会调用计划创建器，成功提示依赖返回的计划对象；单项/批量“仅下载”在未配对时禁用。service worker 新增显式 `autoPair` 消息，popup 每次连接检查都会先尝试恢复，再决定显示“需要配对”。完整构建、安装与 Chrome 实机下载验收见 [1.1.5 验证报告](docs/RELEASE_VERIFICATION_1.1.5.md)。

88 项全量回归、全部扩展 JavaScript/双清单、冻结运行时、隔离全新安装/升级/故障回滚/卸载和当前电脑覆盖安装均通过。当前 `/health` 为 1.1.5、schema 5、协议 1、`desktop_ffmpeg` 且媒体工具就绪；源码与安装目录四个关键扩展文件哈希一致。Chrome 扩展卡已从 1.1.4 重载为 1.1.5。最终 ZIP 大小与 SHA-256 记录在独立验证报告中。Chrome 自动化安全策略拒绝直接进入扩展内部 popup，因此 A130 的最后一次配对点击与真实 `planId` 对照仍需用户在已打开的扩展 UI 中完成。

## 1.1.4 任务同步与通用预览状态

已确认 Tmall 示例页面暴露标准可见 `<video>`，桌面数据库中的同一任务为 `completed_local/100` 且已有真实 `preview_path`，原故障来自 popup 吞掉同步错误和未把瞬时/本机帧绑定到任务。新版在认证失效时自动恢复配对，任务轮询成功会恢复连接灯；失败时保留最后状态但加同步中断提示。任务创建时立即绑定当前视频帧，重开 popup 后通过认证回环接口读取本程序 `下载中转站/预览` 内的 PNG；输出目录同样只按任务 ID 打开已校验的 `已完成` 父目录。

88 项全量测试、冻结运行时、隔离全新安装/升级/故障回滚/卸载和当前电脑覆盖安装已通过。成功升级已实测轮换新的单次恢复凭据。当前后台 `/health` 与安装目录扩展清单均为 1.1.4，四个任务同步/UI 修复文件和源码 SHA-256 一致；旧 Tmall 任务仍为 `completed_local/100` 且保留最终路径与预览路径。最终 ZIP 大小与 SHA-256 见 [1.1.4 验证报告](docs/RELEASE_VERIFICATION_1.1.4.md)。

## 1.1.3 动态质量目录状态

已用 8 档 Vimeo、8 档通用 HLS、3 档通用 DASH 和 8 档桌面 ffprobe 节目映射覆盖“任意数量、按当前视频决定”的路径。清单无法读取或未声明分辨率时不生成猜测档位，继续显示自动质量并由本机安全选择。84 项全量测试、冻结构建、隔离安装器、当前电脑覆盖安装和发行哈希均已通过；仅 Chrome 工具栏 popup 同视口截图待人工点验。

## 1.1.2 质量与清单状态

Behance/Vimeo 实页确认播放器公开 `1080p、720p、540p、360p、240p` 五档 DASH 质量，同时网络层产生 21 个交错的 MP4 分片。新版从播放器结构化配置提取完整 HLS 主清单与质量目录；同组存在清单时隐藏非显式 MP4/音频分片，质量选择器只列五个可用档位并标记最高档为推荐。

当前源码 84 项自动测试通过，新增覆盖播放器配置解析、清单替换 21 个分片、质量排序/默认值及本机节目流选择。1.1.2 冻结构建、隔离安装器、当前电脑覆盖安装、发行哈希和 Behance 实页结构化质量目录均已通过；仅 Chrome 工具栏 popup 同视口视觉截图待人工点验，详见独立验证报告。

## 1.1.1 UI 与候选状态

任务 18–21 在 1.1.1 曾保留深搜、缓存、移动 UA、三类录制、疑似密钥和媒体控制；这些历史入口已由 1.2.0 任务 51 明确退役。稳定播放器归组、分片收敛与真实帧能力继续保留。

桌面软件的“媒体下载”和“IDM 导入记录”双标签、schema 5 与 `desktop_ffmpeg` 状态机不变。当前源码 84 项自动测试、真实普通直链、Referer/User-Agent 分轨、AES-128 HLS、字幕、仅下载、schema 迁移、补丁升级自重载、全部 JavaScript 语法和 Chrome/Firefox 清单检查通过。

1.1.1 源码与冻结健康门均为 `version=1.1.1`、`databaseSchema=5`、`extensionProtocol=1`、`downloadEngine=desktop_ffmpeg`、`mediaReady=true`。83 项测试、冻结载荷、当前电脑覆盖安装、废弃脚本清理和发行哈希已通过；仅 Chrome 工具栏 popup 最终视觉对照仍待人工点验，详见本轮验证报告。

## 1.0.0 集成状态

任务 01–14 的 1.0.0 历史能力已实现；浏览器/在线下载路线由 1.1.0 本机统一下载取代，缓存/录制/移动 UA/密钥/媒体控制又由 1.2.0 退役。捕获、HLS/DASH 发现、B 站分轨归组、安全边界、许可材料和安装回滚继续保留。

68 项自动测试通过；真实 FFmpeg AES-128 HLS 和分轨音视频合并测试通过；Apple 公共 HLS 与 B 站公共非 DRM DASH 均生成并探测出音频+视频样本。隔离安装、0.6.0 升级、故障回滚、卸载和冻结运行时验证纳入发行脚本。完整证据见 [1.0.0 验证报告](docs/RELEASE_VERIFICATION_1.0.0.md)。

1.0.0 当时未完成的缓存/录制肉眼复验不再构成 1.2.0 发布门；当前人工范围改为分片默认隐藏、最新项定位和本地完成文件补导。正式 GitHub Release 上传仍需项目所有者签字。

## 已完成并验证

### 1.4.1 全仓无用代码与资源清理

已对 Python、活动 JavaScript/CSS、视频号 bridge、本机代理、C# 入口、打包入口和资源引用做全仓静态复核。删除旧独立设置弹窗、无调用接口/包装函数、视频号旧远程命令轮询与动态执行、前端死函数/选择器，以及 60 个重复和 39 个未使用图标；兼容迁移、安装清理、安全门禁、第三方许可与测试夹具均按其真实用途保留。完整记录见 [全仓无用代码与资源清理审计](docs/CODE_CLEANUP_AUDIT_2026-07-28.md)。

187 项 Python、6 组 Node、17 个活动扩展 JavaScript、视频号 bridge、两份清单和冻结发行构建通过。当前电脑已静默覆盖为清理后的 1.4.1；后台哈希与发行包一致，安装载荷只含 21 个实际使用图标，健康状态为 schema 6、`mediaReady=true`、`desktop_ffmpeg`。本轮 ZIP SHA-256 为 `88795dfdad5c017a58a36c30322be8e95c9fdb7b9f60cf257f848b6e0db646d0`。

### 1.4.1 桌面按钮与真实功能复核

已按 DESK-001–DESK-025、当前桌面/托盘代码和后台测试夹具逐页复核。下载任务页原先会把暂时不可用的“停止、重试、补导 Eagle、文件位置、来源网页”直接隐藏，造成明显的功能丢失感；现已改为五个动作常驻显示，只按真实状态、文件存在性和安全权限置灰。视频号、IDM、配对、网站规则、网络、更新、诊断和托盘动作均确认仍连接原业务方法。完整证据见 [桌面端按钮与真实功能审计](docs/FUNCTIONAL_UI_AUDIT_2026-07-28.md)。

185 项 Python、6 组 Node、17 个活动 JavaScript 和两份扩展清单已通过；1.4.1 重新构建并静默覆盖当前电脑，安装目录后台程序与发行包哈希一致，健康状态为 schema 6、`mediaReady=true`、`desktop_ffmpeg`。本轮 ZIP SHA-256 为 `7ce70b7ed81aa6d77827981c3752984e084dcaa168ecfc46a400804e84994b98`。

- IDM 下载完成接收、SQLite 持久队列、Eagle 导入、离线重试、内容去重与无来源直接导入。
- Chrome 来源捕获、网站规则、离线事件补发、手动记录、单次忽略和持久配对。
- 可视化网站规则列表；右下角菜单可直接打开规则。
- 助手未运行时由 IDM 接收器静默启动；来源最多短暂等待 4 秒，不阻塞无来源导入。
- Windows Forms 原生托盘宿主，避免旧 Python 原生回调导致的 `0xc0000409` 崩溃。
- PyInstaller 独立后端包含 Python/Tcl/Tk；发行包不依赖接收者电脑的 Python。
- 一键安装器自动复制程序、配置 IDM、创建桌面/开始菜单入口、注册卸载项并启动助手。
- 安装器保护 IDM 既有杀毒软件配置，并保存原值供卸载恢复。
- 每台电脑生成一次性 Chrome 自动配对凭据；成功使用后立即删除。
- 文件尚未稳定时明确显示“等待自动重试”，约 2–3 秒内自动继续；已导入任务不能再被手动重试改成“重复跳过”。
- 升级数据库会把此前“同一任务已成功但误标重复”的记录自动恢复为“导入成功”。
- 自动重试设有硬上限：文件稳定检查最多 20 次，其他临时错误最多 12 次，Eagle 等待最多约 1 小时。
- 同一网页的重复来源事件会先合并，再与其他网页比较；不同网页确实无法区分时仍保持来源为空。
- 主程序、冻结后台、托盘、窗口、安装器和 Chrome 扩展统一使用“下载中转站”名称与图标。
- 日常运行使用 `下载中转站后台.exe`，不再启动开发机 `pythonw.exe`；任务管理器文件说明与产品名均为“下载中转站”。
- 主界面与托盘菜单提供检查更新；每天最多静默检查一次，用户确认后才安装。
- 更新清单使用离线私钥签名，客户端内置公钥验签，并核对 ZIP 的 SHA-256、大小和 GitHub 仓库地址。
- 更新前暂存旧程序目录，新版本需通过 20 秒本机健康检查；隔离测试已确认故障时恢复旧版本和配对信息。
- Chrome 扩展每 30 分钟低频确认一次后台版本，只在后台版本更新时自行重载。
- 源码已发布到 `Ly233ly/download-for-eagle`；当前发行统一采用 GPL-3.0，历史 MIT 通知继续保留。运行数据、配对信息、私钥和发行包不进入 Git。
- 89 项自动测试、Eagle 4 新旧健康接口、扩展连接健康端点、配对竞态与共享状态串行化、真实帧内存边界、任务预览/输出路径归属、稳定播放器归组、自适应清单与质量目录、传输分片折叠、补丁升级自重载、首次缓存快照、统一归组计数、全媒体 `desktop` 路由、普通直链本机下载、仅下载、受保护分轨、AES-128 HLS、schema 5 迁移、所有 JavaScript 语法、Chrome/Firefox 清单和上游对照均通过。
- 公开 Apple HLS 与 B 站 DASH 实际拉流、5 秒 streamcopy 合并及 ffprobe 音视频流检查通过。
- 当前电脑 Eagle 4.0.0 Build 20250917 的 V1 健康回退和助手“Eagle 已连接”状态已实机通过。
- schema 5 新载荷已在当前电脑覆盖安装；冻结后台、安装器健康门、关键文件哈希、Eagle 连接、桌面媒体任务页和重启后任务保留均通过。
- 1.1.1 最终 ZIP 为 93,305,651 字节，SHA-256 `2c5650163ebb6b758486f28ef7e34b1640fcb3205b95673e109e7d6f4fdecc14`；覆盖安装后旧根 `background.js`、`popup.js` 和 `media-control.js` 均已清除。
- 真实 IDM 下载、真实 Eagle 导入和 Behance 来源写入此前已验证成功。

## 仍需接收者手动完成

- Chrome 出于安全限制要求用户亲自开启开发者模式并选择“加载已解压的扩展程序”。安装器会自动打开页面和正确文件夹，加载后无需再输入配对码。
- 在扩展工具栏里确认分片默认隐藏、首次打开定位最新项，并对一个 `completed_local` 任务点“导入现有文件”完成 Eagle 闭环。
- 若 IDM 的“病毒扫描程序”已被安全软件占用，安装器不会覆盖，需要用户决定是否改用该入口。

## 已知边界

- IDM 不提供来源网页；浏览器未识别到的下载会按无来源直接导入。
- 未发布到 Chrome 应用商店，Chrome 会显示正常的开发者模式扩展提示。
- 0.5.0 及更早版本需手动运行一次当前安装器；覆盖 1.2.0 后需在 `chrome://extensions` 手动重载一次已加载的解压扩展并刷新来源页。
