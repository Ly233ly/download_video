# 安全说明

## 本机服务

- 桌面 API、控制信号和视频号捕获代理只监听回环地址。
- 浏览器接口验证扩展来源、配对状态和随机令牌。
- Eagle、本机 API 和控制信号不经过远程代理。

## 敏感信息

- Cookie、Authorization、签名 URL、视频号解密信息和临时会话只在当前任务内存中最小化使用。
- 日志、诊断、SQLite 和发行包不得包含上述秘密、配对令牌、用户路径或开发机数据。
- 自动更新不再使用项目私钥；发布权限由 GitHub 账号、仓库权限和正式 Release 承担。维护者账号应开启两步验证或通行密钥，并限制有权发布 Release 的人员。

## 文件安全

- 用户原下载文件、IDM 文件和目录外文件不得移动、删除或修改。
- 临时文件必须具有明确任务归属，且位于程序自己的临时目录。
- 最终副本只有在用户明确选择、Eagle 成功和文件归属复验全部通过时才能删除。
- 导入失败、文件锁定、归属不明和路径越界时必须保留文件。

## 视频号捕获

- 捕获默认关闭，只能由用户明确启动。
- 首次证书信任必须由用户亲自确认 Windows 安全警告。
- 本机代理只终止明确的视频号目标 TLS；其他连接透明转发。
- 启动前保存代理快照，停止、退出、升级和卸载时只恢复程序仍拥有的设置。
- PAC/WPAD 或上游代理无法安全解析时，在改写系统代理前停止。
- 未提交的 feed、签名、Cookie 和解密上下文在停止捕获时清空。

视频号当前身份、代理恢复、签名回退和验收的完整安全边界统一见 [`docs/WECHAT_CHANNELS.md`](docs/WECHAT_CHANNELS.md)。

## 下载与 DRM

- 下载、合并和校验全部在本机执行，不把媒体上传到第三方服务。
- 远程地址必须通过协议、主机和本机地址安全检查。
- DRM 内容只识别和阻断，不实现、迁移或测试绕过能力。
- 本项目仅用于用户有权保存和使用的内容。

## 安装与更新

- 安装器只能操作能够验证归属的程序目录、快捷方式、注册表项、证书和代理设置。
- 更新先备份程序目录，新版本健康检查失败时恢复旧目录。
- 外部证书工具和清理进程必须有超时，超时后终止并中止更新。
- 稳定自动更新只接受固定 GitHub 仓库的正式 Release，并核对版本、唯一资产身份、GitHub SHA-256、声明/实际大小和安全 ZIP 结构。
- Release 元数据禁止跳转；资产只允许一次跳转到 GitHub 官方发行资产域名。下载或解压任一校验失败都必须停止并清理临时文件。
- 这套机制依赖 GitHub 账号和仓库权限的安全性；GitHub 账号被接管时，攻击者可能发布看似正式的新版本，因此两步验证、通行密钥和最小发布权限属于发布安全边界。

发现安全问题时，请通过仓库的私密安全报告渠道联系维护者，不要在公开 issue 中提交令牌、Cookie、签名地址、私钥或用户媒体。
